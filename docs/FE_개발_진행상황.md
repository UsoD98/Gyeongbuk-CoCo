# 프론트엔드 개발 진행상황 (Task 보드)

> **이 파일이 진행상황의 정본이다.** 새 세션을 시작해도 이 파일을 열면 어디까지 됐는지 알 수 있다.
> 상세 사양: [`FE_개발순서.md`](./FE_개발순서.md) · 현재 상태: [`FE_API_현황.md`](./FE_API_현황.md) · 레시피: [`FE_API_연동가이드.md`](./FE_API_연동가이드.md)
> 완료 이력(완료 Task 상세 · 과거 진행 로그 · 헤더 업데이트 이력): [`FE_개발_진행상황_아카이브.md`](./FE_개발_진행상황_아카이브.md) — **다음 Task 판단에는 읽지 않아도 된다.**

> 최종 업데이트: 2026-09-06 (**🏝️ 섬 M 전량 완료 `☑` — M1·M2·M3·M4**. 백엔드가 accessToken 클레임에 `userId` 를 싣기로 확정돼 **섬 M 의 블로커가 해소**됐다(추적표 #4). `utils/jwt.ts`(base64url 페이로드 디코드) 를 신설하고 `authStore.setAuth` 가 인자 없이도 토큰에서 `userId` 를 꺼내게 해 **로그인·재발급·카카오 세 경로가 한 번에** 채워진다. `api/user.ts` 에 GBC006~009 4종 + 도메인 훅 4종(`useUser`·`useNicknameUpdate`·`usePasswordUpdate`·`useAccountDelete`), `pages/User/MyPage` + `routes/userRouter`(`/mypage`, `RequireAuth` 안), 헤더 계정 메뉴에 '마이페이지' 진입. **덤으로 고친 선재 결함**: 인증 화면(`/auth/*`)이 `Layout` 밖이라 `Toaster` 가 없어 **세션 만료 안내·회원가입 완료 toast 가 원래 보이지 않았다** → `AuthScreen` 에 `Toaster` 마운트. 검증: lint·build 통과 · `jwt.ts` 8케이스 · 388/318px 가로 넘침 0 · 터치 타깃 44px. ⏳ 실 백엔드 왕복은 미검증(클레임 미배포 + Java 부재).)
> 이전 업데이트: 2026-09-06 (**📱 R9 완료 `☑`** — 토스트를 폭에 따라 갈랐다. `Toaster.tsx` 가 `toast-top toast-end` **고정**이라 모바일에서 헤더의 계정 메뉴·테마 토글 위를 덮고 엄지에서 가장 먼 모서리에 떴다 → `toast-center toast-bottom` + `sm:toast-end sm:toast-top` 으로 **모바일 하단 중앙 / 640px 이상 종전 우상단**. 하단에 있는 동안만 `pb-[env(safe-area-inset-bottom)]` 로 홈 인디케이터를 피하고(R5 규칙), 렌더를 `slice(-3)` 으로 묶어 스택 높이를 153px 로 상한했다(스토어 미변경 — 넘친 것도 기존 3초 타이머로 제거). daisyUI 5.5.18 이 `sm:toast-*` 를 **미리 생성해 배포**해 Tailwind 스캔과 무관하고, 빌드 CSS 에서 base → `@media (width>=40rem)` 순서로 방출되는 것까지 확인했다. 검증: lint·build 통과 · 390×731 iframe 하니스 — **폭 6종 스윕**(320·390·639 하단 중앙 `bottom:16px`·헤더 겹침 0 / 640·800·1280 우상단 `top:16px`·우측 16px = 종전 동일) · 헤더 버튼 전수 중심 소유 · success/error/info 3종 · **5개 밀어도 렌더 3개** · POI 드로어를 연 채 3개 스택에도 액션 바 두 버튼 중심 소유 유지 · 콘솔 에러 0. ⏳ **범위 밖**: 데스크톱(1280px)에서는 토스트가 여전히 테마 토글 중심을 덮는다 — R9 이전과 같은 상태이고 사양이 "640px 이상은 종전대로"를 못 박아 손대지 않았다.)
> 그 이전: 2026-09-06 (**📱 R8 부분 완료 `◐`** — 터치 타깃 6곳 중 5곳이 44×44px 실측 통과, 마커 토글만 기하 한계로 32px. 상세는 [아카이브 §1](./FE_개발_진행상황_아카이브.md).)
> 그 이전 회차 전량: [`아카이브 §3`](./FE_개발_진행상황_아카이브.md)

## 업데이트 방법

- 작업을 시작하면 `☐` → `◐`(진행중), 끝나면 `☑`(완료)로 바꾼다.
- 완료 시 각 Task의 **DoD**를 만족했는지 확인하고, 하단 **진행 로그**에 한 줄 남긴다.
- 상태 배지: `☐` 대기 · `◐` 진행중 · `☑` 완료 · `⏸` 백엔드 대기(스펙 `보류`/`개발중`)
- 진행률 표는 완료 개수에 맞춰 갱신한다.
- **보드를 가볍게 유지한다**: Task 가 `☑` 가 되면 착수 당시의 구현·검증 서술은 [`아카이브 §1`](./FE_개발_진행상황_아카이브.md)로 옮기고, 보드의 "완료 Task" 표에는 **한 줄 요약**만 남긴다. 진행 로그가 20줄을 넘으면 오래된 회차를 [`아카이브 §2`](./FE_개발_진행상황_아카이브.md)로 옮긴다. 아카이브는 **추가만** 한다(내용 삭제·요약 금지).

---

## 진행률 요약

| 그룹 | 완료 / 전체 | 비고 |
|------|:---:|------|
| Step 0 · 기반 | 4 / 4 | ☑ 완료 — 모든 작업의 선행 |
| 메인 스파인 (코스) | 8 / 8 | ☑ S1~S8 완료 (S8 라이브 E2E까지 확인) |
| 🏝️ 섬 M · 마이페이지 | 4 / 4 | ☑ 완료 (M1~M4) — userId 를 accessToken 클레임에서 획득 |
| 🏝️ 섬 P · POI | 4 / 4 | ☑ 완료 (P0~P3) |
| 🧩 그룹 F · 피드백 반영 | 5 / 9 | **F3·F4·F5·F7·F9 ☑.** F1·F2·F6 `◐` — 각 DoD 의 마지막 조건만 대기(F1·F6 = 로그인 왕복, F2 = 백엔드 미지원). **F8 `◐`** — ㉠(지도 크롬 겹침) 완료, ㉡(밀집 코스 마커) 미착수 |
| 📱 그룹 R · 모바일 UI/UX | 8 / 11 | **R1~R7·R9 ☑**(🔴 심각 3건 + 🟠 4건 + 🟡 1건 해소) + **R8 `◐`**(6곳 중 5곳 44px 통과, 마커 토글만 기하 한계). 남은 `☐` 2개 — 다음은 R10(🟡) |
| 부록 A · 정리(선택) | 4 / 4 | ☑ 완료 (A1·A2·A3·A4) |
| **합계** | **37 / 44** | |

> 🚧 **지금 진행을 막고 있는 것**: 없다 — **그룹 R 의 남은 2개(R10·R11, 둘 다 🟡)가 `☐` 이고 백엔드·로그인 없이 착수 가능**하다(🔴 심각 3건 R1·R2·R3 과 🟠 R4~R7 · 🟡 R9 완료, R8 은 `◐`). 남은 7개는 전부 **`◐`**(F1·F6 = 로그인 왕복 대행, F2 = 백엔드 미지원, F8 = ㉡ 미착수, R8 = 마커 토글 44px 이 기하 한계로 불가 → 설계 판단 필요) 와 `☐` 2개다. **`⏸` 는 0개** — 섬 M 의 `userId` 블로커가 해소돼 M1~M4 를 완료했다(추적표 #4).
> → 백엔드에 넘길 요청서: **[`BE_계약_요청서.md`](./BE_계약_요청서.md)** (R1 `userId` · ~~R2 코스 장소 좌표~~ 수용됨 · ~~R3 찜 상태 조회~~ **수용됨 — 백엔드 0.6.4/0.6.7 이 `liked`·`totalLiked`·`stars` 를 실어 준다, F9 에서 반영** · 부록 B `transport` 영속). 회신이 오면 [`FE_계약_추적표.md`](./FE_계약_추적표.md)를 갱신하고 해당 Task 를 착수한다.
> → ⚙️ **이 개발 환경에는 Java 가 없다**(`java`·`JAVA_HOME` 부재) → 에이전트가 실 백엔드(`../back`)를 직접 띄울 수 없다. 다만 **사용자가 로컬 백엔드를 띄워 주면 라이브 검증이 된다**(2026-08-29 F7 검증이 그렇게 진행됐다 — :8080 기동 후 dev 5173 에서 실데이터 왕복). 로그인이 필요 없는 흐름(코스 생성 GBC010·POI 목록 GBC017·지도)은 이 방식으로 전부 실측 가능하다.
> → 남은 `◐` 4개 중 **로그인이 필요한 항목**(F1 저장 왕복, F6 저장 후 재진입)은 조직 지침상 에이전트가 비밀번호를 입력할 수 없어 **사용자 대행**이다. **모바일 폭은 더 이상 블로커가 아니다** — 브라우저 창은 여전히 1440px 밑으로 리사이즈되지 않지만, **동일 출처 iframe 을 390×731 로 띄우면 미디어쿼리가 실제 모바일 폭에서 평가**되고 Vite dev 모듈 그래프로 스토어를 직접 주입할 수 있어 이번에 F5·F6 을 그 방식으로 실측했다(앞으로도 이 방식을 쓴다). 다만 **OS 수준의 실제 손가락 터치**(pinch-zoom·롱프레스)는 이 툴체인으로 발생시킬 수 없다.

---

# 활성 Task (착수 대상)

> `☐` 대기 · `◐` 진행중 · `⏸` 백엔드 대기. **완료(`☑`) Task 는 아래 "완료 Task" 표로 내려갔다.**

## 📱 그룹 R · 모바일 UI/UX 개선 (2026-08-29 점검, 전량 착수 가능)

> 사용자 요청("데스크탑/모바일 2가지 반응형 중 모바일 측면 개선점")으로 모바일 관점 전수 점검을 수행해 15개 지적을 11개 Task 로 분해했다. 근거·수용 범위는 [`FE_개발순서.md`](./FE_개발순서.md) §그룹 R 참조.
> **백엔드·로그인이 전혀 필요 없다** — 전부 FE 단독으로 착수·검증 가능하며, 검증은 390×731 동일 출처 iframe 하니스(F5·F7 에서 확립한 방법)로 한다.
> 권장 진행 순서: **~~R1~~ → ~~R2~~ → ~~R3~~ → ~~R4~~ → ~~R5~~ → ~~R6~~ → ~~R7~~ → **R8(`◐`)** → ~~R9~~ → R10 → R11** (심각도 순). **R1(2026-08-29) · R2·R3·R4·R5·R6·R7·R9(2026-09-06) 완료** — 상세는 [아카이브 §1](./FE_개발_진행상황_아카이브.md). R2 가 도입한 `useMediaQuery`(`@/hooks/useMediaQuery.ts`)와 R3 이 정리한 **세 패널 상시 마운트 + `hidden` 전환** 구조가 이후 Task(특히 R6)의 전제다. R4 이후로는 **`main` 에 최소 폭이 없다** — 새로 넣는 요소는 좁은 폭에서 스스로 흘러야 한다(`min-w-0`·`truncate`·`flex-wrap`). R5 이후로는 **새 오버레이를 만들면 `useBodyScrollLock(보이는가)` 을 반드시 부른다**(카운팅이라 겹쳐도 안전) — 그리고 `viewport-fit=cover` 가 켜져 있으므로 **화면 가장자리에 붙는 요소는 `env(safe-area-inset-*)` 를 스스로 피해야 한다**. R6 이후로는 **모바일 지도가 기본 잠금**이다 — 지도 위에 무언가를 얹을 때는 `KakaoMap` 의 `interactive`(= `isDesktop || unlocked`)와 컨트롤 스택(`controlsRef`, F8 ㉠ 의 마커 금지 구역)을 함께 본다. R7 이후로는 **`index.css` 의 z 계층 사다리 주석이 정본**이다(본문 0~10 · 헤더 30 · 드로어 40/50 · 토스트 50 · 모달 60/70) — 새 z 를 쓸 때 그 사다리에 맞추고, `.navbar` 가 스택 컨텍스트라 **헤더 안의 z 는 헤더 밖과 비교되지 않는다**는 점을 기억한다. R8 이후로는 **작은 컨트롤에 `.tap-44` 유틸리티(`index.css`)를 쓴다** — 시각 크기를 그대로 두고 히트박스만 44px 로 넓히며, 전제 두 가지가 있다: ①대상이 이미 positioned 이거나 `relative` 를 함께 준다 ②**바로 옆에 다른 탭 대상이 있으면 쓰지 않는다**(그 자리는 방향을 지정한 `before:` 로 바깥쪽으로만 넓히거나 실제 높이를 준다). 둥근 요소 옆에서는 **확장도 둥글게**(`before:rounded-full`) — 히트 판정이 `border-radius` 를 따르므로 사각 확장은 이웃의 중심을 가로챈다. R9 이후로는 **토스트가 모바일에서 화면 하단 중앙**에 뜬다(`sm:` 이상만 우상단) — 모바일 화면 하단에 새 고정 요소(액션 바 등)를 둘 때 `bottom:16px` 부터 최대 153px(3개 상한) 높이의 띠와 겹칠 수 있음을 감안한다.

- [ ] `◐` **R8 · 터치 타깃 44px 미만 정비** 🟠 중요 ▶ — **6곳 중 5곳 DoD 충족 / 마커 토글 1곳만 미달**(2026-09-06). 의존: 없음.
  **한 일**: ①`index.css:21` 의 `.btn-circle{width:24px}` **전역 override 제거** — 근거 없는 규칙이 daisyUI 원형 버튼 전체를 줄여 찜 하트를 **24×32 로 찌그러뜨리고** 있었다(실 CSS 위 A/B 로 재현: override 有 `24×32` → 제거 후 `32×32`). 이 클래스를 쓰는 곳은 `LikeButton` 하나뿐이라 회귀 없음. ②`.tap-44` 유틸리티 신설(`index.css`) — `::before` 로 `max(100%,44px)` 투명 히트박스를 중앙에 깐다. 하트·홈 인원 `−`/`+`·컬렉션 삭제에 적용. ③코스 카드의 시각·금액 인라인 트리거는 **실제 높이 `min-h-11`** 로 줬다 — 투명 확장으로는 위(삭제 `X`)·아래(금액 줄)의 탭 대상을 덮어 도달성이 오히려 나빠진다(카드 98→**142px**, 320px 에서 164px). ④컬렉션 카드 제목 `pr-9`→`pr-12`(넓어진 삭제 히트박스와 글자가 안 겹치게 — Range 로 실측 확인). ⑤`POICard` stacked 하트 `right-2 top-2`→`right-3 top-3` — 카드가 `rounded-2xl`+`overflow-hidden` 이라 8px 자리에서는 44px 상자의 **오른쪽 위 모서리가 둥근 모서리에 잘려** 판정에 걸렸다(실측 후 발견).
  **실측(390×731 iframe 하니스, `getBoundingClientRect` + `elementFromPoint` 중심 스캔)**: 하트 `btn-xs` 원형 24×24→**44×44** ✅ · 하트 `btn-sm` 원형(stacked) 24×32→32×32 CSS/**44×44** ✅ · 하트 드로어(개수형) 70×32→**71×44** ✅ · 홈 인원 −/+ 24×24→**44×45** ✅(확장 모서리에서 눌러도 증감 동작, 가운데 입력칸은 36px 유지) · 컬렉션 삭제 32×32→**45×44** ✅ · 코스 시각 16→**44** ✅ · 코스 금액 24→**44** ✅.
  ⛔ **남은 것 — 마커 토글은 44px 이 기하학적으로 불가능**: 배지(28px)와 토글(22px)의 **중심 거리가 12.7px** 뿐이라 44px 상자는 배지를 거의 다 덮어 **마커 탭(드로어 열기)을 통째로 먹는다**. 배지 반대편(오른쪽 아래)으로만 넓혀 22→**32px** 로 올렸다. 이 작업 중 **실제 회귀를 하나 만들었다가 잡았다** — 처음의 *사각형* 확장은 원형 배지의 중심을 가로채 **마커를 누르면 코스에서 빠지는** 동작이 됐고(하니스 재현), `before:rounded-full` 로 바꾸자 배지 중심 소유가 되돌아왔다(`elementFromPoint(배지 중심) === 배지`, 배지 히트 17×17 로 R8 이전과 동일). F7/F8 의 **'마커 간 40px 이면 도달 보장'** 결론도 다시 계산했다 — 가려짐이 가능한 최대 앵커 거리 **36.80 → 37.26px(＜40)** 로 유지(같은 모델이 R8 이전 값 36.80 을 정확히 재현해 모델 자체를 검증했다). 확장 영역에서의 담기/빼기도 E2E 로 확인(22px 버튼 밖 지점 탭 → 확인 대화상자 → '빼기' → 코스에서 제거). **44px 을 채우려면 마커 디자인 자체를 바꿔야 한다**(토글을 배지에서 떼거나, 선택된 마커에서만 크게 노출) → **F8 ㉡ 과 같은 성격의 설계 판단이라 사용자 결정 대기**.
  📌 **범위 밖으로 남긴 것(사양의 6곳이 아니지만 실측에서 함께 드러난 44px 미만)**: 코스 카드의 장소 삭제 `X`(`btn-xs` 24×24, **파괴적**이라 히트박스를 키우면 오탭 삭제가 늘고 위·아래 줄의 탭 대상과 충돌) · `POICard` 의 '코스 추가' 정사각 버튼(32×32, 양성 동작이라 확장 여지 있음) · 편집 패널의 '완료'/'되돌리기'. **다음 Task 나 별도 지시로 다룰지 판단 필요.** → 순서 §그룹R R8
- [ ] `☐` **R10 · 홈 검색바 모바일 시각 단서 + DatePicker** 🟡 개선 ▶ — 의존: 없음. `Index.tsx` 의 목적지·일정·이동수단·테마 트리거가 `border-base-100 bg-base-100`(배경과 동일)에 `text-xs` 이고, 칸을 나눠 주던 구분선은 `hidden lg:block` 이라 **모바일에서만 사라진다** → 5개 필드가 눌러야 할 곳인지 알 수 없는 텍스트 목록으로 보인다. `react-datepicker` 기본 팝오버도 좁은 화면에서 셀이 작고 잘릴 수 있다. DoD: 390px 폭에서 각 칸이 입력 컨트롤로 식별되고(테두리·최소 높이 48px) 달력이 화면 안에 온전히 들어온다. → 순서 §그룹R R10
- [ ] `☐` **R11 · 폰트 700 · hover 고착 · 메타 태그** 🟡 개선 ▶ — 의존: 없음. ①`index.css` 가 Pretendard **400 하나만** 로드하는데 UI 전반이 `font-bold`/`font-extrabold` → 합성 볼드로 저해상도 모바일에서 뭉갠다. ②POI·컬렉션 카드의 `hover:-translate-y-0.5 hover:shadow-md` 가 터치 후 고착되고 `active:` 피드백은 없다. ③`index.html` 에 `theme-color`(헤더 teal)·`description`·공유 링크(`/share/:id`)용 OG 태그가 없다 — 카카오 공유가 핵심 기능이라 특히 아쉽다. DoD: 700 웨이트가 실제 로드되고, 터치 후 hover 잔상이 없으며 눌림 피드백이 있고, 모바일 브라우저 크롬 색이 헤더와 맞는다. → 순서 §그룹R R11

## 🧩 그룹 F · 사용자 피드백 반영 — 진행 중(`◐`) 4건

> 완료분(F3·F4·F5·F7·F9)은 아래 "완료 Task" 표 · 상세는 [`아카이브 §1`](./FE_개발_진행상황_아카이브.md).
> 아래 4건은 **FE 구현이 끝나고 마지막 조건만 남은** 상태다. 착수 당시의 구현·검증 서술은 [`아카이브 §4`](./FE_개발_진행상황_아카이브.md)에 있다.

- [ ] `◐` **F1 · 코스 시각·체류시간 편집** — FE 완료. 남은 것: **브라우저 저장 왕복 재확인(로그인 필요 → 사용자 대행)**. 서버가 `durationMinutes`·`cost` 를 저장하는 것은 백엔드 소스 실측으로 확인됨(추적표 #5), API 레벨 PATCH→재조회 왕복도 확인됨. → 순서 §그룹F F1 · 상세 [아카이브 §4](./FE_개발_진행상황_아카이브.md)
- [ ] `◐` **F2 · 이동수단 상태화 + 예산 교통비 편집** — FE 완료 / **영속은 백엔드 미지원(`⏸`)**. `PATCH /tour-course/{id}` 바디가 `{schedule}` 뿐이라 `transport`·교통비를 보낼 자리가 없다. 남은 것: 백엔드가 필드를 열어 주면 영속 연결(요청서 부록 B). → 순서 §그룹F F2 · 상세 [아카이브 §4](./FE_개발_진행상황_아카이브.md)
- [ ] `◐` **F6 · 지도에서 코스 편집 — (a)안(마커 토글) 구현 완료** — 드래그 3안은 기각(근거는 아카이브). 남은 것: **저장 후 재진입 유지(DoD 의 영속 절반)가 로그인 필요 → 사용자 대행**. → 순서 §그룹F F6 · 상세 [아카이브 §4](./FE_개발_진행상황_아카이브.md)
- [ ] `◐` **F8 · 남은 도달 불가 2건** — **㉠(지도 컨트롤 겹침) 완료 / ㉡(밀집 코스 마커) 미착수**. ㉡ 은 "위치 왜곡 < 겹침" 설계 결정을 뒤집을지 사용자 판단이 필요하다. 그 외: 합성 드래그 상태에서 관찰된 컨트롤 겹침 1건을 **실제 손 제스처로 재확인** 필요. **로그인·백엔드 없이 착수 가능**. → 순서 §그룹F F8 · 상세 [아카이브 §4](./FE_개발_진행상황_아카이브.md)

---

# 완료 Task (`☑` 37건)

> 한 줄 요약만 둔다. **착수 당시의 구현·검증 상세는 [`아카이브 §1`](./FE_개발_진행상황_아카이브.md)** 에 원문 그대로 있다.

| Task | 한 줄 요약 |
|------|------|
| `☑` **S0-A** 계약 확인 + 추적표 | 계약 4건 스펙 가정으로 진행(비블로킹). `FE_계약_추적표.md` 신설 |
| `☑` **S0-B** API 응답 타입 정의 | `api/tourCourse.ts`(스펙 1:1)·`api/poi.ts`(잠정 + `cat↔contentTypeId` 매핑) |
| `☑` **S0-C** authStore userId 보관 | `authStore.userId`+`setAuth`, 로그인/카카오/재발급 연동 + localStorage 병행 |
| `☑` **S0-D** 공통 상태 컴포넌트 + 훅 | `common/EmptyState·ErrorState·Skeleton` + `hooks/useAsync` |
| `☑` **S1** GBC010 AI 코스 생성 | 홈 검색 → `createCourse` → `loadFromApi` → `/planner`. transport·sigunguCodes·한국어 theme |
| `☑` **S2** GBC016 저장(소유권 이전) | 게스트 courseId sessionStorage → 로그인 게이트 → 복귀 후 자동 `assignCourse` |
| `☑` **S3** GBC011 내 코스 목록 | `getMyCourses` + `useCourseList` + `Collection.tsx` 카드 그리드(4상태) |
| `☑` **S4** GBC012 코스 상세 | `getCourse` + `useCourseDetail` + `plannerStore.loadDetail` + `/planner/:courseId` |
| `☑` **S5** GBC013 코스 삭제 | `deleteCourse` + 재사용 `ConfirmDialog` + `useCourseDelete` |
| `☑` **S6** GBC015 코스 제목 수정 | `updateCourseTitle` + `useCourseTitle`(낙관적·롤백) + `EditableCourseTitle` |
| `☑` **S7** GBC014 공유 + 공개뷰 | `getPublicCourse`(순수 axios로 401 인터셉터 우회) + 가드 밖 `/share/:id` + 카카오 공유/클립보드 폴백 |
| `☑` **S8** GBC020 코스 수정 영속화 | `updateCourse` + `buildSchedulePayload`(schedule 전체 교체) + `useCourseUpdate`. 라이브 E2E 통과 |
| `☑` **M1** GBC006 회원정보 조회 | `utils/jwt.ts`(토큰 클레임에서 userId) + `getUser`/`useUser` + `pages/User/MyPage` + `userRouter`(`/mypage`) + 헤더 계정 메뉴 진입. **조회 결과는 카드로 노출하지 않고**(사용자 요청) 닉네임 프리필·탈퇴 확인 문구에만 쓴다 |
| `☑` **M2** GBC007 닉네임 수정 | `updateNickname` + `useNicknameUpdate` + `NicknameForm`(2~100자 검증 · 저장 후 재조회 · key 리셋) |
| `☑` **M3** GBC008 비밀번호 변경 | `updatePassword` + `usePasswordUpdate` + `PasswordForm`(현재/신규/확인 3필드) → 성공 시 로그아웃 후 재로그인 유도 |
| `☑` **M4** GBC009 회원 탈퇴 | `deleteUser` + `useAccountDelete` + `DangerZone`(`ConfirmDialog` danger) → `authStore.clear()` → `RequireAuth` 가 로그인 화면으로 |
| `☑` **P0** 목→API 교체 준비 | `usePoiList`(P2 교체점)·`usePoi`(P3 교체점) 신설, 소비처를 훅 경유로 |
| `☑` **P1** GBC019 POI 좋아요 토글 | `togglePoiLike` + `poiLikeStore` + `loginGateStore`(게이트 전역화) + `usePoiLike` + `LikeButton` |
| `☑` **P2** GBC017 큐레이션 POI 목록 | `getPois` 실측 타입 1:1 + `usePoiList` 목→API(시군구 병렬 조회·in-flight dedup) |
| `☑` **P3** GBC018 POI 상세 통합 | `getPoi` + `usePoi` 실조회(캐시·dedup·stale 차단) + `utils/poiDetail`(상세는 빈 칸만 채움) |
| `☑` **F3** 수단별 코스 간 이동시간 | `utils/travelTime.ts`(haversine × 우회 1.3 × 수단별 실효속도) + `TravelConnector` |
| `☑` **F4** 좋아요 토글 정합성 조사 | 원인 확정(백엔드는 진짜 토글, 증상은 기수정된 저장 유실 버그) + FE 결함 2건 수정 |
| `☑` **F5** 지도 코스 전용 표시 | 지도 모드를 지역 미선택 early return **앞으로** + `내 코스만 보기` 토글. 컬렉션 진입 지도 미표시 해소 |
| `☑` **F7** 지도 마커 겹침 도달 불가 | `mapCluster.ts` 신설(`clusterMarkers`·`spreadOverlaps`). 실 백엔드 경주 316곳 라이브 실측 통과 |
| `☑` **F9** `liked`·`totalLiked`·`stars` 연동 | 타입 3종 실측 1:1 + `poiLikeStore` 2맵·비덮어쓰기 hydrate + 세션 키 dedup. 로그인 E2E 통과 |
| `☑` **R1** 코스 카드 모바일 스크롤 | `touch-none` 을 카드 루트 → 사진 드래그 핸들로 이동. 리스트 스크롤 가능 면적 40% → 90.8% |
| `☑` **R2** 트리 동시 마운트 제거 | `useMediaQuery`(`useSyncExternalStore`) 신설 + 한쪽 트리만 마운트. KakaoMap 2개 → 1개, `GET /poi` 1회 |
| `☑` **R3** 모바일 탭 상태 유지 | 세 패널 상시 마운트 + `hidden` 전환, `KakaoMap` 0×0 복귀 `relayout()`. 뷰모드·칩·더보기·스크롤·지도 뷰포트 유지 |
| `☑` **R4** 320px 가로 스크롤 제거 | `Layout` 의 `min-w-90` 제거 + `CatBadge`·`CourseItem`·`Collection` 잘림 3건 정리. 320px 10개 화면 가로 스크롤 0 |
| `☑` **R5** 오버레이 스크롤 락·안전영역 | `useBodyScrollLock`(body fixed + 카운팅) 신설 → 오버레이 4종 적용 + `overscroll-contain`·grabber·`viewport-fit=cover`·`env(safe-area-*)` 3곳 |
| `☑` **R6** 지도 페이지 스크롤 삼킴 해소 | 모바일 지도 기본 잠금(`setDraggable`/`setZoomable` + `.map-locked` 로 SDK 인라인 `touch-action:none` 무력화) + '지도 조작' 토글. 마커 탭·토글 무회귀 |
| `☑` **R7** 헤더 팝오버 z 계층 정리 | `.navbar` 를 `z-30` 스택 컨텍스트로 + 모바일 메뉴 `z-20`·계정 드롭다운 `z-30`. z 사다리를 `index.css` 주석으로 문서화. 6화면 전수 통과 |
| `☑` **R9** 토스트 위치 반응형 | `toast-center toast-bottom` + `sm:toast-end sm:toast-top` — 모바일 하단 중앙 / 640px↑ 종전 우상단. `env(safe-area-inset-bottom)` + 렌더 3개 상한. 폭 6종 실측 |
| `☑` **A1** `/home` 더미 라우트 제거 | `router.tsx` 라우트 블록 + `pages/Home.tsx` 삭제 |
| `☑` **A2** About 콘텐츠 작성 | 서비스 소개 페이지(히어로·핵심기능·이용방법·CTA) + 푸터 링크·저작권 정정 |
| `☑` **A3** 문서 드리프트 최신화 | `CLAUDE.md`·`PRD_FRONT.md`·`FEATURES_FRONT.md` 를 실코드에 맞춤 + 보드 포인터 |
| `☑` **A4** RootLayout 정리 판단 | CLAUDE.md 규칙상 존치 결정. 의도된 플레이스홀더임을 주석으로 문서화 |

---

## 진행 로그

> 완료할 때마다 `- YYYY-MM-DD · <TaskID> 완료 · <한 줄 메모>` 형식으로 추가.
> **2026-08-22 이전 전량은 [`아카이브 §2`](./FE_개발_진행상황_아카이브.md) 로 옮겼다.** 20줄을 넘기면 오래된 회차부터 계속 옮긴다.

- 2026-08-29 · **F7 완료(`☑`)** · 사용자가 로컬 백엔드를 띄워 줘 **실 백엔드 경주 316곳**으로 마지막 조건(그룹을 펼친 뒤 도달 가능성)을 실측했다. 390px iframe 하니스에서 코스 3회 생성(#70·#74·#75) → 초기 뷰부터 그룹이 없어질 때까지 매 단계 전수 `elementFromPoint` 판정: **14/14 → 19/19 → 20/20 → 10/10**, 재현 **16/16 → 21/21 → 22/22 → 13/13**, 가려짐 0. 지난 세션의 "펼친 뒤 마커가 빈다"는 관찰은 **하니스 탓으로 확정**(DoD 흐름 밖인 `축소` 연타 상태에서만 재현). 보강으로 실제 DOM 기하·z 규칙을 옮긴 **도달 가능성 시뮬레이터**(node, 392상태)를 만들어 현실형 데이터셋 전 단계 통과를 확인했다.
- 2026-08-29 · **F8 신설(`☐`)** · F7 검증에서 발견한 잔여 도달 불가 2건 — ㉠ 지도 컨트롤(`z-[3]` 32×108px)이 오른쪽 아래 마커를 덮어 누르면 확대가 실행됨(데스크톱 1280px 실측 재현) ㉡ 한 Day 코스가 5곳 이상 반경 300m 안에 몰리면 `spreadOverlaps` 가 빈 자리를 못 찾아 겹침이 남음(시뮬레이터 경계: 5~6곳=그룹 배지 가려짐, 7곳+=코스 토글 가려짐 · 라이브 3회에서는 미재현). 로그인·백엔드 없이 착수 가능.
- 2026-08-29 · **F8 ㉠ 완료(`◐`)** · 지도 크롬이 마커를 가려 담기 대신 확대가 눌리던 문제를 해소했다. `spreadOverlaps` 에 금지 구역을 더하고(`anchorKeepOut` 으로 클릭 대상 중심 기준 부풀리기 · `canPlace` 로 겹침 회피와 한 경로), `KakaoMap` 은 `idle` 마다 배율과 금지 구역을 한 번의 `getBounds()` 로 함께 읽는다. 검증 중 **카카오 SDK 축척·로고(좌하단 135×19)도 마커를 가리는 것을 새로 발견**해 같은 경로로 해소(`chromeRects` = 컨트롤 + 컨테이너 직속 자식 중 전체 크기가 아닌 것). lint·build 통과 · node 14케이스 · 금지 구역 1218자리 전수 해소 · 82상태에서 컨트롤 가려짐 14→0 / 그 외 겹침 108→108(㉡ 불변) · 라이브에서 `경주 재매정` 이 43px 비켜나 도달 가능으로 전환. ㉡(밀집 코스 마커)은 설계 결정을 뒤집는 선택이라 미착수로 남겼다.
- 2026-08-29 · **F9 신설·완료(`☑`)** · 사용자 요청으로 백엔드 신규 필드 3종을 연결했다. 확인 결과 셋 다 미반영이었고 그중 **토글 응답 필드 개명(0.6.5 `likes`→`totalLiked`)은 실결함**이었다 — `usePoiLike` 가 `res.likes`(존재하지 않는 필드)를 읽어 좋아요 수가 `undefined` 로 유실. `api/poi.ts`(타입 3종 실측 1:1) · `poiLikeStore`(`{liked,totalLiked}` + `hydrate` — 이미 아는 값은 덮지 않아 토글 결과가 낡은 응답에 되돌려지지 않는다) · `usePoiList`/`usePoi`(응답 hydrate + **세션 키**로 조회 dedup·상세 캐시 분리, `authSessionKey()` 를 `authStore` 로 승격) · `usePoiLike`(응답으로 확정, 재조회 없음) · `utils/poiDetail`(별점은 빈 칸만 채움) · `Stars`(0이면 렌더 안 함 → 실 POI 의 `★ 0` 제거) · `LikeButton showCount`(드로어에서만 총개수). lint·build 통과 + node 19케이스. ✅ 라이브(실 백엔드 경주 316곳): 비로그인 — 별점 전건 일치·`★ 0` 제거·게이트 시 `/like` 0건. **로그인 E2E(사용자 대행 로그인)** — 토글 200 `{liked,totalLiked}` 즉시 반영(카드·드로어 동시)·새로고침 후 하트 유지·드로어 재오픈 캐시 히트에도 토글 결과 보존·해제 200·세션 전환(로그아웃 제거 → 재로그인 복구)·콘솔 에러 0. 추적표 **#9 해결**.
- 2026-08-29 · **📱 그룹 R 신설(11개 `☐`) + 보드 최적화** · 사용자 요청으로 모바일 관점 UI/UX 전수 점검(레이아웃·터치·오버레이·마운트 구조 정독 + `touch-none`·`vh`·`safe-area`·브레이크포인트 grep). **🔴 심각 3건**: ①`CourseItem.tsx:87` 카드 루트 `touch-none` → 모바일 코스 탭 스크롤 불가 ②`Planner.tsx:186/210` 데스크톱·모바일 트리 동시 마운트(KakaoMap 2개·POI 중복 조회 — `usePoiList.ts:57` dedup 주석이 이미 인정) ③`Planner.tsx:232` 조건부 렌더로 탭 전환마다 `ResultsPanel` 언마운트 → 뷰모드·카테고리·더보기·스크롤·지도 줌 전멸. **🟠 중요 5건**: `Layout.tsx:25 min-w-90`(320px 가로 스크롤)·오버레이 스크롤 체이닝+`env(safe-area-*)` 0건·지도가 페이지 스크롤 삼킴·`HeaderLayout.tsx:148` z-index 누락·터치 타깃 6곳 44px 미만(`index.css:21 .btn-circle{width:24px}` 전역 override 가 `LikeButton` 을 찌그러뜨림). **🟡 개선 3건**: 토스트 위치·홈 검색바 모바일 시각 단서(구분선이 `hidden lg:block` 이라 모바일에서만 사라짐)+DatePicker·폰트 700 미로드/hover 고착/메타 태그. → R1~R11 로 분해해 보드와 `FE_개발순서.md` §그룹 R 에 등록(전부 백엔드·로그인 불필요). **동시에 보드 최적화**: 151KB → 26KB. 완료 Task 상세·2026-08-22 이전 진행 로그(56줄)·헤더 "이전 업데이트" 28줄·`◐` 4건의 구현 서술을 `FE_개발_진행상황_아카이브.md` 로 **원문 그대로** 이관하고, 보드엔 활성 Task 상세 + 완료 25건 한 줄 요약표만 남겼다. `.claude/skills/next-task/SKILL.md` 도 그룹 R 우선순위·아카이브 분리 규약을 반영.
- 2026-08-29 · **R1 완료(`☑`)** · 모바일 코스 탭 스크롤 불가 해소. `CourseItem.tsx` 카드 루트의 `touch-none` 을 사진 드래그 핸들 div 로만 옮겼다 — `touch-action` 은 포인터를 가로챌 요소에만 거는 것이 dnd-kit 권장 형태이고, 루트에 걸면 카드 전면이 터치를 삼켜 `gap-2.5`(10px) 틈으로만 스크롤됐다. 390×731 iframe 하니스에 8개 항목 코스를 주입해 리스트 영역을 6px 격자로 전수 스윕(실효 `touch-action` = 히트 요소→스크롤 컨테이너 교집합): **스크롤 가능 면적 40% → 90.8%**, 차단 셀 1608 → 247(= 의도된 사진 핸들). 카드 루트에 `touch-action:none` 을 되돌리면 40% 가 재현돼 인과 확정. 재정렬 회귀 없음(모바일 390px·데스크톱 1280px 모두 `T1,T2,T3 → T2,T3,T1`), 인라인 편집·콘솔 에러 0. 덤으로 알아낸 것 둘: ①`left_click_drag` 는 dnd-kit 을 구동하지 못한다(수정 전에도 동일 실패 → 하니스 한계) — 앞으로 DnD 검증은 `PointerEvent` 직접 디스패치로 한다. ②`useSortable` 의 `setNodeRef` 가 사진 div 에 있어 **드롭 판정 사각형이 64×64 사진 영역**이다(카드 전체가 아니다) — 드롭 목표를 사진에 맞춰야 재정렬이 성립한다. `select-none` 은 루트에 존치: 카드 안 입력은 전부 `time`/`number` 라 텍스트 선택 API 자체가 없어 실효 영향이 없고, 편집·포커스는 실측으로 정상.
- 2026-09-06 · **R2·R3 완료(`☑`)** · 모바일 플래너의 🔴 심각 2건을 한 묶음으로 해소했다. **R2**: `hooks/useMediaQuery.ts` 신설(`matchMedia` 구독을 `useSyncExternalStore` 로 감싸고 `MediaQueryList` 를 쿼리별 캐시 · 데스크톱 경계는 `DESKTOP_MEDIA_QUERY='(min-width: 1024px)'` 상수 한 곳에서 Tailwind `lg` 와 맞춤) → `Planner` 가 `isDesktop ? desktopTree : mobileTree` 로 **한쪽만 마운트**하고 `hidden`·`lg:flex`·`lg:hidden` 을 걷어냈다(`PoiDrawer`·`LoginGateModal` 은 두 트리 밖 공통). `usePoiList` 의 in-flight dedup 은 **존치 결정** — 동시 마운트 원인은 사라졌지만 dev StrictMode 이중 effect 와 시군구 병렬 호출이 남고 `GET /poi` 는 TourAPI 라이브라 동시 호출이 503 을 낸다(주석 근거만 갱신). **R3**: 모바일 세 패널을 **모두 마운트**하고 `hidden` 으로만 전환(탭에 `aria-selected` 추가). 지도는 `KakaoMap` 에 `syncSize()` 를 두어 **0×0 이었다가 돌아오는** 전환에서만 `relayout()` 하며, 호출처를 `useLayoutEffect`(리렌더 동반 전환)와 `ResizeObserver`(창 리사이즈·회전) 둘로 나눴다 — 처음엔 관찰자만 뒀다가 배경 탭에서 rAF 동결로 발화하지 않는 것을 격리 실험으로 확인하고 렌더 경로를 보탰다. 검증: lint·build 통과 · 390×731 iframe 하니스에 **실 백엔드 라이브**(경주 314곳) — 결과→코스→예산→결과 왕복 후 다섯 항목 완전 일치(`리스트`·`관광지`·`더 보기 120/132`·cards 120·scrollTop 1500) · 지도 level 8·중심 35.81211,129.27508·343×423 유지에 인스턴스 재생성 0 · `relayout()` 숨김 중 0회/복귀 1회 · 1280px 데스크톱 3분할 정상 · 인라인 편집(`10:45`)·예산 탭 정상 · 콘솔 에러 0. **인과는 `git stash` 로 수정 전 코드를 서빙해 양쪽 다 재현**(KakaoMap 루트 2개 · 왕복 후 `지도`·`전체`·cards 0). ⚠️ 자동화 탭은 `visibilityState:'hidden'` 이라 **rAF 가 동결**돼 `matchMedia` change·`ResizeObserver`·dnd-kit 드롭이 오지 않는다 — 둘 다 **수정 전 코드에서도 동일 실패**해 하니스 한계로 확정했고, 실시간 리사이즈 전환은 리렌더를 일으켜 `getSnapshot` 재평가로 확인했다.
- 2026-09-06 · **R4 완료(`☑`)** · 320px 기기(iPhone SE 1세대·폴드 접힘)에서 페이지 전체가 가로로 밀리던 문제를 없앴다. 원인은 `Layout.tsx` 의 `min-w-90`/`lg:min-w-90`(=360px) — 주석은 "모바일에서도 레이아웃이 깨지지 않도록"이었지만 실제로는 **깨짐을 가로 스크롤로 바꿔 놓은** 것이었다. 걷어내고 방침(넘침은 각 요소에서 `min-w-0`·`truncate`·`flex-wrap`)을 같은 자리에 주석으로 남겼다. 스크롤이 0 이 된 뒤에야 보이는 잘림 3건도 함께 고쳤다 — ①`CatBadge` 가 flex 자식으로 눌려 `관광지` 가 '관광 / 지' 두 줄이 되던 것(컴포넌트에 `shrink-0 whitespace-nowrap` → `CourseItem`·`POICard`·`PoiDrawer` 동시 해결) ②`CourseItem` 메타 행에서 방문 시각이 `0…` 으로 truncate 되던 것(행을 `flex-wrap` 으로 → 390px·1280px 에서는 종전대로 한 줄, 실측 배지 top 505 / 시각 top 507) ③`Collection` 카드 기간이 `2박 3 / 일` 로 글자 단위 분해되던 것(조각별 `whitespace-nowrap` + 행 `flex-wrap`). 검증: lint·build 통과 · **320×640 동일 출처 iframe 하니스 + 실 백엔드 라이브**(사용자가 :8080 을 띄워 둔 상태에서 `POST /tour-course` 로 경주 3일 코스 **#102** 신규 생성 · `GET /poi` 314곳) — 홈·소개·로그인·회원가입·공유·플래너(결과(지도)·코스·예산·결과·결과(리스트)) **10개 화면 전부 가로 스크롤 0 / 삐져나온 요소 0**(프로브는 클리핑 조상이 있는 지도 타일 등을 제외해 오탐을 걸렀다), 오버레이도 254px 폭으로 온전(`LoginGateModal`·`SettleModal` 실측). **인과 확정**: `main{min-width:22.5rem}` 를 다시 주입하면 `scrollWidth 360 / clientWidth 305` = **55px 가로 스크롤이 그대로 재현**되고 걷어내면 0 이다. 회귀: 390×731 다섯 화면 0/0 + 코스 카드 메타 한 줄 유지 · 1280×900 데스크톱 3분할(코스 패널 + 실지도 + 마커·경로선) 정상 · 콘솔 에러 0. 컬렉션은 로그인이 필요해(조직 지침) `apiClient.defaults.adapter` 를 **런타임에만** 갈아 끼워 GBC011 목록을 대역하고 카드 2장을 320px 에서 잰 것이다(코드 미변경) — 기간 행 잘림은 그 과정에서 발견했다. ⏳ 범위 밖으로 남긴 것: `PoiDrawer` 사진 위 `CatBadge` 의 낮은 대비(`bg-cat-sight/12`, 폭과 무관).
- 2026-09-06 · **R6 완료(`☑`)** · 모바일에서 지도가 페이지 세로 스크롤을 통째로 삼켜 예산 탭·푸터로 내려갈 수 없던 문제를 해소했다. 사용자가 사양의 **1안**(구글맵 임베드 관례 — 기본 비활성, 명시로 켜기)을 택해, `useIsDesktop()`(R2) 으로 모바일에서만 `interactive=false` 로 열고 `map.setDraggable(false)`·`setZoomable(false)` 를 건다. 켜는 경로는 **컨트롤 스택 맨 위의 '지도 조작' pill 버튼**(`aria-pressed`, 해제 시 '지도 잠금') — 지도를 덮는 탭-투-활성화 오버레이는 잠긴 동안 마커 탭·담기/빼기 토글까지 막아 DoD("활성화 전에도 종전대로")와 충돌한다. 버튼을 `controlsRef` 안에 둔 덕에 F8 ㉠ 의 마커 금지 구역에 자동 포함된다. 확대/축소 버튼은 `setLevel` 직접 호출이라 잠긴 채로도 동작한다. **검증 중 결정적 사실 하나를 실측했다** — 카카오 SDK 는 `setDraggable(false)` 뒤에도 지도 내부 div 에 `touch-action: none` 을 **인라인으로** 박아 두고, 터치 판정은 히트 요소와 조상의 교집합이라 컨테이너의 `touch-pan-y` 로는 덮이지 않는다(= SDK 설정만으로는 실기기에서 여전히 안 내려간다). 그래서 잠긴 동안만 `.map-locked, .map-locked *{touch-action:pan-y!important}`(`index.css`)로 후손까지 되돌렸다. 검증: lint·build 통과 · 390×731 iframe 하니스(카카오 실지도, 코스는 `plannerStore.setState` 주입) — 잠김: 체인 전체 `pan-y`·`none` 후손 0·합성 드래그 −120px 에 마커 3개 좌표 완전 불변·wheel `defaultPrevented=false`·문서 스크롤 여유 153px·끝까지 내리면 푸터가 화면 안(top 679/bottom 731) / 해제: `touch-action:auto`·SDK 인라인 `none` 부활·같은 드래그에 마커가 **정확히 −120px** 이동·wheel 소비 / 재잠금 전부 복귀. 잠긴 상태 회귀 없음 — 마커 탭 → 드로어(불국사), 토글 → 확인 대화상자(취소 시 코스 `['a','b','c']` 불변), 확대/축소 동작. 데스크톱 1280 무회귀(버튼 없음·드래그 −80px 정확), 390→1280→390 왕복 후 **잠금 기본값 복귀**(폭 전환은 effect 대신 렌더 중 조정 — lint 가 effect 내 `setState` 를 막는다), 320px 가로 스크롤 0(R4 무회귀), 다크 테마 토큰 반전, 콘솔 에러 0. ⏳ **미검증**: 실제 손가락 스와이프 — `computer.scroll` 은 좌표와 무관하게 **최상위 문서**를 움직여(부모 `overflow:hidden` 에서도 iframe 0 / 부모 36px 로 확정) 트러스트된 터치·휠을 iframe 안에 만들 수 없다. 브라우저 몫은 메커니즘(실효 `pan-y` + SDK 무소비 + 스크롤 여유)으로 대체 검증했고 실기기 확인은 사용자 몫으로 남긴다.
- 2026-09-06 · **R5 완료(`☑`)** · 오버레이가 열린 채 배경이 따라 움직이던 것(스크롤 체이닝)과 하단 안전영역 미대응을 함께 해소했다. 핵심은 `hooks/useBodyScrollLock.ts` — `body{overflow:hidden}` 은 **iOS Safari 가 무시**하므로 `position:fixed; top:-scrollY` 로 화면에 고정해(보이는 그림은 그대로) 문서 스크롤 자체를 없애고, 풀 때 그 y 로 되돌린다. 오버레이는 겹쳐 열릴 수 있어(드로어 위 로그인 게이트) **모듈 스코프 카운터**로 세어 처음 하나가 걸고 마지막 하나가 푼다 — 중간에 하나만 닫혔다고 배경이 풀리면 남은 오버레이 뒤가 다시 움직인다. `body` 고정으로 사라지는 문서 스크롤바 폭은 `paddingRight` 로 메워 데스크톱 콘텐츠가 튀지 않게 했다. 적용처는 넷 — 사양이 지목한 `PoiDrawer`·`ConfirmDialog`·`LoginGateModal` 에 **`SettleModal` 을 더했다**(같은 계열 오버레이라 빼면 예산 탭에서만 배경이 움직인다). 그 밖에 드로어 본문 스크롤 컨테이너에 `overscroll-contain`, 시트 상단에 grabber(40×4 흰 반투명 pill, 사진 위라 그림자 · `lg:hidden`), `index.html` 에 `viewport-fit=cover`. cover 는 노치 아래까지 그리는 대가로 안전영역을 각자 피하게 만들므로 세 자리를 함께 손봤다 — 드로어 액션 바와 푸터에 `pb-[max(1rem,env(safe-area-inset-bottom))]`, 셸(`Layout` 래퍼)에 좌우 `env(safe-area-inset-left/right)`(가로 모드 노치). 검증: lint·build 통과 · dist CSS 에 `env(safe-area-inset-*)` 8건·`overscroll-behavior:contain` 2건 emit 확인 · **390×731 iframe 하니스 라이브**(백엔드가 이번엔 전 엔드포인트에 401 을 내 코스는 `plannerStore.loadFromApi` 로 주입하고 컬렉션은 R4 와 같이 `apiClient.defaults.adapter` 를 **런타임에만** 갈아 끼워 대역했다 — 코드 미변경) — ①네 오버레이 전부 열림 중 `body{position:fixed}`·문서 비스크롤, 스크립트 `scrollTo` 와 `wheel` 어느 쪽으로도 배경 `scrollY` 불변, 닫으면 원위치 복귀(**120→0→120 · 140 · 100 · 41.6px 소수점까지**)하고 인라인 스타일이 빈 문자열로 완전 정리 ②**겹침 시나리오**(드로어 → 게이트 → 게이트만 닫음)에서 배경이 `fixed`·`top:-120px` 그대로 유지되고 드로어까지 닫아야 120 으로 복귀 ③드로어 본문을 904/338 오버플로 상태로 만들어 끝까지 스크롤(`scrollTop 564.8`) 후 추가 `wheel` 5회에도 배경 불변, 컨테이너 `overscroll-behavior` 실측 `contain` ④grabber 실측 40×4 @ 시트 상단 +8px(스크린샷으로도 확인) · 액션 바 `padding-bottom` 16px(inset 0 → `max(1rem,0)`, 즉 규칙이 실제로 적용된 증거 — `p-4` 를 `px-4 pt-4` 로 쪼갰으므로 규칙이 없으면 0 이 된다) ⑤스크롤바 보정 실측: 1280×420 에서 gutter 15px → 잠금 중 `padding-right:15px`, `main` 우변 1264.8→1265(튐 없음) ⑥a11y 회귀 없음 — Escape·오버레이 클릭·X 버튼 **세 경로 모두** 닫히고 잠금이 풀린다 ⑦390/320/1280 가로 스크롤 전부 0(R4 회귀 없음), 셸 좌우 패딩·푸터 하단 패딩은 inset 0 에서 종전과 동일. ⏳ **미검증**: 데스크톱 Chrome 은 `env(safe-area-inset-*)` 가 전부 0 이라 **0 아닌 실제 노치 기기에서의 이동량**은 확인하지 못했다(사용자 실기기 확인 필요). ⏳ **범위 밖으로 판단해 미구현**: 스와이프 다운 닫기 — 사양이 '선택 범위'로 두고 착수 시 판단하라고 한 항목이다. 닫는 수단은 이미 셋(X·오버레이·Escape)이고 grabber 로 '시트 상단' 단서는 확보됐다. 반면 자동화 탭은 `visibilityState:'hidden'` 이라 rAF 가 동결돼 **실제 손 제스처를 만들 수 없고**(R1·R2·R3 에서 확정된 하니스 한계), 드래그 임계값·관성·`touch-action` 상호작용은 검증 없이 넣으면 R1 이 고친 스크롤 차단을 되돌릴 위험이 크다. ⚠️ 검증 중 관찰: 자동화 탭에서는 CSS 등장 애니메이션(`coco-slide-up`)이 첫 프레임에 얼어붙어 시트가 24px 아래에 머문다 — 기하 측정 시 `animation:none` 으로 무력화했다(레이아웃 규칙은 불변). 제품 결함이 아니라 하니스 한계다.
- 2026-09-06 · **R8 부분 완료(`◐`)** · 터치 타깃 6곳 중 **5곳을 44×44px 로 올리고 마커 토글 1곳은 기하 한계로 32px** 에서 멈췄다. 뿌리는 `index.css:21` 의 `.btn-circle{width:24px}` **전역 override** — 근거 없는 규칙이 daisyUI 원형 버튼 전체를 줄여 찜 하트를 **24×32 로 찌그러뜨리고** 있었고(실 CSS 위 A/B 로 재현), 이 클래스를 쓰는 곳은 `LikeButton` 하나뿐이라 그냥 걷어냈다. 대신 `.tap-44` 유틸리티를 두어 **시각 크기는 그대로, 히트박스만 44px**(하트·홈 인원 −/+·컬렉션 삭제). **코스 카드의 인라인 트리거만은 투명 확장을 쓰지 않았다** — 시각(16px)·금액(24px) 두 줄의 중심 간격이 28px 뿐이라 44px 상자끼리 겹치고, 위쪽 삭제 `X`(24px)까지 덮어 **도달성이 오히려 나빠진다**(DOM 순서상 나중 요소가 이긴다). 그래서 두 트리거에 실제 높이 `min-h-11` 을 줬고 카드가 **98 → 142px**(320px 에서 164px) 로 커졌다. 부수로 두 가지를 실측에서 발견해 고쳤다: ①컬렉션 카드 제목 `pr-9`→`pr-12`(넓어진 삭제 히트박스가 제목 글자와 겹치지 않게 — Range 로 텍스트 줄 사각형과 44px 상자의 교차 0 확인) ②`POICard` stacked 하트 `right-2 top-2`→`right-3 top-3`(카드가 `rounded-2xl`+`overflow-hidden` 이라 8px 자리에서는 **44px 상자의 오른쪽 위 모서리가 둥근 모서리에 잘려** 판정 실패). ⛔ **마커 토글은 44px 이 불가능**: 배지(28px)와 토글(22px)의 중심 거리가 **12.7px** 뿐이라 44px 상자는 배지를 거의 다 덮어 마커 탭(드로어 열기)을 먹는다. 바깥쪽으로만 넓혀 22→**32px**. 이 과정에서 **회귀를 하나 만들었다가 잡은 것이 이번 검증의 핵심**이다 — 처음 넣은 *사각형* 확장은 원형 배지의 중심(대각선상 사각형 안이지만 원 밖이라 지금까지 살아 있던 지점)을 가로채 **마커를 누르면 코스에서 빠지는** 동작이 됐고(하니스 재현: `elementFromPoint(배지 중심)` 이 토글을 반환), `before:rounded-full` 로 확장을 원형으로 바꾸자 확장 원의 중심이 배지 바깥으로 밀려나 배지 중심 소유가 복구됐다(배지 히트 17×17 = R8 이전과 동일). F7/F8 의 **'마커 간 40px 이면 도달 보장'** 도 다시 계산했다 — 도형별 `max|p−t|` 로 '가려짐이 가능한 최대 앵커 거리'를 재면 **36.80 → 37.26px(＜40, 여유 2.74px)**. 같은 모델이 R8 이전 값으로 아카이브에 적힌 **36.80px 을 정확히 재현**해 모델 자체를 먼저 검증했다. 검증: `npm run lint`·`npm run build` 통과 · **빌드 CSS 에 `before:rounded-full`·`before:-right-2.5`·`before:-bottom-2.5`·`before:top-0`·`before:left-0`·`before:absolute`·`before:content-[`·`tap-44`·`min-h-11` 전량 emit 확인**(마커는 명령형 DOM 이라 Tailwind 스캔 누락이 치명적) · 390×731 iframe 하니스 6곳 실측(위 표) · 홈 ±는 **확장 모서리(중심에서 20px)에서 눌러도 증감**하고 가운데 입력칸은 36px 유지 · 마커 토글은 **22px 버튼 밖 지점 탭 → 확인 대화상자 → '빼기' → 코스에서 제거** E2E 통과 · 320px 가로 스크롤 0(R4 무회귀) · 1280px 데스크톱 무회귀(stacked 하트 44×44 통과) · 다크 테마 · 콘솔 에러 0. 백엔드가 없어 `GET /poi`·`GET /tour-course` 는 `apiClient.defaults.adapter` 를 **런타임에만** 갈아 끼워 대역했고(코드 미변경), 컬렉션 가드는 더미 문자열 토큰으로 렌더만 띄웠다(실제 로그인·자격증명 아님). ⏳ **남긴 것**: ①마커 토글 44px — 마커 디자인을 바꾸지 않는 한 불가, F8 ㉡ 과 같은 설계 판단이라 사용자 결정 대기 ②사양의 6곳 밖에서 실측으로 드러난 44px 미만 3종(코스 카드 장소 삭제 `X` 24px — **파괴적이라 확장이 오탭 삭제를 늘린다**, `POICard` '코스 추가' 정사각 32px, 편집 패널 '완료'/'되돌리기') ③실기기 손가락 터치는 이 툴체인으로 만들 수 없어 미검증(기존 하니스 한계와 동일).
- 2026-09-06 · **R7 완료(`☑`)** · 헤더 팝오버가 본문 요소에 깔리던 스택 순서 결함을 없앴다. 뿌리는 `.navbar` 가 `position:relative` 만 갖고 **z-index 가 없어 스택 컨텍스트를 만들지 못한 것** — 헤더 팝오버와 `main` 의 positioned 요소가 같은 루트 무대에서 겨루고, `z-index:auto` 인 팝오버는 페인트 순서상 `z-10` 아래로 깔린다. `.navbar{z-index:30}`(index.css, `position` 과 같은 자리)으로 헤더를 스택 컨텍스트로 만들고 모바일 메뉴 `z-20`·계정 드롭다운 `z-1`→**`z-30`**. **계정 드롭다운은 사양에 없던 추가 판단**이다 — 두 팝오버는 실제로 겹쳐서(드롭다운 `y 58..107` vs 메뉴 `y 64..185`), 메뉴만 올리면 버거 메뉴를 연 채 계정 아이콘을 눌렀을 때 드롭다운이 메뉴 뒤로 숨는 **새 회귀**가 난다(하니스에서 `z-1` 로 되돌려 재현 확인). 겸해 프로젝트 z 계층 사다리(본문 0~10 · 헤더 30 · 드로어 40/50 · 토스트 50 · 모달 60/70)를 `index.css` 주석으로 문서화했다 — 앞으로 새 z 는 여기에 맞춘다. 검증: lint·build 통과 · 390×731 iframe 하니스. **판정 오라클부터 검증**했다 — 메뉴를 덮는 `z-10` `fixed` 프로브(홈 검색바 드롭다운·컬렉션 삭제 버튼과 동일 조건) + `elementFromPoint` 로, `.navbar` 의 z 를 `auto` 로 되돌리면 프로브가 이기고(버그 재현) 되돌리면 메뉴가 이긴다. **화면 6종 전수 통과**: 홈·플래너 3탭(결과·코스·예산, 지도 포함)·컬렉션·공유·소개 — 메뉴 항목 2건 모두 `ok:true`. **합성이 아닌 실제 요소로도 확정**: 컬렉션에 코스 3건을 실으면 첫 카드 삭제 버튼(`Collection.tsx:102` `z-10`, `y 178..210`)이 메뉴 띠(`y 64..185`)와 7px 겹치는데, 겹침점 (333,181) 에서 **R7 이전엔 삭제 버튼이 이겼다**(= 메뉴를 탭했는데 삭제 확인 다이얼로그로 갔을 자리) → 이후엔 메뉴가 이긴다. **오버레이 3종 모두 메뉴·헤더 위**: 토스트(`z-50`)·POI 드로어(백드롭 `z-40` 이 네비바를 덮고 시트 `z-50` 이 메뉴를 덮음)·로그인 게이트 모달(`z-60/70` 이 양쪽 다 덮음). 폭 1280/390/**320** 무회귀(`navZ 30`·계정 드롭다운 정상·`scrollWidth==clientWidth` 로 R4 무회귀), 다크 테마 동일, 콘솔 에러 0. 백엔드가 없어 가드가 걸리는 화면은 더미 문자열 토큰(`setAuth('dummy-ui-fixture',1)` — 실제 로그인·자격증명 아님)과 axios 어댑터 스텁으로 렌더만 띄웠고 코드는 건드리지 않았다.
- 2026-09-06 · **R9 완료(`☑`)** · 토스트가 폭과 무관하게 우상단 고정이라 모바일에서 헤더의 계정 메뉴·테마 토글을 덮던 것을 갈랐다 — `Toaster.tsx` 를 `toast-center toast-bottom` + `sm:toast-end sm:toast-top` 으로 두어 **모바일 하단 중앙 / 640px 이상 종전 우상단**. 토스트는 **탭으로 닫히는 `<button>`** 이라 자리 자체가 도달성 문제였다. 하단에 있는 동안만 `pb-[env(safe-area-inset-bottom)] sm:pb-0` 로 홈 인디케이터를 피한다(컨테이너가 `bottom:1rem` 고정이라 padding-bottom 이 내용을 위로 민다 — R5 규칙). DoD 의 '쌓여도 하단 버튼을 영구히 가리지 않는다' 는 렌더를 `toasts.slice(-3)` 로 묶어 **스택 높이 자체를 153px 로 상한**해 만족시켰다 — **스토어는 미변경**이라 넘친 토스트도 기존 3초 타이머로 정상 제거된다. 의존 확인: daisyUI 5.5.18 은 `sm:toast-*` 를 **미리 생성해 배포**하므로(`components/toast.css` 의 `@media (width>=640px)`) Tailwind 스캔 누락 위험이 없고, 빌드 CSS 에서 base(`.toast-center` @74082) → `@media (width>=40rem){@layer daisyui.l1.l2{.sm\:toast-end …}}`(@113241) 순서로 방출돼 같은 레이어·특이도에서 순서로 이긴다(`.pb-[env(...)]` @97821 ↔ `.sm\:pb-0` @113793 도 동일). 검증: lint·build 통과 · **390×731 iframe 하니스**(dev :5175, `import("/src/stores/toastStore.ts")` 로 Vite 모듈 그래프에서 같은 스토어 인스턴스를 잡아 직접 띄웠다 — 코드 미변경) — ①**폭 6종 스윕**: 320·390·639 는 `bottom:16px`·`translate:-50%`·중심이 정확히 `innerWidth/2`·**헤더 겹침 0**, 640·800·1280 은 `top:16px`·`translate:0`·우측 여백 **16px**(= R9 이전과 동일) ②390px 에서 `.navbar` 버튼·링크 전수 `elementFromPoint(중심)` 자기 소유 ③success/error/info 3종이 각 `alert-*` 로 렌더되고 **최신이 가장 아래**(y 565·618·672), **5개를 밀어도 렌더 3개**(스택 `y 562..715`) ④`plannerStore.loadDetail` 로 코스를 주입해 **POI 드로어를 연 채** 3개를 쌓아도 액션 바 '닫기'·'Day 1에서 제거' **두 버튼 모두 중심 소유 유지**(겹치는 것은 70×16px 띠뿐, 3초 뒤 소멸) ⑤콘솔 에러 0 · 390px 스크린샷 육안 확인. ⏳ **범위 밖으로 남긴 것**: 1280px 데스크톱에서는 토스트 스택(`x 1222..1264 · y 16..169`)이 **테마 토글 중심 `(1222,33)` 을 여전히 덮는다** — 토스트가 없을 땐 `svg`, 있을 땐 토스트가 잡힌다. **R9 이전과 같은 상태**이고 사양이 '640px 이상은 종전대로 우상단'을 못 박아 손대지 않았다(고치려면 `sm:top-16` 류로 데스크톱 자리를 내리는 별도 판단). ⏳ **미검증**: `env(safe-area-inset-bottom)` 은 데스크톱 Chrome 에서 항상 0 이라 노치 기기의 실제 이동량은 확인 못 했다(R5 와 같은 한계).
- 2026-09-06 · **🏝️ 섬 M 전량 완료(M1·M2·M3·M4 `☑`)** · 백엔드가 accessToken 클레임에 `userId`(+`role`) 를 싣기로 확정해 **추적표 #4 블로커가 해소**됐다. **획득 경로**: `utils/jwt.ts` 신설(base64url 패딩 복원 + `TextDecoder` 로 UTF-8 — 한글 클레임이 섞여도 안전, 서명 검증이 아니라 **디코딩일 뿐**임을 주석에 못 박음) → `authStore.setAuth(token)` 이 인자가 없으면 토큰에서 스스로 꺼낸다(우선순위 **인자 > 클레임 > 기존값**, 토큰 없으면 null). 덕분에 로그인·재발급·카카오 **세 경로 모두 호출부 수정 0**. **M1** `api/user.ts` 에 GBC006~009 4종(`UserInfo` 는 백엔드 `UserInfoResponseDto` 1:1 · `gender` 는 Byte 코드고 라벨 매핑이 계약에 없어 미노출) + `useUser`(userId 없으면 요청 자체를 보내지 않고 재로그인 안내) + `MyPage` + `routes/userRouter.tsx`(`/mypage`, `RequireAuth` 안) + 헤더 계정 메뉴 진입(daisyUI 드롭다운이 포커스로 열려 있어 `closeMenus` 로 blur — 기존 게스트 링크도 같은 결함이라 함께 적용). **M2** `NicknameForm` — 미변경이면 버튼 비활성, 성공 후 `reload()` 로 서버를 진실로 삼고 `key={nickname}` 리셋(effect 로 state 를 맞추면 `react-hooks/set-state-in-effect` 위반). **M3** `PasswordForm` — 성공 시 로그아웃 후 로그인 화면으로(서버가 기존 토큰을 무효화하지 않아 **FE 정책**). **M4** `DangerZone` + `ConfirmDialog(danger)` → `authStore.clear()`. **실측으로 뒤집은 설계 하나**: 탈퇴 후 `navigate('/')` 로 홈에 보내려 했는데 **가드가 이긴다** — `clear()` 에 `RequireAuth` 가 먼저 반응해 `/auth/login` 으로 튕겼고, 데이터 라우터의 이동이 비동기라 `await navigate()` 로도 못 이겼다(`await` 가 즉시 resolve). 경쟁을 없애는 쪽으로 바꿔 **이동 코드를 지우고 가드에 맡겼다**(훅 주석에 근거 기록). 그 과정에서 **선재 결함**을 찾아 함께 고쳤다 — `/auth/*` 는 `Layout` 밖이라 `Toaster` 가 마운트되지 않아 **세션 만료 안내(`client.ts` flash)·회원가입 완료 toast 가 원래 한 번도 보이지 않았다** → `AuthScreen` 에 `Toaster` 추가. 검증: lint·build 통과 · `jwt.ts` esbuild→node **8케이스**(정상·문자열 id·패딩 경계·한글 클레임·클레임 없음·세그먼트 부족·깨진 base64·null) · 브라우저(백엔드가 없어 `App.tsx` 에 **임시** 목 부팅 + `apiClient.defaults.adapter` 대역 후 **원복**) — 마이페이지 4상태 렌더, 닉네임 미변경 비활성/1자 검증/저장 토스트, 비밀번호 불일치·8자 미만 검증, 변경 성공 → `/auth/login` + '새 비밀번호로 다시 로그인' 토스트 실제 표시, 탈퇴 → 로그인 화면 + 완료 토스트 + `localStorage` 정리(`userId`·`hasSession` 둘 다 null), 헤더 '마이페이지' 클릭 시 이동·메뉴 닫힘 · **388/318px 팝업 창에서 가로 넘침 0**, 액션 버튼 44px·입력 48px(daisyUI 기본 40px 라 `h-11` 로 올림). ⏳ **미검증(사용자 대행 필요)**: 실 백엔드 왕복 — 클레임이 아직 배포 전이고 이 환경엔 Java 가 없다. 배포되면 로그인 후 `/mypage` 진입만으로 GBC006~009 4종이 그대로 동작해야 한다. ⚠️ **백엔드에 남은 숙제**: `UserServiceImpl` 이 경로변수 `userId` 만 믿고 **소유자 검증을 하지 않는다**(`getUser`·`updateNickname`·`deleteUser` 가 `Authentication` 을 보지 않음) → 로그인한 아무나 남의 정보 조회·닉네임 변경·탈퇴가 가능하다. 토큰 email 과 대상 userId 일치 검증 필요.
- 2026-09-06 · **M1 화면 축소** · 사용자 요청으로 마이페이지에서 **프로필 카드(이메일·주소·생년월일·여행 성향 표시)를 제거**하고 닉네임 변경·비밀번호 변경·회원 탈퇴 **세 가지만** 남겼다. `components/user/ProfileSummary.tsx` 삭제. GBC006 조회 자체는 유지 — 닉네임 입력의 현재값과 탈퇴 확인 문구의 계정 이름이 서버 값이어야 하기 때문(파일 상단 주석에 근거 기록). 검증: lint·build 통과 · 브라우저 실측(임시 목 후 원복) — 섹션 `['닉네임','비밀번호 변경','회원 탈퇴']` 3개만, 닉네임 프리필 유지, 탈퇴 확인 문구에 닉네임 반영, 이메일 등 프로필 값 **화면 노출 0**, 318px 가로 넘침 0 · 버튼 44px 유지.
- 2026-09-06 · **카카오 계정 비밀번호 변경 선차단 — 넣었다가 되돌림(`defb72a` revert)** · 사용자 판단. 서버가 이미 `password == null` 로 정확히 거절하고 그 메시지가 toast 로 그대로 노출되는 반면, FE 가 쓸 수 있는 기준은 **로그인 수단**뿐이라 부정확하다 — 같은 이메일의 로컬 계정에 카카오를 연결한 계정은 비밀번호가 있는데도 변경 UI 를 잃는다. `authStore.provider`·`SocialPasswordNotice` 를 전부 걷어내고 원래대로 모든 계정에 폼을 띄운다(카카오 계정은 제출 시 서버 메시지로 안내). 판단 근거는 추적표 #10 에 기록 — 백엔드가 `hasPassword` 를 내려 주면 다시 열 수 있다. 검증: lint·build 통과.
