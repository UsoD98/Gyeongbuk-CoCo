# 프론트엔드 개발 진행상황 (Task 보드)

> **이 파일이 진행상황의 정본이다.** 새 세션을 시작해도 이 파일을 열면 어디까지 됐는지 알 수 있다.
> 상세 사양: [`FE_개발순서.md`](./FE_개발순서.md) · 현재 상태: [`FE_API_현황.md`](./FE_API_현황.md) · 레시피: [`FE_API_연동가이드.md`](./FE_API_연동가이드.md)
> 완료 이력(완료 Task 상세 · 과거 진행 로그 · 헤더 업데이트 이력): [`FE_개발_진행상황_아카이브.md`](./FE_개발_진행상황_아카이브.md) — **다음 Task 판단에는 읽지 않아도 된다.**

> 최종 업데이트: 2026-08-29 (**🧩 그룹 R · 모바일 UI/UX 개선 11개 Task 신규 등록 + 보드 최적화**. 사용자 요청으로 모바일 관점 UI/UX 를 전수 점검해 **실제 조작이 막히거나 상태가 유실되는 결함 3건(🔴 심각)** 을 포함한 15개 지적을 R1~R11 로 분해했다 — R1 코스 카드 `touch-none` 으로 모바일 코스 탭 스크롤 불가, R2 데스크톱 트리가 모바일에서도 동시 마운트(KakaoMap 2개·POI 중복 조회, `usePoiList` 의 in-flight dedup 이 증상만 막고 있음), R3 모바일 탭 전환마다 `ResultsPanel` 언마운트로 뷰모드·카테고리·더보기·지도 줌 전멸. 이하 R4 320px 가로 스크롤(`min-w-90`)·R5 오버레이 스크롤 체이닝+안전영역·R6 지도가 페이지 스크롤 삼킴·R7 헤더 모바일 메뉴 z-index·R8 터치 타깃 44px 미만(`.btn-circle{width:24px}` 전역 override 포함)·R9 토스트 위치·R10 홈 검색바 모바일 시각 단서·R11 폰트 700·hover 고착·메타 태그. 동시에 **보드를 151KB → 슬림화**했다: 완료 Task 상세·2026-08-22 이전 진행 로그·헤더 "이전 업데이트" 28줄을 [`FE_개발_진행상황_아카이브.md`](./FE_개발_진행상황_아카이브.md) 로 원문 그대로 이관하고, 보드에는 **활성 Task(`☐`·`◐`·`⏸`) 상세 + 완료 Task 한 줄 요약표** 만 남겼다. `next-task` 스킬도 그룹 R 우선순위와 아카이브 분리 규약을 반영해 갱신했다)

> 이전 업데이트: 2026-08-29 (**F8 ㉠ 완료 `◐` — 지도 크롬이 마커를 가리는 문제 해소**. 승인 범위(㉠ 컨트롤 겹침)만 구현했다. `spreadOverlaps` 에 **금지 구역**(`ForbiddenBox[]`)을 더하고, `anchorKeepOut` 이 컨트롤 사각형을 **클릭 대상(배지·토글) 중심 기준**으로 부풀려 앵커 금지 영역으로 바꾼다. `KakaoMap` 은 `idle` 마다 `getBounds()` 한 번으로 배율과 금지 구역을 **함께** 읽고(따로 읽으면 이동 중 두 값이 어긋난다) `chromeRects` 로 **우리 컨트롤 + SDK 가 컨테이너에 직접 붙이는 축척·로고**를 함께 잡는다 — 검증 중 **축척(좌하단 135×19)도 마커를 가리는 것을 실측으로 새로 발견**했고 같은 경로로 해소했다. 검증: lint·build 통과 · node **14케이스** 통과 · 금지 구역 **전수 스윕 1218자리 전부 해소** · 앱 줌 흐름 82상태에서 **컨트롤 가려짐 14→0**, **그 외 겹침 108→108**(㉡ 을 악화시키지 않음). 라이브(실 백엔드 경주, 코스 #77·#78·#79·#84): 축척에 가려졌던 `경주 재매정` 이 **43px 비켜나 도달 가능**으로 바뀌는 것을 확인. ⏳ **남은 것**: ㉡(밀집 코스 마커) 미착수 · 합성 드래그로 만든 상태에서 컨트롤 겹침 1건이 관찰돼 **실제 손 제스처로 재확인 필요**.)
> 그 이전 회차 전량: [`아카이브 §3`](./FE_개발_진행상황_아카이브.md)

## 업데이트 방법

- 작업을 시작하면 `☐` → `◐`(진행중), 끝나면 `☑`(완료)로 바꾼다.
- 완료 시 각 Task의 **DoD**를 만족했는지 확인하고, 하단 **진행 로그**에 한 줄 남긴다.
- 상태 배지: `☐` 대기 · `◐` 진행중 · `☑` 완료 · `⏸` 백엔드 대기(스펙 `보류`/`개발중`)
- 진행률 표는 완료 개수에 맞춰 갱신한다.
- **보드를 가볍게 유지한다**: Task 가 `☑` 가 되면 착수 당시의 구현·검증 서술은 [`아카이브 §1`](./FE_개발_진행상황_아카이브.md)로 옮기고, 보드의 "완료 Task" 표에는 **한 줄 요약**만 남긴다. 진행 로그가 20줄을 넘으면 오래된 회차를 [`아카이브 §2`](./FE_개발_진행상황_아카이브.md)로 옮긴다. 아카이브는 **추가만** 한다(내용 삭제·요약 금지).

---

## 진행률 요약

| 그룹 | 완료 / 전체 | 비고 |
|------|:---:|------|
| Step 0 · 기반 | 4 / 4 | ☑ 완료 — 모든 작업의 선행 |
| 메인 스파인 (코스) | 8 / 8 | ☑ S1~S8 완료 (S8 라이브 E2E까지 확인) |
| 🏝️ 섬 M · 마이페이지 | 0 / 4 | ⏸ userId 계약 대기(백엔드 미제공) — FE 준비는 완료 |
| 🏝️ 섬 P · POI | 4 / 4 | ☑ 완료 (P0~P3) |
| 🧩 그룹 F · 피드백 반영 | 5 / 9 | **F3·F4·F5·F7·F9 ☑.** F1·F2·F6 `◐` — 각 DoD 의 마지막 조건만 대기(F1·F6 = 로그인 왕복, F2 = 백엔드 미지원). **F8 `◐`** — ㉠(지도 크롬 겹침) 완료, ㉡(밀집 코스 마커) 미착수 |
| 📱 그룹 R · 모바일 UI/UX | 0 / 11 | **☐ 전량 대기 — 지금 착수 가능한 유일한 그룹.** R1~R3 은 🔴 심각(조작 불가·상태 유실) |
| 부록 A · 정리(선택) | 4 / 4 | ☑ 완료 (A1·A2·A3·A4) |
| **합계** | **25 / 44** | |

> 🚧 **지금 진행을 막고 있는 것**: 없다 — **그룹 R(11개)이 전부 `☐` 이고 백엔드·로그인 없이 착수 가능**하다. 그 밖의 남은 8개는 **`⏸` 4개(섬 M — 백엔드 `userId` 미제공)** 와 **`◐` 4개**(F1·F6 = 로그인 왕복 대행, F2 = 백엔드 미지원, F8 = ㉡ 미착수)다.
> → 백엔드에 넘길 요청서: **[`BE_계약_요청서.md`](./BE_계약_요청서.md)** (R1 `userId` · ~~R2 코스 장소 좌표~~ 수용됨 · ~~R3 찜 상태 조회~~ **수용됨 — 백엔드 0.6.4/0.6.7 이 `liked`·`totalLiked`·`stars` 를 실어 준다, F9 에서 반영** · 부록 B `transport` 영속). 회신이 오면 [`FE_계약_추적표.md`](./FE_계약_추적표.md)를 갱신하고 해당 Task 를 착수한다.
> → ⚙️ **이 개발 환경에는 Java 가 없다**(`java`·`JAVA_HOME` 부재) → 에이전트가 실 백엔드(`../back`)를 직접 띄울 수 없다. 다만 **사용자가 로컬 백엔드를 띄워 주면 라이브 검증이 된다**(2026-08-29 F7 검증이 그렇게 진행됐다 — :8080 기동 후 dev 5173 에서 실데이터 왕복). 로그인이 필요 없는 흐름(코스 생성 GBC010·POI 목록 GBC017·지도)은 이 방식으로 전부 실측 가능하다.
> → 남은 `◐` 4개 중 **로그인이 필요한 항목**(F1 저장 왕복, F6 저장 후 재진입)은 조직 지침상 에이전트가 비밀번호를 입력할 수 없어 **사용자 대행**이다. **모바일 폭은 더 이상 블로커가 아니다** — 브라우저 창은 여전히 1440px 밑으로 리사이즈되지 않지만, **동일 출처 iframe 을 390×731 로 띄우면 미디어쿼리가 실제 모바일 폭에서 평가**되고 Vite dev 모듈 그래프로 스토어를 직접 주입할 수 있어 이번에 F5·F6 을 그 방식으로 실측했다(앞으로도 이 방식을 쓴다). 다만 **OS 수준의 실제 손가락 터치**(pinch-zoom·롱프레스)는 이 툴체인으로 발생시킬 수 없다.

---

# 활성 Task (착수 대상)

> `☐` 대기 · `◐` 진행중 · `⏸` 백엔드 대기. **완료(`☑`) Task 는 아래 "완료 Task" 표로 내려갔다.**

## 📱 그룹 R · 모바일 UI/UX 개선 (2026-08-29 점검, 전량 착수 가능)

> 사용자 요청("데스크탑/모바일 2가지 반응형 중 모바일 측면 개선점")으로 모바일 관점 전수 점검을 수행해 15개 지적을 11개 Task 로 분해했다. 근거·수용 범위는 [`FE_개발순서.md`](./FE_개발순서.md) §그룹 R 참조.
> **백엔드·로그인이 전혀 필요 없다** — 전부 FE 단독으로 착수·검증 가능하며, 검증은 390×731 동일 출처 iframe 하니스(F5·F7 에서 확립한 방법)로 한다.
> 권장 진행 순서: **R1 → R2 → R3 → R4 → R5 → R6 → R7 → R8 → R9 → R10 → R11** (심각도 순. R3 은 R2 가 도입하는 `useMediaQuery` 위에 얹히므로 R2 뒤에 둔다).

- [ ] `☐` **R1 · 코스 카드 `touch-none` 으로 모바일 코스 탭 스크롤 불가** 🔴 심각 ▶ — 의존: 없음. `CourseItem.tsx:87` 이 카드 **루트**에 `touch-none` 을 걸어 `touch-action:none` 이 카드 전체에 적용된다. 드래그 핸들은 사진 영역(`setNodeRef` 가 붙은 div)뿐인데도 카드 위 스와이프가 스크롤로 안 먹어, 모바일 코스 탭에서 카드 사이 `gap-2.5` 틈으로만 스크롤된다(항목 5~6개면 사실상 스크롤 불가). DoD: 390px 폭 코스 탭에서 **카드 위를 쓸어올려 리스트가 스크롤**되고, 사진 영역 드래그의 재정렬은 그대로 동작한다. → 순서 §그룹R R1
- [ ] `☐` **R2 · 데스크톱·모바일 트리 동시 마운트 제거** 🔴 심각 ▶ — 의존: 없음. `Planner.tsx:186`(`hidden … lg:flex`)와 `:210`(`flex … lg:hidden`)이 **둘 다 렌더**돼, 모바일에서 `display:none` 인 데스크톱 쪽 `ResultsPanel`·`MapView`·`CoursePanel`·`BudgetDashboard` 가 살아 있다 → KakaoMap 인스턴스 2개(하나는 크기 0 컨테이너)·POI 중복 조회. `usePoiList.ts:57` 의 in-flight dedup 주석이 이미 이 사실을 인정하고 **증상만** 막고 있다. DoD: `useMediaQuery`(`matchMedia('(min-width:1024px)')`) 로 한쪽만 마운트 → 모바일에서 KakaoMap 인스턴스 1개·`GET /poi` 1회. dedup 우회 로직의 존치 여부도 함께 판단해 기록. → 순서 §그룹R R2
- [ ] `☐` **R3 · 모바일 탭 전환 시 탐색 상태 전멸** 🔴 심각 ▶ — 의존: R2. `Planner.tsx:232-241` 이 `{tab === 'results' && <ResultsPanel mobile />}` 조건부 렌더라 탭을 떠나면 언마운트된다 → `viewMode`(→`'map'`)·`courseOnly`(→`true`)·`cat`(→`'all'`)·`limit`(→60)·스크롤 위치·지도 줌/팬이 전부 초기화. 리스트로 바꿔 '더 보기' 3번 눌러 180개를 본 뒤 코스 탭에 갔다 오면 다시 지도·60개·맨 위다. DoD: 결과↔코스↔예산을 왕복해도 뷰모드·카테고리·더보기 개수·스크롤·지도 뷰포트가 유지된다. → 순서 §그룹R R3
- [ ] `☐` **R4 · 320px 기기 가로 스크롤(`min-w-90`)** 🟠 중요 ▶ — 의존: 없음. `Layout.tsx:25` 의 `min-w-90`(=360px)이 `px-4` 와 함께 걸려 뷰포트 360px 미만에서 페이지 전체가 가로로 밀린다(iPhone SE 1세대·갤럭시 폴드 접힘 320px). DoD: 320px 폭에서 `document.documentElement.scrollWidth <= clientWidth`(가로 스크롤 0) + 주요 화면 4곳(홈·플래너 3탭·컬렉션·공유)의 레이아웃 붕괴 없음. → 순서 §그룹R R4
- [ ] `☐` **R5 · 오버레이 스크롤 체이닝 + 하단 안전영역** 🟠 중요 ▶ — 의존: 없음. `PoiDrawer`·`ConfirmDialog`·`LoginGateModal` 모두 body 스크롤 락과 `overscroll-behavior:contain` 이 없어 바텀시트 내용을 끝까지 스크롤하면 뒤 페이지가 따라 움직인다. 프로젝트 전체에 `env(safe-area-inset-*)` 사용이 **0건**이라 드로어 하단 액션 바(`닫기`/`Day N에 추가`)가 iOS 홈 인디케이터에 물린다. 상단 grabber·스와이프 다운 닫기도 없어 사진 위 X 버튼만이 유일한 닫기 수단. DoD: 오버레이가 열린 동안 배경이 스크롤되지 않고, 하단 액션 바가 홈 인디케이터에 가리지 않으며(`viewport-fit=cover` + `pb-[max(1rem,env(safe-area-inset-bottom))]`), 바텀시트에 grabber 가 보인다. → 순서 §그룹R R5
- [ ] `☐` **R6 · 지도가 페이지 스크롤을 삼킴** 🟠 중요 ▶ — 의존: R3. 모바일 결과 탭 기본값이 지도(`ResultsPanel.tsx:79 viewMode='map'`)이고 `h-[70vh]` 카드가 화면 대부분을 차지해, 지도 위 스크롤이 전부 pan 으로 먹혀 아래 예산 탭·푸터로 내려갈 수 없다. DoD: 390px 폭에서 지도 영역 위 세로 스와이프로 **페이지를 끝까지 내릴 수 있고**, 의도적으로 지도를 조작하는 경로(오버레이 탭 또는 명시 토글)도 남는다. → 순서 §그룹R R6
- [ ] `☐` **R7 · 헤더 모바일 메뉴 z-index 누락** 🟠 중요 ▶ — 의존: 없음. `HeaderLayout.tsx:148` 의 모바일 드롭다운이 `absolute top-full` 인데 z-index 가 없고, `index.css:24` 의 `.navbar{position:relative}` 에도 z 가 없다 → `main` 안의 positioned 요소(홈 검색바 드롭다운 `z-10`, POI 카드 `relative` 배지 등)가 메뉴 위로 올라온다. DoD: 홈·플래너·컬렉션 각 화면에서 버거 메뉴를 열었을 때 메뉴가 항상 최상단에 온전히 보인다. → 순서 §그룹R R7
- [ ] `☐` **R8 · 터치 타깃 44px 미만 정비** 🟠 중요 ▶ — 의존: 없음. `index.css:21` 의 `.btn-circle { width: 24px; }` **전역 override** 가 daisyUI 원형 버튼을 24px 로 줄이고 있고, 여기에 찜 하트(`LikeButton`)가 걸려 24×32 로 찌그러진다. 그 밖에 홈 인원 `−`/`+`(≈24px)·컬렉션 카드 삭제 버튼(32px, 탭 가능한 카드 위에 겹쳐 오탭 시 삭제 다이얼로그)·지도 마커 토글(22px, `markerStyle.ts:39`)·코스 카드 시각/금액 인라인 편집 트리거(`text-xs`)가 기준 미달. DoD: 위 6곳의 실측 히트박스가 44×44px 이상(투명 확장 허용)이고 `.btn-circle` 전역 override 는 제거되거나 범위가 좁혀진다. → 순서 §그룹R R8
- [ ] `☐` **R9 · 토스트 위치(모바일에서 헤더를 가림)** 🟡 개선 ▶ — 의존: 없음. `Toaster.tsx:16` 이 `toast-top toast-end` 고정이라 모바일에서 계정 메뉴·테마 토글 위를 덮고 엄지에서 가장 먼 자리다. DoD: 모바일은 하단 중앙, `sm:` 이상은 현행 우상단 유지. → 순서 §그룹R R9
- [ ] `☐` **R10 · 홈 검색바 모바일 시각 단서 + DatePicker** 🟡 개선 ▶ — 의존: 없음. `Index.tsx` 의 목적지·일정·이동수단·테마 트리거가 `border-base-100 bg-base-100`(배경과 동일)에 `text-xs` 이고, 칸을 나눠 주던 구분선은 `hidden lg:block` 이라 **모바일에서만 사라진다** → 5개 필드가 눌러야 할 곳인지 알 수 없는 텍스트 목록으로 보인다. `react-datepicker` 기본 팝오버도 좁은 화면에서 셀이 작고 잘릴 수 있다. DoD: 390px 폭에서 각 칸이 입력 컨트롤로 식별되고(테두리·최소 높이 48px) 달력이 화면 안에 온전히 들어온다. → 순서 §그룹R R10
- [ ] `☐` **R11 · 폰트 700 · hover 고착 · 메타 태그** 🟡 개선 ▶ — 의존: 없음. ①`index.css` 가 Pretendard **400 하나만** 로드하는데 UI 전반이 `font-bold`/`font-extrabold` → 합성 볼드로 저해상도 모바일에서 뭉갠다. ②POI·컬렉션 카드의 `hover:-translate-y-0.5 hover:shadow-md` 가 터치 후 고착되고 `active:` 피드백은 없다. ③`index.html` 에 `theme-color`(헤더 teal)·`description`·공유 링크(`/share/:id`)용 OG 태그가 없다 — 카카오 공유가 핵심 기능이라 특히 아쉽다. DoD: 700 웨이트가 실제 로드되고, 터치 후 hover 잔상이 없으며 눌림 피드백이 있고, 모바일 브라우저 크롬 색이 헤더와 맞는다. → 순서 §그룹R R11

## 🧩 그룹 F · 사용자 피드백 반영 — 진행 중(`◐`) 4건

> 완료분(F3·F4·F5·F7·F9)은 아래 "완료 Task" 표 · 상세는 [`아카이브 §1`](./FE_개발_진행상황_아카이브.md).
> 아래 4건은 **FE 구현이 끝나고 마지막 조건만 남은** 상태다. 착수 당시의 구현·검증 서술은 [`아카이브 §4`](./FE_개발_진행상황_아카이브.md)에 있다.

- [ ] `◐` **F1 · 코스 시각·체류시간 편집** — FE 완료. 남은 것: **브라우저 저장 왕복 재확인(로그인 필요 → 사용자 대행)**. 서버가 `durationMinutes`·`cost` 를 저장하는 것은 백엔드 소스 실측으로 확인됨(추적표 #5), API 레벨 PATCH→재조회 왕복도 확인됨. → 순서 §그룹F F1 · 상세 [아카이브 §4](./FE_개발_진행상황_아카이브.md)
- [ ] `◐` **F2 · 이동수단 상태화 + 예산 교통비 편집** — FE 완료 / **영속은 백엔드 미지원(`⏸`)**. `PATCH /tour-course/{id}` 바디가 `{schedule}` 뿐이라 `transport`·교통비를 보낼 자리가 없다. 남은 것: 백엔드가 필드를 열어 주면 영속 연결(요청서 부록 B). → 순서 §그룹F F2 · 상세 [아카이브 §4](./FE_개발_진행상황_아카이브.md)
- [ ] `◐` **F6 · 지도에서 코스 편집 — (a)안(마커 토글) 구현 완료** — 드래그 3안은 기각(근거는 아카이브). 남은 것: **저장 후 재진입 유지(DoD 의 영속 절반)가 로그인 필요 → 사용자 대행**. → 순서 §그룹F F6 · 상세 [아카이브 §4](./FE_개발_진행상황_아카이브.md)
- [ ] `◐` **F8 · 남은 도달 불가 2건** — **㉠(지도 컨트롤 겹침) 완료 / ㉡(밀집 코스 마커) 미착수**. ㉡ 은 "위치 왜곡 < 겹침" 설계 결정을 뒤집을지 사용자 판단이 필요하다. 그 외: 합성 드래그 상태에서 관찰된 컨트롤 겹침 1건을 **실제 손 제스처로 재확인** 필요. **로그인·백엔드 없이 착수 가능**. → 순서 §그룹F F8 · 상세 [아카이브 §4](./FE_개발_진행상황_아카이브.md)

## 🏝️ 섬 M · 마이페이지 (S0-C 후 병렬 가능 · 전량 `⏸`)

> ⛔ **착수 보류(백엔드 대기)**: 백엔드가 로그인/재발급 응답·JWT·`/user/me` 어디에도 `userId`를 제공하지 않아, `/user/{userId}` 계열(GBC006~009)을 부를 자기 id를 FE가 얻을 수 없다. 백엔드에 userId 제공(응답 포함 or `/user/me`) 확정 후 착수. FE 측 준비(S0-C)는 완료. 상세: [`FE_계약_추적표.md`](./FE_계약_추적표.md) #4.
- [ ] `⏸` **M1 · GBC006 회원정보 조회** — 의존: S0-C. `userRouter`+`MyPage`+`getUser`+헤더 진입. DoD: 마이페이지에서 내 정보 표시. → 순서 §섬M M1
- [ ] `⏸` **M2 · GBC007 닉네임 수정** — 의존: M1. `updateNickname`. DoD: 편집 후 유지.
- [ ] `⏸` **M3 · GBC008 비밀번호 변경** — 의존: M1. `updatePassword`(현재/신규). DoD: 변경 후 새 비번 재로그인.
- [ ] `⏸` **M4 · GBC009 회원 탈퇴** — 의존: M1. `deleteUser`→`authStore.clear()`. DoD: 탈퇴 후 로그아웃·재로그인 불가.

---

# 완료 Task (`☑` 25건)

> 한 줄 요약만 둔다. **착수 당시의 구현·검증 상세는 [`아카이브 §1`](./FE_개발_진행상황_아카이브.md)** 에 원문 그대로 있다.

| Task | 한 줄 요약 |
|------|------|
| `☑` **S0-A** 계약 확인 + 추적표 | 계약 4건 스펙 가정으로 진행(비블로킹). `FE_계약_추적표.md` 신설 |
| `☑` **S0-B** API 응답 타입 정의 | `api/tourCourse.ts`(스펙 1:1)·`api/poi.ts`(잠정 + `cat↔contentTypeId` 매핑) |
| `☑` **S0-C** authStore userId 보관 | `authStore.userId`+`setAuth`, 로그인/카카오/재발급 연동 + localStorage 병행 |
| `☑` **S0-D** 공통 상태 컴포넌트 + 훅 | `common/EmptyState·ErrorState·Skeleton` + `hooks/useAsync` |
| `☑` **S1** GBC010 AI 코스 생성 | 홈 검색 → `createCourse` → `loadFromApi` → `/planner`. transport·sigunguCodes·한국어 theme |
| `☑` **S2** GBC016 저장(소유권 이전) | 게스트 courseId sessionStorage → 로그인 게이트 → 복귀 후 자동 `assignCourse` |
| `☑` **S3** GBC011 내 코스 목록 | `getMyCourses` + `useCourseList` + `Collection.tsx` 카드 그리드(4상태) |
| `☑` **S4** GBC012 코스 상세 | `getCourse` + `useCourseDetail` + `plannerStore.loadDetail` + `/planner/:courseId` |
| `☑` **S5** GBC013 코스 삭제 | `deleteCourse` + 재사용 `ConfirmDialog` + `useCourseDelete` |
| `☑` **S6** GBC015 코스 제목 수정 | `updateCourseTitle` + `useCourseTitle`(낙관적·롤백) + `EditableCourseTitle` |
| `☑` **S7** GBC014 공유 + 공개뷰 | `getPublicCourse`(순수 axios로 401 인터셉터 우회) + 가드 밖 `/share/:id` + 카카오 공유/클립보드 폴백 |
| `☑` **S8** GBC020 코스 수정 영속화 | `updateCourse` + `buildSchedulePayload`(schedule 전체 교체) + `useCourseUpdate`. 라이브 E2E 통과 |
| `☑` **P0** 목→API 교체 준비 | `usePoiList`(P2 교체점)·`usePoi`(P3 교체점) 신설, 소비처를 훅 경유로 |
| `☑` **P1** GBC019 POI 좋아요 토글 | `togglePoiLike` + `poiLikeStore` + `loginGateStore`(게이트 전역화) + `usePoiLike` + `LikeButton` |
| `☑` **P2** GBC017 큐레이션 POI 목록 | `getPois` 실측 타입 1:1 + `usePoiList` 목→API(시군구 병렬 조회·in-flight dedup) |
| `☑` **P3** GBC018 POI 상세 통합 | `getPoi` + `usePoi` 실조회(캐시·dedup·stale 차단) + `utils/poiDetail`(상세는 빈 칸만 채움) |
| `☑` **F3** 수단별 코스 간 이동시간 | `utils/travelTime.ts`(haversine × 우회 1.3 × 수단별 실효속도) + `TravelConnector` |
| `☑` **F4** 좋아요 토글 정합성 조사 | 원인 확정(백엔드는 진짜 토글, 증상은 기수정된 저장 유실 버그) + FE 결함 2건 수정 |
| `☑` **F5** 지도 코스 전용 표시 | 지도 모드를 지역 미선택 early return **앞으로** + `내 코스만 보기` 토글. 컬렉션 진입 지도 미표시 해소 |
| `☑` **F7** 지도 마커 겹침 도달 불가 | `mapCluster.ts` 신설(`clusterMarkers`·`spreadOverlaps`). 실 백엔드 경주 316곳 라이브 실측 통과 |
| `☑` **F9** `liked`·`totalLiked`·`stars` 연동 | 타입 3종 실측 1:1 + `poiLikeStore` 2맵·비덮어쓰기 hydrate + 세션 키 dedup. 로그인 E2E 통과 |
| `☑` **A1** `/home` 더미 라우트 제거 | `router.tsx` 라우트 블록 + `pages/Home.tsx` 삭제 |
| `☑` **A2** About 콘텐츠 작성 | 서비스 소개 페이지(히어로·핵심기능·이용방법·CTA) + 푸터 링크·저작권 정정 |
| `☑` **A3** 문서 드리프트 최신화 | `CLAUDE.md`·`PRD_FRONT.md`·`FEATURES_FRONT.md` 를 실코드에 맞춤 + 보드 포인터 |
| `☑` **A4** RootLayout 정리 판단 | CLAUDE.md 규칙상 존치 결정. 의도된 플레이스홀더임을 주석으로 문서화 |

---

## 진행 로그

> 완료할 때마다 `- YYYY-MM-DD · <TaskID> 완료 · <한 줄 메모>` 형식으로 추가.
> **2026-08-22 이전 전량은 [`아카이브 §2`](./FE_개발_진행상황_아카이브.md) 로 옮겼다.** 20줄을 넘기면 오래된 회차부터 계속 옮긴다.

- 2026-08-29 · **F7 완료(`☑`)** · 사용자가 로컬 백엔드를 띄워 줘 **실 백엔드 경주 316곳**으로 마지막 조건(그룹을 펼친 뒤 도달 가능성)을 실측했다. 390px iframe 하니스에서 코스 3회 생성(#70·#74·#75) → 초기 뷰부터 그룹이 없어질 때까지 매 단계 전수 `elementFromPoint` 판정: **14/14 → 19/19 → 20/20 → 10/10**, 재현 **16/16 → 21/21 → 22/22 → 13/13**, 가려짐 0. 지난 세션의 "펼친 뒤 마커가 빈다"는 관찰은 **하니스 탓으로 확정**(DoD 흐름 밖인 `축소` 연타 상태에서만 재현). 보강으로 실제 DOM 기하·z 규칙을 옮긴 **도달 가능성 시뮬레이터**(node, 392상태)를 만들어 현실형 데이터셋 전 단계 통과를 확인했다.
- 2026-08-29 · **F8 신설(`☐`)** · F7 검증에서 발견한 잔여 도달 불가 2건 — ㉠ 지도 컨트롤(`z-[3]` 32×108px)이 오른쪽 아래 마커를 덮어 누르면 확대가 실행됨(데스크톱 1280px 실측 재현) ㉡ 한 Day 코스가 5곳 이상 반경 300m 안에 몰리면 `spreadOverlaps` 가 빈 자리를 못 찾아 겹침이 남음(시뮬레이터 경계: 5~6곳=그룹 배지 가려짐, 7곳+=코스 토글 가려짐 · 라이브 3회에서는 미재현). 로그인·백엔드 없이 착수 가능.
- 2026-08-29 · **F8 ㉠ 완료(`◐`)** · 지도 크롬이 마커를 가려 담기 대신 확대가 눌리던 문제를 해소했다. `spreadOverlaps` 에 금지 구역을 더하고(`anchorKeepOut` 으로 클릭 대상 중심 기준 부풀리기 · `canPlace` 로 겹침 회피와 한 경로), `KakaoMap` 은 `idle` 마다 배율과 금지 구역을 한 번의 `getBounds()` 로 함께 읽는다. 검증 중 **카카오 SDK 축척·로고(좌하단 135×19)도 마커를 가리는 것을 새로 발견**해 같은 경로로 해소(`chromeRects` = 컨트롤 + 컨테이너 직속 자식 중 전체 크기가 아닌 것). lint·build 통과 · node 14케이스 · 금지 구역 1218자리 전수 해소 · 82상태에서 컨트롤 가려짐 14→0 / 그 외 겹침 108→108(㉡ 불변) · 라이브에서 `경주 재매정` 이 43px 비켜나 도달 가능으로 전환. ㉡(밀집 코스 마커)은 설계 결정을 뒤집는 선택이라 미착수로 남겼다.
- 2026-08-29 · **F9 신설·완료(`☑`)** · 사용자 요청으로 백엔드 신규 필드 3종을 연결했다. 확인 결과 셋 다 미반영이었고 그중 **토글 응답 필드 개명(0.6.5 `likes`→`totalLiked`)은 실결함**이었다 — `usePoiLike` 가 `res.likes`(존재하지 않는 필드)를 읽어 좋아요 수가 `undefined` 로 유실. `api/poi.ts`(타입 3종 실측 1:1) · `poiLikeStore`(`{liked,totalLiked}` + `hydrate` — 이미 아는 값은 덮지 않아 토글 결과가 낡은 응답에 되돌려지지 않는다) · `usePoiList`/`usePoi`(응답 hydrate + **세션 키**로 조회 dedup·상세 캐시 분리, `authSessionKey()` 를 `authStore` 로 승격) · `usePoiLike`(응답으로 확정, 재조회 없음) · `utils/poiDetail`(별점은 빈 칸만 채움) · `Stars`(0이면 렌더 안 함 → 실 POI 의 `★ 0` 제거) · `LikeButton showCount`(드로어에서만 총개수). lint·build 통과 + node 19케이스. ✅ 라이브(실 백엔드 경주 316곳): 비로그인 — 별점 전건 일치·`★ 0` 제거·게이트 시 `/like` 0건. **로그인 E2E(사용자 대행 로그인)** — 토글 200 `{liked,totalLiked}` 즉시 반영(카드·드로어 동시)·새로고침 후 하트 유지·드로어 재오픈 캐시 히트에도 토글 결과 보존·해제 200·세션 전환(로그아웃 제거 → 재로그인 복구)·콘솔 에러 0. 추적표 **#9 해결**.
- 2026-08-29 · **📱 그룹 R 신설(11개 `☐`) + 보드 최적화** · 사용자 요청으로 모바일 관점 UI/UX 전수 점검(레이아웃·터치·오버레이·마운트 구조 정독 + `touch-none`·`vh`·`safe-area`·브레이크포인트 grep). **🔴 심각 3건**: ①`CourseItem.tsx:87` 카드 루트 `touch-none` → 모바일 코스 탭 스크롤 불가 ②`Planner.tsx:186/210` 데스크톱·모바일 트리 동시 마운트(KakaoMap 2개·POI 중복 조회 — `usePoiList.ts:57` dedup 주석이 이미 인정) ③`Planner.tsx:232` 조건부 렌더로 탭 전환마다 `ResultsPanel` 언마운트 → 뷰모드·카테고리·더보기·스크롤·지도 줌 전멸. **🟠 중요 5건**: `Layout.tsx:25 min-w-90`(320px 가로 스크롤)·오버레이 스크롤 체이닝+`env(safe-area-*)` 0건·지도가 페이지 스크롤 삼킴·`HeaderLayout.tsx:148` z-index 누락·터치 타깃 6곳 44px 미만(`index.css:21 .btn-circle{width:24px}` 전역 override 가 `LikeButton` 을 찌그러뜨림). **🟡 개선 3건**: 토스트 위치·홈 검색바 모바일 시각 단서(구분선이 `hidden lg:block` 이라 모바일에서만 사라짐)+DatePicker·폰트 700 미로드/hover 고착/메타 태그. → R1~R11 로 분해해 보드와 `FE_개발순서.md` §그룹 R 에 등록(전부 백엔드·로그인 불필요). **동시에 보드 최적화**: 151KB → 26KB. 완료 Task 상세·2026-08-22 이전 진행 로그(56줄)·헤더 "이전 업데이트" 28줄·`◐` 4건의 구현 서술을 `FE_개발_진행상황_아카이브.md` 로 **원문 그대로** 이관하고, 보드엔 활성 Task 상세 + 완료 25건 한 줄 요약표만 남겼다. `.claude/skills/next-task/SKILL.md` 도 그룹 R 우선순위·아카이브 분리 규약을 반영.
