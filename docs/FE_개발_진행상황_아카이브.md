# 프론트엔드 진행상황 · 아카이브 (완료 이력)

> **정본 보드는 [`FE_개발_진행상황.md`](./FE_개발_진행상황.md) 다.** 이 파일은 거기서 덜어낸
> **완료 Task 의 상세 기록 · 과거 진행 로그 · 헤더 업데이트 이력** 을 원문 그대로 보관한다.
>
> ⚠️ **`next-task` 스킬은 이 파일을 읽지 않는다.** 다음 착수 Task 판단에 필요 없는 정보이며,
> 보드를 가볍게 유지하는 것이 이 분리의 목적이다. 과거 구현 근거·검증 결과를 되짚어야 할 때만 연다.
>
> 이관일: 2026-08-29

---

## 1. 완료 Task 상세

보드에는 한 줄 요약만 남기고, 착수 당시 기록한 구현·검증 상세는 여기에 보관한다.

### Step 0 · 기반

- [x] `☑` **S0-A · 계약 확인 시도 + 추적표** — 4건 모두 스펙 가정으로 진행(비블로킹). 추적표 신설: `docs/FE_계약_추적표.md`. → 순서 §Step0 0-A
- [x] `☑` **S0-B · API 응답 타입 정의** — `api/tourCourse.ts`(스펙 200 1:1)·`api/poi.ts`(보류라 잠정+`cat↔contentTypeId` 매핑). → 가이드 §1-B
- [x] `☑` **S0-C · authStore userId 보관** — `authStore.userId`+`setAuth`(setAccessToken 확장), `auth.ts`/`client.ts`/`Login`/`Kakao` 연동, localStorage 병행 보관. → 가이드 §1-C
- [x] `☑` **S0-D · 공통 상태 컴포넌트 + 훅 패턴** — `common/EmptyState·ErrorState·Skeleton` + `hooks/useAsync`(loading/error/data/reload). → 가이드 §1-D·§4

### 메인 스파인 · 코스 (S1~S8)

- [x] `☑` **S1 · GBC010 AI 코스 생성** — `Index.handleSearch`→`createCourse`→`plannerStore.loadFromApi`(placeholder POI 합성)→`/planner`. transport 선택 UI·단일 sigunguCode(`47`+값)·한국어 theme 라벨·게스트 courseId 보관·부팅 목업 제거. DoD 코드 충족(장소명은 POI 상세=P3 후). ⚠️ 실백엔드 E2E는 dev 서버 기동 시 확인, ResultsPanel 목 브라우즈는 P2 경계. → 순서 §Step1
- [x] `☑` **S2 · GBC016 저장(소유권 이전)** — 게스트 courseId `sessionStorage` 보관→저장 클릭 시 로그인 게이트→복귀 후 자동 `assignCourse`(`PATCH .../assign`). `onSave` 목업 제거, 중복 저장 가드(`saved`/`saving`), 저장 버튼 진행/완료 UI. `useCourseSave` 훅으로 캡슐화. DoD 충족. ⚠️ 실백엔드 E2E는 dev+백엔드 기동 시 확인. → 순서 §Step2
- [x] `☑` **S3 · GBC011 내 코스 목록** — `api/tourCourse.ts` `getMyCourses`(`GET /tour-course`) + `hooks/useCourseList`(useAsync 래핑) + `Collection.tsx` 전면 구현(카드 그리드·최초로딩 스켈레톤/빈/에러 4상태). 카드=제목·기간(N박M일)·인원·이동수단·테마·생성일. ⚠️ "지역"은 목록 응답 필드에 없어 생략. 카드→`/planner/:courseId` 링크는 전방 배선(라우트는 S4). DoD 충족. → 순서 §Step3
- [x] `☑` **S4 · GBC012 코스 상세** — `api/tourCourse.ts` `getCourse`(`GET /tour-course/{id}`→`CourseDetail`) + `hooks/useCourseDetail`(useAsync 래핑, 성공 시 `loadDetail` 주입) + `plannerStore.loadDetail`(상세→스토어 매핑) + `plannerRouter` `:courseId` 세그먼트. `Planner`가 `useParams`로 진입: 스토어에 해당 코스 없으면 `<Loading>`/`<ErrorState onRetry>`, 있으면 즉시 렌더. 상세 응답 `placeName`으로 실 장소명 표시(`synthesizePoi` 폴백 유지). DoD 충족(카드→상세·URL 재진입). ⚠️ 실백엔드 E2E는 dev+백엔드+로그인 시 확인. → 순서 §Step4
- [x] `☑` **S5 · GBC013 코스 삭제** — `api/tourCourse.ts` `deleteCourse`(`DELETE /tour-course/{id}`) + 재사용 `common/ConfirmDialog`(role=dialog·aria-modal·Escape·오버레이·danger/busy) + `hooks/useCourseDelete`(요청+busy+toast 캡슐화). `Collection` 카드에 삭제 버튼(Link 형제 오버레이, 중첩 회피) → 확인 → 삭제 → `reload()`(서버 진실 재조회). 이중 발사·busy 중 취소 차단, 실패 시 다이얼로그 유지+재시도. DoD 충족. ⚠️ 실백엔드 E2E는 dev+백엔드+로그인 시 확인. → 순서 §Step5
- [x] `☑` **S6 · GBC015 코스 제목 수정** — `api/tourCourse.ts` `updateCourseTitle`(`PATCH /tour-course/{id}/title`) + `plannerStore.setTitle`(낙관적/롤백용) + `hooks/useCourseTitle`(낙관적 업데이트+실패 시 롤백+toast) + `components/planner/EditableCourseTitle`(요약 헤더 인라인 편집: 클릭→input→Enter/blur 저장·Escape 취소·maxLength 255). 편집 노출 조건=`isAuthenticated && storeCourseId!=null`(게스트·미저장은 읽기전용). DoD 충족. ⚠️ 실백엔드 E2E는 dev+백엔드+로그인 시 확인. → 순서 §Step6
- [x] `☑` **S7 · GBC014 공유 + 공개뷰** — `api/tourCourse.ts` `getPublicCourse`(`GET /tour-course/{id}/view`, **순수 axios**로 호출해 401 인터셉터·강제 로그아웃 우회) + `routes/shareRouter`(가드 밖 `/share/:courseId`, `router.tsx`에 RequireAuth 형제로 spread) + `pages/Share/Share`(읽기전용 공개뷰: 요약 헤더+일정 카드, `useAsync`로 로딩/에러) + `hooks/useCourseShare`(카카오 `Share.sendDefault` 시도→키없음/도메인미등록/로드실패 시 클립보드 복사 폴백) + `utils/kakaoShare`(SDK 온디맨드 로드+init) + `utils/courseFormat`(TRANSPORT/PLACE_TYPE 라벨·날짜·기간 헬퍼, Collection과 공유). `Planner.onShare` toast 목업→`share()`, 공유는 로그인 불필요(수신자만 비로그인 열람)로 게이트 제거. DoD 충족. ✅ 5173 라이브: 가드 밖(로그인 리다이렉트 없음)·`/view` 순수 axios 직행(reissue 루프 안 탐)·부재 코스 ErrorState(백엔드 msg 노출)·콘솔 에러 없음. ✅ **해피패스도 검증 완료**(2026-08-15, 백엔드 AI 생성 복구 후): 공유 버튼 → 카카오 `Share.sendDefault`가 실제 피커(`sharer.kakao.com/picker/link`) 오픈(클립보드 폴백 미발동), `/share/37` 공개뷰가 비로그인으로 실데이터 렌더(요약 + Day1 6곳·Day2 5곳 실장소명·시각·타입). → 순서 §Step7
- [x] `☑` **S8 · GBC020 코스 수정 영속화** — 백엔드 상세 명세 수령(요청 바디 = `schedule` 전체 교체, 장소명 `contentName`)으로 ⏸ 해제. `api/tourCourse.ts` `updateCourse(courseId, schedule)`(`PATCH /tour-course/{id}`) + `utils/coursePayload.ts`(신설: UI 축약형 코스 → 서버 스키마 복원. 원본 `baseSchedule` 우선, 새로 담은 장소는 카탈로그 Poi, 방문 시각은 **그 날 원본 시각 슬롯을 순번대로 배분**해 재정렬 후에도 역전 없음, 예산 override(총액)→`cost`(1인) 환산) + `hooks/useCourseUpdate.ts`(명시적 저장 트리거·성공 시 `markPristine`·실패 시 dirty 유지 재시도) + `plannerStore`(`baseSchedule` 원본 보관·`dirty` 편집 플래그, 제자리 드롭·동일 금액 같은 no-op 은 dirty 미세움). UI: 저장 버튼 하나가 **미소유=소유권 이전(GBC016) / 소유=변경 저장(GBC020)** 겸용('저장'·'변경 저장'·'저장됨'), 게스트 편집분은 assign 성공 직후 자동 flush(`useCourseSave.onAssigned`). DoD 충족. ✅ **라이브 E2E 완료**(코스 39: 제거·재정렬·비용 편집 → `PATCH /tour-course/39` 200 → 재진입 시 구성·순서·시각·체류시간 유지, 콘솔 에러 0). ⚠️ **비용은 서버가 저장하지 않는다**(백엔드가 표시 전용 필드 `cost`·`contentName`·`thumbnailImg`·`operatingHours` 무시) → 예산 편집 영속이 필요하면 백엔드 요청 대상. 검증 중 발견한 빈 Day → 400(`@NotEmpty`)은 전송 전 차단 + Day 지목 toast 로 보강. → 순서 §Step8

### 🏝️ 섬 P · POI (P0~P3)

- [x] `☑` **P0 · 목→API 교체 준비** — `hooks/usePoiList`(P2 교체점)·`hooks/usePoi`(P3 교체점) 신설, `ResultsPanel`·`PoiDrawer`가 훅 경유로 POI 데이터 수신(현재 목), `api/poi.ts` `CONTENT_TYPE_BY_CAT`+역방향 `CAT_BY_CONTENT_TYPE` 매핑. DoD 충족. → 순서 §섬P P0
- [x] `☑` **P1 · GBC019 POI 좋아요 토글** — `togglePoiLike`(`POST /poi/{id}/like`) + `poiLikeStore`(찜 상태 공유) + `loginGateStore`(게이트 전역화) + `usePoiLike`(게이트+낙관적+롤백) + `LikeButton` 하트(POICard·PoiDrawer). DoD 코드 충족(즉시 토글·실패 롤백·비로그인 게이트). ✅ **비로그인 게이트 라이브 검증**(2026-08-15): 실 POI 카드의 하트 클릭 → "찜하려면 로그인" 모달(로그인하러 가기 CTA + "탐색과 코스 짜기는 로그인 없이도 가능해요"), 서버 호출 없음. ⚠️ **로그인 상태 실동작**(`POST /poi/{id}/like` 서버 반영·재조회 유지)은 미검증 — 세션이 필요한데 조직 지침상 비밀번호 입력 불가. → 순서 §섬P P1
- [x] `☑` **P2 · GBC017 큐레이션 POI 목록** — 백엔드 v0.5.1에서 `GET /poi` 구현 확인 → 착수. `api/poi.ts` `getPois`(실측 타입 1:1) + `usePoiList` 내부를 목→API 교체(선택 시군구 **병렬 조회 후 합침**, in-flight dedup) + `plannerStore.poiCatalog`/`registerPois`(담은 POI를 코스·예산·드로어가 해석) + `ResultsPanel` 4상태(스켈레톤/에러/데이터없음/카테고리없음) + 실 썸네일(`ImgPlaceholder` img 폴백). DoD 충족(실 지역·인원 필터 POI 렌더). ⚠️ 백엔드에 `theme` 파라미터 없음(테마 필터 불가), `avgPrice` 항상 null(가격 미정). → 순서 §섬P P2
- [x] `☑` **P3 · GBC018 POI 상세 통합** — **DoD 충족**(2026-08-22 목 대역으로 브라우저 실측: **카드 클릭·마커 클릭 모두** 실상세 렌더). `api/poi.ts` 의 잠정 `PoiDetail` 을 백엔드 `PoiDetailResponseDto` **실측 1:1** 로 교체(`title`·`tel`·`homepage`·`overview`·`firstimage`/`firstimage2`·`addr1`/`addr2`·`mapx`/`mapy`·`avgPrice`·`infoList[{infoname,infotext}]`) + `getPoi(contentId)` 신설(`GET /poi/{contentId}`, 인증 불필요). `hooks/usePoi` 를 **목/스토어 해석 → 실상세 조회**로 교체: contentId 단위 **캐시 + in-flight dedup**(드로어 재오픈 시 재요청 없음·데스크톱·모바일 동시 마운트 대비), 캐시 적중 시 스켈레톤이 깜빡이지 않게 **동기 시드**, `useAsync` 의 stale-while-revalidate 때문에 POI 를 갈아탄 직후 이전 상세가 보이던 문제는 **응답 `contentId` 대조**로 차단. 반환 타입을 `{poi, detail, loading, error, reload}` 로 넓혔다(소비처는 드로어 1곳). 병합 규칙은 `utils/poiDetail.ts`(신설, 순수 함수)에 모았다 — **상세는 빈 칸만 채운다**(코스의 방문 시각·비용·운영시간, 목록의 실좌표를 덮지 않음), 예외로 `장소 #id` placeholder 이름만 상세 제목으로 승격, 운영시간은 `infoList` 의 "…시간" 항목으로 폴백, 좌표는 **쌍이 갖춰졌을 때만** 유지/교체. 좌표 유효성(`0` 좌표·범위 밖 방어)은 `utils/coords.isValidTourCoord` 로 추출해 `usePoiList` 와 공유. `PoiDrawer` 는 **점진적 보강** 렌더: 기존 데이터로 즉시 그리고 상세가 오면 소개(`whitespace-pre-line`, 로딩 중 3줄 스켈레톤)·주소·전화(`tel:` 링크)·홈페이지(URL 이면 링크·아니면 텍스트)·부가정보(`dl/dt/dd`)를 채우고, 실패 시엔 보이는 정보를 유지한 채 인라인 `다시 시도`(`role="alert"`)만 띄운다. 정보 카드의 운영시간과 같은 값인 부가정보 행은 중복 제거. **HTML 은 백엔드가 이미 제거**해 주므로 텍스트로 렌더(`dangerouslySetInnerHTML` 없음). 검증: `npm run lint`·`npm run build` 통과 + `utils/poiDetail`·`utils/coords` 를 번들 → node 로 **39케이스 통과**(상세만으로 구성·placeholder 이름 승격·실이름 보존·운영시간 폴백 5종·이미지 폴백 3종·좌표 5종(0/null/한쪽만/기존 우선)·코스 확정값 6종 보존·`avgPrice` 무시·좌표 검증 5종). ✅ **라이브 확인**(2026-08-22, 목 대역 + dev 5173): 경주 14곳 목록에서 **카드 클릭** → 소개 2줄(`
` 보존)·주소(addr1+addr2)·전화(`tel:`)·홈페이지 링크·부가정보(`dl/dt/dd`) 렌더, **운영시간과 값이 같은 부가정보 행은 실제로 제거**됨(불국사 '이용시간', 첨성대 '이용시간'). **마커 클릭**(경주월드) 도 같은 상세 렌더 — 토글 배지 클릭과 구분됨. 데이터 3종을 모두 태웠다: ①rich(불국사: 홈페이지+부가정보 4행) ②홈페이지 없음(첨성대) ③**전부 null**(보문호반길: 좌표·이미지·소개·주소·전화·부가정보 없음 → 이미지 폴백 + 운영시간 '정보 준비 중' + 가격 미정, 에러 없음). `contentTypeId 25`(여행코스)가 `catOfContentType` 로 관광지에 접히는 것도 확인. ⏳ **실 백엔드 왕복은 여전히 미검증** — 목은 백엔드 소스 실측을 1:1 로 옮긴 대역이라 TourAPI 라이브 지연·HTML 잔여·실 contentId 는 대체하지 못한다. 계약은 백엔드 소스 실측으로 대조(추적표 GBC018 항목). ⚠️ `avgPrice` 는 상세에서도 **항상 null**(BOQ14) → 가격 표기는 여전히 '가격 미정'. 찜 상태(`liked`)도 상세 응답에 없다(추적표 #9). 카카오맵 실연동은 2026-08-15 선반영·검증 완료. → 순서 §섬P P3

### 🧩 그룹 F · 완료분 (F3·F4·F5·F7·F9)

- [x] `☑` **F3 · 이동수단별 코스 간 이동시간 표시** — `utils/travelTime.ts` 신설(`haversineKm` 직선거리 → `DETOUR_FACTOR 1.3` 우회 보정 → 수단별 **실효** 속도(도보 4 · 대중교통 20 · 자차 40km/h, 대기·환승·시내 저속 포함한 문앞-문앞 기준)로 환산, **5분 단위 라운딩·하한 5분**, `coordsOf`/`formatTravelMinutes`(`55분`/`1시간`/`3시간 20분`) 동반. 좌표를 모르면 **`null` 을 돌려 표기를 생략**한다 — 0분으로 보여 "바로 옆"으로 오해하게 두지 않는다) + `components/planner/parts/TravelConnector.tsx`(항목 사이 `약 20분 이동` + 수단별 lucide 아이콘 `Footprints`/`Bus`/`Car`, sortable 로 등록하지 않아 DnD 와 무관) + `CoursePanel`(활성 Day 항목을 한 번 해석해 `items` 로 두고 그 사이 이동시간을 `useMemo` 로 계산, `Fragment` 로 커넥터를 끼운다). **좌표 확보 경로 보강**: 코스 조회 응답엔 좌표가 없어(GBC012) 지도를 열지 않으면 계산할 값이 없으므로 `CoursePanel` 도 `useCourseCoords` 를 마운트한다(`MapView` 와 같은 훅·같은 `placeCoords` 캐시라 중복 조회 없음). 추정 근거는 패널에 한 줄로 명시(`항목 사이 이동시간은 직선거리 기반 추정치예요`, 표시할 구간이 하나라도 있을 때만). 부수: 해석 실패 장소를 미리 걸러 순번 배지가 건너뛰지 않는다. DoD 충족. 검증: `npm run lint`·`npm run build` 통과 + `travelTime` 을 rolldown 번들 → node 로 **28케이스 통과**(실좌표 거리·대칭성·수단 단조성·하한/라운딩/미지 수단 폴백·좌표 결측 4종 `null`·NaN 방어·포맷 6종). ✅ **라이브 확인**(dev 5173, 상세 응답 + `setPlaceCoords` 주입): 자차 20/20분 → 대중교통 45/40분 → 도보 3시간 40분/3시간 20분 **즉시 갱신**(아이콘도 함께), **실제 마우스 드래그로 재정렬**하니 새 순서로 재계산(20분/5분), 좌표 없는 장소(숙박) 앞 구간은 표기 없음, 장소 1곳인 Day 는 커넥터·안내문 모두 없음, 다크 테마 정상, 콘솔 에러 0. ⚠️ 실 경로 API(카카오 길찾기)는 범위 외 — 값은 어디까지나 추정치다. → 순서 §그룹F F3
- [x] `☑` **F4 · POI 좋아요 토글 정합성 조사·수정** — **원인 확정 + FE 결함 2건 수정**. **(1) 원인**(백엔드 소스 실측): ①후보 (a) *insert-only* 는 **기각** — `PoiLikeServiceImpl.toggleLike` 는 진짜 토글이다(기존 행 있으면 `delete`+`decrementLikes`(0 하한)→`liked:false`, 없으면 `saveAndFlush`+`incrementLikes`→`liked:true`, 컨트롤러 메시지도 "좋아요가 취소되었습니다."로 갈린다). ②사용자 증상("추가만 됨")의 실제 원인은 **백엔드 저장 유실 버그**로, 커밋 `6e4e682`(2026-08-16, "벌크 업데이트 `clearAutomatically` 로 INSERT 유실")에서 **이미 수정됐다** — 즉 보고 시점의 백엔드 버그이며 FE 몫이 아니다. ③후보 (b) **초기 찜 상태 복원 경로 없음은 확정** — `PoiController` 는 `/poi`·`/poi/{contentId}`·`/poi/{contentId}/like` 3개뿐이고 `PoiCurationItemDto`·`PoiDetailResponseDto` 에 `liked`/`likes` 가 없다(`UserPoiLikeRepository.existsByUserIdAndContentId` 는 있으나 어떤 컨트롤러도 쓰지 않음) → 새로고침하면 하트가 비어 보이고 그 상태에서 누르면 실제로는 **해제**된다. → 계약 추적표 **#7 확정 · #9 신설**(조회 API 요청: `GET /poi/likes` 또는 목록·상세 응답에 `liked` 필드). **(2) FE 수정**: ㉮ `poiLikeStore.reset()` + `authStore` 구독 — **세션이 바뀌면(로그아웃·계정 전환) 찜 상태를 비운다**. 없으면 로그아웃 후에도 이전 사용자의 하트가 채워진 채 남았다(조사 중 발견한 실결함). 401 재발급(`setAuth(token)` 으로 userId 유지)·`status` 변화만으로는 비우지 않는다. ㉯ `usePoiLike` — 응답 `liked` 가 낙관적 기대와 **다를 때** toast 로 알린다(`이미 찜한 곳이어서 찜을 해제했어요` / `찜에 추가했어요`). 찜 상태 조회 API 가 없는 동안 "왜 하트가 꺼지지?"를 사용자가 알 수 있게 하는 최소 보정이며, 표시 자체는 **끝까지 서버 응답 기준**이다(localStorage 로 찜을 흉내내는 안은 서버 진실과 어긋나 채택하지 않음 — 사양 명시). DoD 충족(원인 문서화 + FE 몫 수정 + "두 번째 클릭에서 무엇이 오는지" 기록). 검증: `npm run lint`·`npm run build` 통과 + 두 스토어를 번들 → node 로 **13케이스 통과**(로그아웃 시 `liked`·`likes` 비움, 계정 전환 A→B 비움, 재발급은 유지, `status` 변화는 유지, userId null 세션도 비움, `reset()`, `count` 생략 시 개수 보존). ⏳ **브라우저 실측은 사용자 대행 필요**(좋아요는 로그인 필수 + 조직 지침상 에이전트 비밀번호 입력 불가). 코스 생성 API 보완 중이라 이번 세션에서 생성 호출은 하지 않았다. → 순서 §그룹F F4
- [x] `☑` **F5 · 지도 코스 전용 표시(내 코스만 보기) + 컬렉션 진입 지도 미표시 수정** — **DoD 충족**(2026-08-22 모바일 폭 실측으로 마지막 조건 해소). **원인 확정**: `loadDetail` 이 `search.dests` 를 빈 배열로 두고(상세 응답에 시군구 없음) `ResultsPanel` 이 지역 미선택 EmptyState 로 early return → 지도 토글을 눌러도 `MapView` 에 도달하지 못했다. 수정: (1) `viewMode === "map"` 이면 지역 미선택·로딩·데이터 없음보다 **먼저** 지도를 렌더, (2) 헤더에 `내 코스만 보기` 토글(지역 미선택이면 기본 on, 켜면 카테고리 칩 숨김), (3) **좌표 복구** — 코스 응답엔 좌표가 없어 지도가 비므로 `utils/kakaoGeocode`(카카오 로컬 키워드 검색, 경북 bbox 밖 동명 장소 배제·이름별 1회 캐시·6초 타임아웃) + `hooks/useCourseCoords` + `plannerStore.placeCoords`(좌표는 변하지 않는 사실이라 코스 재로드에도 유지) + `mergePoi(…, coords)` 가 **좌표 없는 장소에만** 얹는 구조, (4) 폴백 지도(`PlaceholderMap`)는 % 좌표로 그려 코스 장소가 전부 중앙에 겹치던 문제를 `mapModel.projectToPlaceholder`(실좌표 bbox → 10~90% 재투영)로 해결. ✅ **라이브 확인**(dev 서버 + 스토어에 실제 상세 응답 형태를 주입해 컬렉션 진입 재현): 지도 클릭 시 **즉시** 지도 렌더(이전엔 안내 화면), 토글 on/off 로 칩·마커 반응, 좌표 주입 시 마커가 10~90% 로 퍼지고 경로선이 코스 순서대로 이어짐, 리스트 모드 안내는 그대로 유지, 콘솔 에러 0. ✅ **라이브 재확인**(2026-08-22, 목 대역 데이터): '지도' 클릭 → 즉시 렌더, `내 코스만 보기` on → 카테고리 칩 사라지고 코스 마커만 남으며 순번 배지·경로선 유지, off → POI 마커 복귀. 폴백 지도의 bbox 재투영으로 마커가 10~90% 로 퍼진다. ✅ **모바일 폭(390px) 라이브 확인**(2026-08-22): 브라우저 창 리사이즈는 여전히 무효(`resize_window` 는 성공을 보고하지만 `innerWidth` 가 1440 에 고정)라 **동일 출처 iframe 을 390×731 로 띄워 미디어쿼리를 실제 모바일 폭에서 평가**시키고, Vite dev 모듈 그래프에서 `plannerStore` 를 직접 import 해 `loadDetail(상세응답)` 로 **컬렉션 진입 상태(`dests: []`)를 그대로 재현**했다. 결과: 탭 전환형 모바일 레이아웃(결과/코스/예산) 정상 → '지도' 탭 **즉시 렌더**(지역 미선택 안내 없이), `내 코스만 보기` 가 **기본 on** 이고 카테고리 칩이 숨겨지며 코스 마커·순번 배지·경로선만 표시, `dests` 를 채우니 토글이 **자동 off** 로 풀리고 카테고리 칩·POI 마커가 복귀. **카카오 지도 SDK 도 이번엔 정상 로드**(이전 세션의 503 은 외부 일시 장애였다) → 폴백 지도가 아닌 **실 지도 타일 위에서** 마커·경로선을 확인했다. 좌표 없는 장소(`mapx/mapy: null`)는 마커가 생기지 않는 설계대로 동작. 모바일 폭 초기 로드 **콘솔 에러·경고 0건**. ⏳ **남은 것**: **컬렉션 → 코스 카드 → 플래너** 진입의 **문자 그대로의 클릭 경로**는 로그인이 필요해 사용자 대행이다(조직 지침상 에이전트가 비밀번호를 입력할 수 없다). 다만 그 경로가 만들어 내는 **상태**(`loadDetail` + `dests: []`)는 위에서 그대로 재현해 데스크톱·모바일 양 폭에서 검증했고, 네비게이션 자체는 S4(GBC012)로 이미 충족돼 있어 DoD 를 `☑` 로 판정한다. ✅ **좌표는 근본 해결됨**(2026-08-22): 백엔드가 R2 를 수용해 `cd6c7ec`(0.6.3)로 코스 상세·공개뷰에 `mapx`/`mapy` 를 실어 준다 → `synthesizePoi` 가 그 좌표로 `Poi.lat/lng` 를 채우고 **장소명 지오코딩은 좌표 null 장소 전용 폴백으로 격하**됐다(제거하지 않음 — 백엔드 POI 캐시 미스가 실재해서 코스 1의 18곳 중 6곳이 좌표 `null` 이다. 다만 그 6곳은 `placeName` 도 `''` 라 폴백도 건너뛴다 = 백엔드 캐시 공백이 남는다). **실 백엔드 라이브 실측**: 18곳 중 12곳 즉시 렌더·지오코딩 왕복 0회, 폴백 지도에서 실좌표 4곳이 bbox 재투영으로 퍼지고 좌표 없는 2곳만 중앙에 겹침. 추적표 #8 `☑`. → 순서 §그룹F F5
- [x] `☑` **F7 · 지도 마커 겹침으로 담기/빼기 토글이 도달 불가** — **DoD 충족**(2026-08-29 그룹을 펼친 뒤 상태까지 실측 통과). 의존: F6. **2026-08-22 모바일 폭(390px) 검증에서 발견한 실결함.** 좁은 지도에서 마커가 겹치면 **가려진 마커의 토글이 자기 중심점에서도 눌리지 않는다** — `elementFromPoint(토글 중심)` 이 옆 마커 본체를 되돌려 주고, 그 자리를 누르면 담기 대신 **엉뚱한 POI 의 상세 드로어가 열린다**. 실측: 코스 3곳 + POI 4곳(토글 6개) 중 **1개**(대릉원 — 교리김밥과 CSS 9px 간격, 실거리 약 0.7km)가 도달 불가로 재현. 데스크톱은 지도가 넓어 빈도가 낮을 뿐 **원인은 같다**(마커 오버레이에 겹침 시 z-order 규칙이 없고 클러스터링도 없다). 후보안: ㉮ 겹친 마커 감지 시 **활성/최근 조작 마커를 위로 올리는 z-index 규칙**(가장 좁은 수정) ㉯ 저줌에서 **마커 클러스터링**(카카오 SDK 클러스터러 또는 자체 그리드) ㉰ 토글을 마커에서 떼어 **선택된 마커 하나에만 노출**(2단 인터랙션이지만 겹침에 면역). DoD: 겹친 마커 상황에서도 각 마커의 토글이 눌리고, 오조작으로 다른 POI 의 드로어가 열리지 않는다(모바일 폭 실측 포함). **채택안(사용자 승인)**: POI 클러스터링 + 코스 마커 예외. ㉮ z-order 단독안은 **분석으로 기각** — `KakaoMap.zIndexOf` 가 이미 활성 30 / 코스 20 / 일반 10 인데 문제의 두 마커는 **둘 다 일반 POI(z 10)** 라 DOM 순서로 승패가 갈리고, 배지(28px)와 토글(22px, 배지 오른쪽 아래로 6px 돌출)의 기하상 **가려진 쪽은 배지 중심까지 덮여** 상세 드로어조차 못 연다. **구현**: `components/planner/mapCluster.ts` **신설**(순수 함수 2개) — ①`clusterMarkers`: 코스 마커(순번 있음)·열려 있는 마커(`active`)는 **절대 접지 않고** 나머지 POI 는 화면상 40px(`배지 28 + 토글 돌출 6 + 여유 6`) 안쪽이면 접는다. 접은 그룹 중심끼리 또 겹칠 수 있어 **겹침이 사라질 때까지 반복 병합**(union-find). 배율이 무효면 **아무것도 접지 않고 전부 개별로** 돌려준다(클러스터링 실패가 마커 소실로 이어지지 않게). ②`spreadOverlaps`: 접고도 남는 겹침(코스↔코스, 그룹↔코스)을 해소 — z 내림차순으로 놓으며 **위에 그려지는 항목은 제자리**를 지키고 아래쪽만 원래 자리 주변 고리(반지름 `minSep`→캡 `minSep×1.2`, 12슬롯)에서 **빈 자리를 찾아** 옮긴다("밀어내기"는 연쇄로 날아가고 캡에 걸려 겹침이 남아 단위테스트로 확인 후 기각). 빈 자리가 없으면 **옮기지 않는다**(위치 왜곡 < 겹침). `markerStyle`: `markerClusterClass`(32px · 흰 바탕+브랜드색 테두리·숫자 → 카테고리색 위 흰 숫자인 코스 순번 배지와 **반대로** 칠해 구분)·`markerClusterLabel`. `KakaoMap`: 배율(화면 1도당 px)을 `getBounds()`+컨테이너 크기로 읽고 **`idle` 에서만** 갱신(`zoom_changed` 는 애니메이션 **시작**에 와 중간 상태를 물어온다) · 오버레이 저장소를 `marker`/`cluster` 판별 유니온으로 확장(`at` 에 마지막 표시 좌표를 기록해 바뀔 때만 `setPosition`) · 그룹 배지 클릭 → `setCenter`+`setLevel(-2, animate:false)` 로 펼치고 최소 레벨이면 toast 안내 · **경로선에도 같은 변위 적용**(`routePath`) · 폭주 방어 2겹(`MAX_PLAUSIBLE_SPAN_DEG=3` 으로 비정상 시야 배율 폐기, `MAX_SHIFT_DEG=0.5` 로 변위 상한 — 처음 0.01° 로 뒀더니 **광역 시야의 정당한 변위(40px≈수 km)까지 막혀** 실측으로 되돌렸다). `utils/kakaoMap.ts` 타입 확장(`getBounds`·`getSouthWest/getNorthEast`·`setPosition?`·`event?`) — 모두 optional/guard 라 없으면 클러스터링만 비활성된다. **검증(완료)**: `npm run lint`·`npm run build`(tsc 포함) 통과 + `mapCluster` 를 rolldown 번들 → node 로 **50케이스 통과**(클러스터 27 / 겹침해소 23) + 빌드 산출 CSS 에 클러스터 유틸(`h-8`·`ring-2`·`ring-primary`·`scale-110`) 생성 확인(**명령형 DOM 이라 Tailwind 스캔 누락이 치명적**). ✅ **라이브 실측**(390px iframe 하니스 + **실 백엔드 경주 318곳**): 좌표 유효한 POI **60곳이 2개 그룹(56곳·4곳)으로 접히고** 코스 마커 3곳은 접히지 않고 순번 배지·경로선 유지. 지도 컨테이너 안 **8개 항목 전부 `elementFromPoint` 가 자기 자신**(=도달 가능) — **수정 전 같은 시나리오에서는 13개 중 3개가 도달 불가**(불국사 배지·토글이 석굴암에 가려짐, 56곳 그룹이 황남빵 토글에 가려짐). 그룹 배지 클릭 → 확대되어 **56곳 → 40곳+3곳+개별 마커**로 풀림. ✅ **그룹을 펼친 뒤 상태 실측 통과(2026-08-29 · 사용자가 띄운 실 백엔드 경주 316곳)** — 마지막 남은 조건을 닫았다. 390px iframe 하니스에서 `createCourse`(GBC010)로 코스를 **3회 새로 만들고**(#70 Day1 5곳 · #74 Day1 7곳 · #75 Day1 8곳) **초기 뷰 → 가장 큰 그룹 배지 클릭 → 그룹이 없어질 때까지** 반복하며 매 단계 지도 컨테이너 안의 모든 마커·토글·그룹 배지 중심에서 `elementFromPoint` 가 자기 자신인지 봤다. 결과 모바일 390px **14/14 → 19/19 → 20/20 → 10/10**, 재현 **16/16 → 21/21 → 22/22 → 13/13**(`55곳` 그룹이 `40곳 + 11곳 + 개별` 로 두 단계에 걸쳐 풀렸다), **가려짐 0 · 크기0 0**. 지난 세션의 "펼친 뒤 마커 DOM 이 빈다"는 관찰은 **하니스 탓으로 확정** — `축소` 를 연타해 만든 상태(DoD 흐름 밖)에서만 재현됐고, 그때 카카오 SDK 의 오버레이 레이어(`position:absolute; z-index:1; height:0`)가 `display:none` 이었다. 🧪 **결정적 보강**: `mapCluster` 를 rolldown 번들 → node 로 돌려 **실제 DOM 기하와 z 규칙을 그대로 옮긴 도달 가능성 시뮬레이터**를 만들었다 (배지 = 28px **사각** `button`, 토글 = 22px **원**(`rounded-full` 이라 배지 중심을 덮지 않는다 — 거리 12.7px > 반지름 11px), 그룹 = 32px 사각, z 활성30/코스20/그룹15/일반10, **동률 겹침은 실패로 계산**). 앱의 줌 흐름(`setBounds` fit → 그룹 클릭 시 `level−2`)을 6개 데이터셋 × 5시드 × 모바일·데스크톱으로 **392상태** 판정 — 경주형(318곳)·격자·균등·활성마커 데이터셋은 **전 단계 통과**. 부수 확인: 위 기하에서 **점 간격 40px 이면 도달 가능이 수학적으로 보장**된다(최악 조합이 36.8px < 40px) → `DEFAULT_MIN_SEPARATION_PX` 40 은 충분한 값이다. ⚠️ **잔여 2건은 F8 로 분리**(둘 다 이 Task 의 채택안 = POI 클러스터링이 다루는 원인이 아니다): 지도 컨트롤 겹침 · 밀집 코스 마커에서 `spreadOverlaps` 자리 없음. → 순서 §그룹F F7
- [x] `☑` **F9 · 찜 상태(`liked`)·좋아요 수(`totalLiked`)·별점(`stars`) 연동** — **DoD 충족**(2026-08-29 로그인 E2E 통과). 사용자 요청 2건으로 착수(①GBC019 토글 반환값을 해당 contentId 의 정보에 반영 ②GBC017·GBC018 의 `liked`·`totalLiked`·`stars` 구현). **백엔드 실측**(`../back` 0.6.4~0.6.7): 목록 `PoiCurationItemDto` += `liked`·`stars`(총개수는 **없다**), 상세 `PoiDetailResponseDto` += `liked`·`totalLiked`·`stars`, 토글 `PoiLikeResponseDto` 는 **`likes` → `totalLiked` 로 개명**(0.6.5). 둘 다 `permitAll` 이라 비로그인·탈퇴는 `liked:false`, 평점 행이 없으면 `stars:null`·`totalLiked:0`. **FE 변경**: ①`api/poi.ts` 타입 3종을 실측 1:1 로 갱신(개명 반영 — 종전 `res.likes` 는 `undefined` 라 **좋아요 수가 유실되고 있었다**), ②`poiLikeStore` 를 `{liked, totalLiked}` 2맵 + `hydrate()` 로 확장(**이미 아는 값은 덮지 않음** → 방금 토글한 결과를 낡은 목록·캐시된 상세가 되돌리지 못한다, 필드 단위 판정이라 목록의 `liked` 뒤에 상세의 `totalLiked` 가 붙는다), ③`usePoiList`/`usePoi` 가 응답을 hydrate + **세션 키를 조회·캐시 키에 포함**(`authSessionKey()` 를 `authStore` 로 승격 — `liked` 는 사용자별 값이라 로그인 전후 응답을 공유·재사용하면 하트가 거짓이 된다. 로그인하면 목록·상세가 자동 재조회된다), ④`usePoiLike` 가 토글 응답 `{liked,totalLiked}` 를 그대로 확정(재조회 없음), ⑤별점을 `Poi.rating` 으로 옮기고(`withDetail` 은 빈 칸만 채움) **`Stars` 는 0이면 렌더하지 않는다**(실 POI 카드에 붙던 `★ 0` 제거, 리뷰 수 0도 생략), ⑥`LikeButton showCount` 로 **드로어에서만** 하트 옆 총개수 표시(목록엔 `totalLiked` 가 없어 카드에 붙이면 들쭉날쭉해진다). **DoD**: 로그인 상태에서 ⓐ목록·상세 진입 시 이미 찜한 POI 의 하트가 채워져 보이고 ⓑ토글 즉시 하트·개수가 바뀌며 ⓒ드로어를 닫았다 열어도(캐시) 유지되고 ⓓ새로고침 후에도 서버 상태로 복구되며 ⓔ별점이 있는 POI 만 `★` 가 보인다. 검증: `npm run lint`·`npm run build` 통과 + node **19케이스**(별점 병합 7 · 스토어 12). ✅ **라이브 실측(실 백엔드 :8080 · dev 5173, 비로그인 범위)**: `GET /poi?sigunguCode=130` 316곳 응답에 `liked`·`stars` 실림(`stars` 는 JSON number, 316곳 중 108곳만 값 있음)·`GET /poi/128677` 에 `liked`·`totalLiked`·`stars` 실림 → **결과 카드의 별점이 API `stars` 와 전건 일치**(별점 없는 POI 는 `★` 자체를 그리지 않음, `★ 0` 0건), 드로어는 `♡ 0`(총개수, `aria-label="찜하기 (좋아요 0개)"`)와 `★ 4.0` 렌더, 목록 hydrate 로 `poiLikeStore.liked` 316키 주입, 비로그인 하트 클릭 → **`찜하려면 로그인` 게이트 · `/like` 요청 0건**, 콘솔 에러 0. ✅ **로그인 E2E 완료(2026-08-29, 사용자가 로그인 대행 · 실 백엔드 :8080 · dev 5173)**: ①로그인 목록 조회가 `liked` 316키 hydrate ②드로어 하트 클릭 → `POST /poi/128677/like` **200 `{liked:true,totalLiked:1}`** → 하트 채워짐·`찜 취소 (좋아요 1개)`·**결과 카드 하트도 동시 반영**(스토어 공유) ③**새로고침 후에도 하트 유지**(재조회 `liked:true` → 추적표 #9 의 목표 달성) ④드로어를 닫았다 다시 열면 **네트워크 요청 0건**(캐시 히트)이고 캐시에 든 낡은 상세(`liked:false`)가 **토글 결과를 되돌리지 않음**(hydrate 비덮어쓰기 규칙 실동작) ⑤해제 → **200 `{liked:false,totalLiked:0}`** → 카드·드로어 동시 해제 ⑥세션 전환: 로그아웃 시 하트 제거(게스트 재조회 `liked:false`) → 재로그인 시 **자동 재조회로 하트 복구** ⑦콘솔 에러 0. 검증 후 서버 상태는 원래대로 되돌려 뒀다(`totalLiked:0`). 계약 추적표 #9.

### 부록 A · 정리 (A1~A4)

- [x] `☑` **A1 · `/home` 더미 라우트 제거** — `router.tsx`에서 `Home` lazy import·`home` 라우트 블록 삭제 + `src/pages/Home.tsx` 삭제. 링크·네비게이션 없음 확인, lint·build 통과.
- [x] `☑` **A2 · About 콘텐츠 작성** — `pages/About.tsx` 서비스 소개(히어로·핵심기능 F1~F3·이용방법 4단계·CTA, 시맨틱 토큰·lucide·반응형) + 푸터 "서비스 소개" 링크·저작권 정정. 라이브 라이트/다크 확인.
- [x] `☑` **A3 · 문서 드리프트 최신화** — `CLAUDE.md`(디렉터리 트리 정정+핵심 파일 보강)·`PRD_FRONT.md`(§5 구현 매핑 갱신+보드 포인터)·`FEATURES_FRONT.md`(구현 표기 스냅샷 경고+보드 포인터). 보드를 구현 현황 정본으로 명시.
- [x] `☑` **A4 · RootLayout 정리 판단** — CLAUDE.md 규칙상 삭제 금지 → **존치 결정**. 의도된 미연결 플레이스홀더(향후 Layout 상위 공통 셸 자리)임을 `RootLayout.tsx` 주석으로 문서화.

---

## 2. 진행 로그 (2026-07-17 ~ 2026-08-22)

> 보드에는 **최근 회차(2026-08-29~)** 만 남기고 그 이전 전량을 여기로 옮겼다. 형식은 원문 그대로.

- 2026-07-17 · 보드 초기 등록 (24개 Task). 착수 전 상태.
- 2026-07-17 · S0-A 완료 · 계약 4건 스펙 가정으로 진행, `docs/FE_계약_추적표.md` 신설(비블로킹).
- 2026-07-17 · S0-B 완료 · `api/tourCourse.ts`·`api/poi.ts` 타입 정의(코스=스펙 1:1, POI=잠정). lint·build 통과.
- 2026-07-17 · S0-C 완료 · `authStore` `userId`+`setAuth` 확장, 로그인/카카오/재발급 연동 + localStorage 보관.
- 2026-07-17 · S0-D 완료 · 공용 `EmptyState`/`ErrorState`/`Skeleton` + `hooks/useAsync` 패턴 확립.
- 2026-07-17 · S0 검증 · 4렌즈 적대적 검증 워크플로 → 확정 2건 반영(useAsync 실패 시 data 보존, ErrorState `aria-hidden`). lint·build 재통과.
- 2026-07-17 · 백엔드 소스(`../back`) 실측 검증 · 계약 4건 대조 → transport/sigunguCode/theme 타입 일치 확정, **userId 백엔드 미제공 확인(섬 M 보류)**, sigungu(법정동47)·theme(라벨) 값주의. 타입 보정 2건(`CoursePlace.type` 주석, `TogglePoiLikeResponse.likes`) + 추적표 실측 갱신.
- 2026-07-17 · S1 완료 · GBC010 생성 파이프라인(홈 검색→`createCourse`→`loadFromApi`→`/planner`) + 이동수단 선택 UI + placeholder POI 렌더(Option A: 생성 응답에 장소명 없어 `장소 #contentId`로 표시, 실데이터는 P3 후). 계약값 적용: transport 대문자 enum·sigunguCode `47`+값 단일·theme 한국어 라벨·게스트 courseId 스토어 보관·부팅 목업/`console.log` 제거. 4렌즈 적대적 검증(15에이전트) → 확정 3종 반영: ①`search.dests`(시군구 코드)를 sigunguStore 라벨로 해석(Planner/ResultsPanel 지역명), ②빈 코스 유령 요약/예산 가드, ③하루 내 `contentId` dedup. lint·build 통과. ⚠️ 실백엔드 E2E는 dev 서버 기동 시 확인 필요, ResultsPanel 목 브라우즈는 P2에서 실 POI로 대체.
- 2026-08-08 · S1 정정 · `sigunguCodes` 계약 드리프트 수정 — FE가 단수 `sigunguCode`(접두 47, 예 `47130`)를 보내 백엔드 `sigunguCodes`(복수, bare 3자리 `lDongSignguCd`, 예 `130`)와 불일치 → 지역 필터가 조용히 무시되던 문제. `CreateCourseRequest.sigunguCodes?: string[]`로 변경, `Index`가 선택 지역 전부(bare value)를 전송(`47` 접두 제거). 라이브 실측: `47130`→"지역 데이터 없음"400 / `130`→지역 통과. 계약 추적표 #2 ☑ 확정으로 갱신. lint·build 통과. (별건: 지역 지정 시 백엔드 AI가 간헐적으로 "존재하지 않는 장소 ID" 400 — 백엔드 이슈. POI `getPois`의 단수 `sigunguCode`는 P2 착수 시 재확인.)
- 2026-08-08 · S3 브라우저 E2E 검증 · 실백엔드(`localhost:8080`) 대상 라이브 확인. ✓가드 리다이렉트(무세션 `/collection/`→`/auth/login`), ✓세션 복원(`POST /auth/reissue` 200)→가드 통과, ✓`GET /tour-course` 200→빈 상태(EmptyState), ✓데이터 상태(코스 생성→목록 카드 렌더: 기간·N박M일·인원·이동수단·테마 배지·생성일 정확, 카드→`/planner/13` 링크 확인). **발견·수정**: AI 생성 코스는 title 빈 값 저장(제목 지정=S6 이후)이라 카드 제목 헤딩이 공백 → `course.title?.trim() || 'AI 추천 코스'` 폴백 추가(커밋 2087c4a). **백엔드 이슈**: 인증 상태 생성 시 백엔드가 생성 시점에 소유권 부여 → 명시적 `PATCH .../assign`은 중복이라 **500** 반환(스펙상 "이미 소유자" 403이어야 함, 백엔드 불일치). FE는 500을 에러 토스트로 정상 처리. (에러 상태 렌더는 라이브 미검증 — 백엔드 강제 실패 경로 필요, 코드/빌드로 커버.)
- 2026-08-08 · S3 완료 · GBC011 내 코스 목록. `api/tourCourse.ts` `getMyCourses()`(`GET /tour-course` → `CourseSummary[]`) + `hooks/useCourseList.ts`(useAsync 래핑, fetcher `useCallback` 안정 참조) 신설. `Collection.tsx`(5줄 스텁 → 전면 구현): 카드 그리드(1/2/3열 반응형), 4상태(최초로딩=스켈레톤 6장·`error`(`!!error`로 좁힘)+데이터 없음=재시도 ErrorState·빈=코스 만들기 CTA EmptyState·데이터=CourseCard). 카드 필드: 제목·기간(`startDate~endDate` + `N박 M일`)·인원·이동수단(enum→한국어)·테마 배지·생성일. 시맨틱 토큰만(base-*·badge-ghost·btn-primary), a11y(skeleton `aria-hidden`·Link `focus-visible`). **주의: GBC011 응답에 지역 필드 없음** → 카드에서 "지역" 생략(DoD 문구와 차이, 스펙상 불가). 카드→`/planner/:courseId` 링크는 전방 배선(동적 라우트는 S4에서 추가, 현재 클릭 시 NotFound). lint·build 통과. ⚠️ 실백엔드 E2E는 dev+백엔드 기동+로그인 시 확인.
- 2026-08-08 · S4 브라우저 E2E 검증 · 실백엔드(`localhost:8080`) 대상 라이브 확인. ✓세션 복원(`POST /auth/reissue` 200)→가드 통과→컬렉션 카드 1개("AI 추천 코스", 2박3일·1명·자동차·문화/음식), ✓카드 클릭→`/planner/13` 이동→`GET /tour-course/13` 200→상세 렌더(요약=경상북도·2박3일·1명, 좌측 코스 패널 Day1/2/3 각 6곳, **실 장소명** `placeName` 표시: 경주 석빙고(관광지 09:00)·고려분식(음식점 12:00)·구미시문화예술회관(문화시설 13:30)·경주 첨성대…), ✓**새로고침 URL 재진입**(`/planner/13` 리로드→`reissue` 200→`getCourse` 200→동일 렌더). (요청 2회는 dev StrictMode 이중 호출, 멱등 확인.) **에러 경로**: 존재하지 않는 id(`/planner/999999`)→백엔드가 **401** 반환(404/403 아님)→axios 인터셉터가 401 처리(reissue 재시도 후 401→auth clear→`/auth/login` 리다이렉트). 즉 401은 페이지 `ErrorState`가 아니라 인터셉터가 처리(가이드 명시대로). ⚠️ **관찰된 부작용**: 로그인 상태에서 잘못된/삭제된 코스 링크 클릭 시 단순 에러가 아니라 **로그아웃**된다(인터셉터 기존 동작, S4 결함 아님 — 401을 토큰만료로 간주해 reissue→실패 시 로그인 유도). 향후 백엔드가 비소유/부재 코스에 403/404를 주면 인앱 에러 상태로 개선 가능.
- 2026-08-08 · S4 완료 · GBC012 코스 상세. `api/tourCourse.ts` `getCourse(courseId)`(`GET /tour-course/{id}` → `CourseDetail`, 봉투 벗김) + `hooks/useCourseDetail.ts`(URL param 받아 유효성 검사 후 fetch, 성공 시 `loadDetail` 주입; param 없으면 fetch 없이 idle) + `plannerStore.loadDetail`(상세 응답을 스토어에 매핑, `search.dests=[]`—상세엔 지역 필드 없음→'경상북도' 폴백) + `plannerRouter` `:courseId` 자식 라우트. `Planner.tsx`: `useParams`로 진입, **판정 기준=스토어가 해당 코스를 들고 있는가**(로딩 플래그 아님 → URL 전환 시 이전 코스 stale 렌더 차단). 스토어에 없으면 `<Loading>`(에러 시 `<ErrorState onRetry={reloadDetail}>`), 있으면 즉시 렌더 후 상세는 백그라운드 갱신(게스트 생성 직후 placeholder→실명 progressive enhancement). `synthesizePoi`에 `place.placeName?.trim() || '장소 #id'` 폴백 추가 → 상세(placeName 있음)는 실 장소명, 생성(없음)은 종전대로. `/planner/*`는 게스트 허용이나 상세는 소유자 인증 필요(정상 흐름=컬렉션 경유 또는 로그인 상태 새로고침이라 인증 상태). Planner 단일 lazy 청크 유지(index·:courseId 공유). lint·build 통과. ⚠️ 실백엔드 E2E는 dev+백엔드+로그인 시 확인.
- 2026-08-08 · S5 브라우저 E2E 검증 · 실백엔드(`localhost:8080`) 대상 라이브 확인(로그인 세션). ✓카드 우상단 삭제 버튼 렌더(aria-label "AI 추천 코스 삭제"), ✓클릭→`ConfirmDialog` 렌더(danger 아이콘·error색·설명에 코스명 보간 "'AI 추천 코스' 코스가 삭제되며…"·취소/삭제 버튼), ✓삭제 확인→`DELETE /api/v1/tour-course/13` **200**→후속 `GET /tour-course` 200(reload)→카드 제거·헤더 카운트 사라짐·EmptyState("저장한 코스가 없어요"+CTA) 렌더, ✓**새로고침 URL 재진입**(`/collection` 리로드→세션 복원→`GET /tour-course` 200 빈 목록→EmptyState 유지 = 서버 삭제 영속, DoD 충족). (GET 2회는 dev StrictMode 이중 호출.) 제품 버그 없음(첫 좌표 클릭 빗나감은 MCP 창 리사이즈 탓, 요소 ref 클릭으로 재수행 — 제품 무관).
- 2026-08-08 · S5 완료 · GBC013 코스 삭제. `api/tourCourse.ts` `deleteCourse(courseId)`(`DELETE /tour-course/{id}`, 봉투 벗김 void) + 재사용 `components/common/ConfirmDialog.tsx`(LoginGateModal a11y 패턴 계승 — `role=dialog`·`aria-modal`·Escape 닫기·오버레이 클릭 닫기, `danger`(error색 강조)·`busy`(스피너+버튼/Escape/오버레이 잠금) prop) + `hooks/useCourseDelete.ts`(서버 요청+진행상태+성공/실패 toast 캡슐화, `deleting` 가드로 이중 발사 방지; useCourseSave 결과 동일). `Collection.tsx`: 카드 우상단 삭제 버튼을 **Link 의 형제**로 배치(앵커 안 버튼 중첩 회피, z-10 오버레이)·제목 `pr-9`로 겹침 방지·생성일은 하단 푸터로 이동. 삭제 확인 대상(`pendingDelete`)은 UI 상태라 페이지 보유, 확인 시 `remove(courseId, ()=>{닫기+reload()})` → 성공 시 목록 서버 재조회(reload는 stale-while-revalidate라 스켈레톤 플래시 없음). 실패 시 다이얼로그 유지+재시도 가능. ⚠️ 코스 **전체** 삭제(플래너 POI 단위 `removePoi`와 무관). lint·build 통과. 실백엔드 E2E(실제 삭제는 파괴적)는 dev+백엔드+로그인 시 확인.
- 2026-08-08 · S6 완료 · GBC015 코스 제목 수정. `api/tourCourse.ts` `updateCourseTitle(courseId, title)`(`PATCH /tour-course/{id}/title`, body `{title}`(255자 이하), 봉투 벗김 void) + `plannerStore.setTitle`(낙관적 업데이트·롤백용 동기 세터) + `hooks/useCourseTitle`(요청 직전 `getState().course.title`로 이전값 캡처→낙관적 `setTitle`→실패 시 롤백+toast, 공백·미변경은 no-op) + `components/planner/EditableCourseTitle`(요약 헤더 제목: `editable=false`면 읽기전용 `<h1>`, `true`면 클릭→autofocus+전체선택 input→**Enter/blur 단일 커밋**·Escape는 `cancelRef`로 blur 커밋 차단해 취소, 저장 중 스피너). `Planner.tsx` 요약 헤더의 읽기전용 `<h1>{course.title}` → `EditableCourseTitle`로 교체, 편집 노출 조건 `isAuthenticated && storeCourseId!=null`(게스트·미저장 코스는 편집 UI 미노출). CoursePanel의 제목 표시(:98)는 스토어 title 반영이라 무변경(낙관적 편집 시 자동 갱신). ⚠️ 비소유 코스 편집 시 백엔드 401→인터셉터 로그아웃(S4 기록 기존 동작, 정상 흐름=컬렉션 경유 소유 코스에선 미발생). lint·build 통과. 실백엔드 E2E(제목 영속·새로고침 유지)는 dev+백엔드+로그인 시 확인.
- 2026-08-08 · S7 완료 · GBC014 공유 + 공개뷰. `api/tourCourse.ts` `getPublicCourse(courseId)`(`GET /tour-course/{id}/view`, **`apiClient` 대신 순수 axios**로 호출 — 공개뷰는 토큰 없이 200이어야 하는데 인터셉터를 타면 비로그인 수신자에게 재발급 실패→강제 로그인 리다이렉트가 걸려 공유 UX가 깨지므로 원천 차단). `routes/shareRouter.tsx`(가드 밖 `/share/:courseId`, `router.tsx`에서 Layout 하위·RequireAuth 형제로 spread) + `pages/Share/Share.tsx`(읽기전용: "공유받은 코스" 배너+요약 헤더(제목·경상북도·기간·인원·이동수단·테마)+Day별 일정 카드(시간·장소명·타입 라벨)+"코스 만들기" CTA, `useAsync`로 로딩/에러) + `hooks/useCourseShare.ts`(공유 흐름 캡슐화: `shareViaKakao`→false면 `copyToClipboard` 폴백, courseId 없으면 no-op, 로그인 불필요) + `utils/kakaoShare.ts`(Kakao JS SDK 온디맨드 `<script>` 주입+`init`, `Share.sendDefault` 피드 템플릿; 키없음·도메인미등록·로드실패 등 모든 실패를 삼켜 false 반환 → 클립보드 폴백이 DoD "링크 생성"을 항상 보장) + `utils/courseFormat.ts`(`TRANSPORT_LABEL`·`PLACE_TYPE_LABEL`·`formatDate`·`formatTime`·`tripDuration` 추출, Collection이 로컬 정의 대신 재사용). `Planner.tsx` `onShare` toast 목업 제거→`share()`, 공유 게이트(requireAuth) 제거(수신자만 비로그인). `client.ts`는 `API_BASE_URL` export(공개 요청 재사용). lint·build 통과.
- 2026-08-08 · S7 브라우저 E2E 검증(5173, 실백엔드) · ✓`/share/999` 진입 시 URL 유지(=**가드 밖**, `/auth/login` 리다이렉트 없음), ✓`GET /tour-course/999/view` **순수 axios 직행→404**(reissue 인터셉터·강제 로그아웃 루프 안 탐; 보인 `auth/reissue` 200 2건은 App 부팅 세션복원으로 `/view`와 무관), ✓부재 코스→`ErrorState`("코스를 찾을 수 없어요"+백엔드 msg "존재하지 않는 코스입니다"(`getApiErrorMessage`)+다시 시도), ✓StrictMode 이중 호출 멱등(2×404 동일), ✓콘솔 에러 없음. App.tsx 부팅 로직 대조: 세션복원 실패 시 `clear()`(게스트)만·리다이렉트 없음 → 로그아웃 수신자도 리다이렉트 없이 공개뷰 열람(DoD 메커니즘 충족). ⚠️ **백엔드 아웃티지**: `POST /tour-course`(AI 생성)가 모든 파라미터 조합에서 즉시 **500**(지역 지정/미지정·테마 무관), 기존 코스 0개(id 1~40 `/view` 스캔 전부 부재) → 실데이터 해피패스 렌더·공유 버튼(카카오/클립보드)은 라이브 미검증(코드·build·lint로 커버, 백엔드 AI 복구 시 재확인). 이 500은 S1 로그의 AI 간헐 오류와 동일 계열의 백엔드 이슈.
- 2026-08-08 · Index 검색바 lg 개선 + 일정 날짜 축약 · 사용자 요청(lg 구간 개선 + 일정 선택 시 날짜가 길어져 잘리는 문제). **①날짜 잘림 해결(핵심)**: `react-datepicker`가 `dateFormat="yyyy/MM/dd"`로 범위를 "2026/08/12 - 2026/08/14"(≈23자)로 렌더 → 좁은 검색바 칸에서 잘림. **커스텀 입력 컴포넌트 `DateTrigger`**(`forwardRef<HTMLButtonElement>`, 모듈 스코프라 리마운트 없음) + `dateFormat="MM/dd"`로 교체 → react-datepicker 가 만든 "MM/dd - MM/dd"의 구분자만 `~`로 치환해 **"08/12 ~ 08/22"**로 표시(연도 제거). 값 없으면 placeholder 흐리게, `truncate`로 최후 방어. 제출값은 별도 `formatDate`(yyyy-MM-dd)라 표시 형식과 무관(계약 영향 없음). `index.css`에 `.react-datepicker-wrapper{display:block;width:100%}` 추가(커스텀 버튼이 칸 전체 폭 차지). **②lg 여백/비중**: 필드 `lg:px-4`→`lg:px-3`(5필드 일괄)·컨테이너 `lg:gap-4`→`lg:gap-2`(≈64px 확보) + 콤마 라벨이 길어지는 목적지·테마에 `lg:flex-[1.3]` 비중(다른 필드 flex-1 유지, mobile flex-col 무영향). lint·build 통과. **5173 라이브**(홈=백엔드 불필요): 캘린더 정상 오픈·범위 선택(12~22) → "08/12 ~ 08/22" 잘림 없이 표시 확인(zoom 대조)·검색바 행 무결·placeholder 정상. ⚠️ **lg 실폭 렌더 한계**: 창을 1180px로 resize해도 스크린샷이 1545px로 고정(자동화 창 폭 락, 보드 기존 기록과 동일) → 1024~1280 크라우딩 완화는 정적 검증으로만 커버(실기기 devtools 권장). **성격**: 별도 Task 아님(진행률 24개 불변). 계약 추적표 무관.
- 2026-08-08 · Planner 패널 반응성 개선 · 사용자 요청 UI/UX 후속. `Planner.tsx` 데스크톱 레이아웃 손질(사용자 지적: 사이드바 디자인·너비). **①패널 높이**: 코스·결과 패널 `h-[560px]` 고정 → `h-[clamp(500px,calc(100vh_-_15rem),780px)]`(뷰포트에 맞춰 늘리되 헤더·요약·여백 ≈15rem 차감, 하한 500·상한 780px). **②사이드바 너비**: `grid-cols-[360px_1fr]` → `grid-cols-[clamp(320px,26%,400px)_1fr]`(320~400px 사이 컨테이너 26% 비례 — 1024~1280 중간 폭에서 360 고정보다 좁아져 결과 패널에 여유). 공유 상수 `PANEL_H`·`PANEL_GRID`로 추출. 모바일 `h-[70vh]`는 이미 뷰포트 기반이라 무변경. lint·build 통과 + **빌드 CSS에 clamp 유틸 2종 정상 생성 확인**(`clamp(500px,calc(100vh - 15rem),780px)`·`clamp(320px,26%,400px)` — Tailwind arbitrary value 파싱·언더스코어→공백 변환 검증). ⚠️ **패널 실렌더 라이브 보류**: 5173 실측 `POST /tour-course` **500**(백엔드 AI 생성 아웃티지 지속) → 게스트 코스 생성 불가로 플래너 패널 진입 못 함, 로그인 경유는 조직 지침(비밀번호 입력 금지)상 불가. CSS-only 저위험이라 build+CSS 생성 확인으로 커버(백엔드 복구 시 실렌더 재확인 권장). **성격**: 별도 Task 아님(진행률 24개 불변). 계약 추적표 무관.
- 2026-08-08 · 헤더 UI/UX 정비 · 남은 Task 전부 백엔드 대기라 사용자 요청으로 전역 헤더(`HeaderLayout`·`ThemeController`) 손질. 사용자 지적(아이콘 배치·간격) 대응. **①죽은 알림 벨 제거**: `<Bell>` 버튼이 `onClick`·기능·`aria` 없이 활성 아이콘처럼 놓인 dead affordance라 제거(우측 아이콘 3→2개로 촘촘함 해소, `lucide-react` Bell import 정리). **②접근성/시맨틱**: 테마 래퍼 `<div role="button">`→role 제거(내부 `<label>`+checkbox가 상호작용·포커스 담당, 중첩 인터랙티브 회피)·checkbox에 상태 가변 `aria-label`(다크/라이트 전환)·swap SVG `aria-hidden`; 계정 트리거 `<div role="button">`에 `aria-label="계정 메뉴"`+`aria-haspopup="menu"`(daisyUI dropdown 포커스 동작 유지 위해 div 구조는 존치). **③크기·간격**: 테마 SVG 24→20px(계정 User 20과 정렬)·`navbar-end` `gap-1 md:gap-2`→`gap-0.5 md:gap-1`. `btn-square`는 base `btn` 없는 기존 관례라 미적용(무효 회피). lint·build 통과. 5174 라이브(자동화창 1440px): 벨 제거·아이콘 2개 정렬 확인, 테마 토글 라이트↔다크 정상(다크 배경·검색바 적응 렌더), 계정 드롭다운(게스트=로그인/회원가입) 정상 오픈. **성격**: 별도 Task 아님(진행률 24개 불변) — 백엔드 대기 중 정비. 계약 추적표 무관.
- 2026-08-08 · 반응형 전면 점검 · 사용자 요청으로 모바일·데스크톱 반응형 전수 점검. **방법**: 반응형 리스크 패턴(`w-[…]`·`min-w`·`overflow-x`·`grid-cols-N`·`whitespace-nowrap`·고정 h/w) 전수 grep → 핵심 화면 정독(Layout·HeaderLayout·FooterLayout·Index 검색바·Planner·CoursePanel/ResultsPanel/BudgetDashboard·Collection·About·AuthScreen). **결론**: 코드베이스가 이미 mobile-first로 견고 — grid는 전부 `grid-cols-1` 모바일 베이스 + `sm:`/`lg:` 확장, Index 검색바 `flex-col rounded-2xl`(모바일 세로 스택)↔`lg:flex-row lg:rounded-full`(데스크톱 알약)+구분선 `hidden lg:block`, Planner 데스크톱 `grid-cols-[360px_1fr]`↔모바일 세그먼트 탭(결과/코스/예산, `h-[70vh]`), Header 버거(`md:hidden`)↔데스크톱 탭(`hidden md:flex`), About/Collection 반응형 grid+`flex-wrap` CTA, 반응형 타이포(`text-xl sm:text-2xl …`). **오버플로 검사**: `/`·`/about` 등 도달 가능 전 페이지에서 `document.documentElement.scrollWidth ≤ clientWidth`(가로 스크롤·삐져나옴 0) JS로 확인. **발견·수정 2건**(index.html): `<html lang="en">`→`lang="ko"`(한국어 앱인데 영어 선언=a11y/SEO 결함), `<title>React</title>`→`경북 CoCo`(Vite 기본 제목이 탭에 노출). viewport meta(`width=device-width, initial-scale=1`)는 이미 정상. **함께 확인**: 직전 커밋의 테마 수정(`bg-base-200` 메인·teal 스피너)이 라이브에서 정상 반영됨. ⚠️ **한계**: MCP automation 창이 1440px에 하드락(resize 호출이 무시됨, `innerWidth`=`screen.availWidth`=1440)이라 실제 모바일 폭 라이브 구동은 불가 → mobile-first 클래스 정적 검증 + 데스크톱 오버플로 스캔으로 커버(실기기/브라우저 devtools 디바이스 모드 스팟체크 권장). 잔여 리스크 1건: `react-datepicker` 캘린더 팝업(`custom-datepicker-calendar` 미스타일)의 초협폭(<360px) 오버플로 가능성 — 실기기 확인 필요. lint·build 통과. 계약 추적표 무관.
- 2026-08-08 · 디자인 테마 가변성 전면 점검 · 남은 Task가 전부 백엔드 대기(⏸)라 사용자 요청으로 라이트/다크 양 테마 정비. **전수 스캔**: 팔레트 고정색(`bg-white`·`text-gray-*`·`slate/zinc/…`) grep → 코드베이스 대부분 이미 시맨틱 토큰화됨(과거 작업 성과)을 확인, 실결함만 정밀 수정. **수정 3파일**: ①`Layout.tsx` `<main>` `bg-[#F8F9FF]`→`bg-base-200`(다크에서 밝은 고정 배경 위에 어두운 `bg-base-100` 카드가 떠 깨지던 **핵심 결함** 해소), ②`index.css :root` `background-color: #F8F9FF`→`var(--color-base-200)`(overscroll·초기 페인트 배경도 테마 가변, daisyUI v5 CSS 변수), ③`Loading.tsx` 스피너 `text-indigo-500`(오프브랜드)→`text-primary`·메시지 `text-slate-500`→`text-base-content/60`. **정당한 예외 유지**: Kakao 브랜드색(`KakoLoginComponent`), 백드롭 `bg-black/40~50`(PoiDrawer·ConfirmDialog·LoginGateModal), 항상 어두운 컬러 배경 위 `text-white`(Index 검색버튼·CourseItem 배지·BudgetDashboard 칩·Marker·AuthScreen teal 히어로), 지도 플레이스홀더 `bg-[#E9F0EF]`(MapView), 평점 별 `fill-amber-400`(Stars). **문서**: `DESIGN.md` §2.3·2.4·3·6.4·6.6이 §6.7 "테마 가변 필수" 규정과 모순되게 고정색(`#F8F9FF`·`bg-white`·`text-gray-*`·인디고)을 권장하던 드리프트를 시맨틱 토큰으로 정정. lint·build 통과. 5173 라이브 검증: `/`(홈 검색바)·`/about`(히어로+핵심기능+이용방법) 라이트·다크 양 테마 렌더 확인(다크에서 카드가 페이지 위로 또렷이 뜨고 텍스트 대비·teal 강조 가독성 유지). 계약 추적표 무관.
- 2026-08-08 · A2·A4 완료 · 부록 A 정리 마무리(부록 A 4/4). **A2(About)**: `pages/About.tsx` 5줄 스텁(`<>About</>`)→서비스 소개 페이지 — 히어로(배지+헤드라인+부제+CTA 2개), 핵심 기능 3카드(F1 인원 큐레이션·F2 예산 시뮬레이션·F3 통합 공유, PRD §4), 이용 방법 4단계(검색→AI 생성→예산→저장/공유), 프라이머리 CTA 배너. 시맨틱 토큰만(`bg-base-100/200`·`text-base-content/70`·`bg-primary-50`·`text-primary`·`bg-primary`+`text-primary-50`), `lucide-react` 아이콘, 반응형 그리드(`grid-cols-1 sm:grid-cols-3`/`lg:grid-cols-4`). `/about`이 헤더/푸터 어디에도 링크가 없던 문제 → `FooterLayout`에 "서비스 소개" NavLink 추가 + `ACME Industries Ltd` placeholder 저작권을 `경북 CoCo`로 정정(푸터도 스텁이었음). **A4(RootLayout)**: 라우터 미연결 상태였고 CLAUDE.md가 삭제를 금지(의도된 placeholder) → **존치 결정**. 실제 셸은 `Layout`이며 `RootLayout`은 향후 `Layout` 상위 공통 셸(전역 프로바이더·배너) 자리로 남긴 뼈대임을 파일 상단 주석으로 명시(삭제 금지 근거 포함). lint·build 통과. 5173(포트 점유로 5175) 라이브: `/about` 라이트·다크 양 테마 렌더 정상(히어로 `bg-base-200` 다크 적응·배지/`text-primary` 가독성 유지), 푸터 링크·저작권 확인.
- 2026-08-08 · P1 완료 · GBC019 POI 좋아요(찜) 토글. **API**: `api/poi.ts` `togglePoiLike(contentId)`(`POST /poi/{id}/like`, 스펙 `완료`, 봉투 벗김 `{liked,likes}`). **스토어**: `stores/poiLikeStore`(poiId→liked/likes 공유 — 카드·드로어가 같은 POI 찜 상태 공유), `stores/loginGateStore`(로그인 게이트를 전역 스토어로 승격 → 깊은 트리의 찜 버튼도 게이트 오픈 가능·프롭 드릴 회피). **훅**: `hooks/usePoiLike`(비로그인→`openGate('찜')`, 로그인→낙관적 `setLiked`→실 contentId(양수 정수)면 서버 호출→성공 시 서버값 확정/실패 시 롤백+토스트; 목 슬러그 id는 서버 호출 건너뛰고 로컬만 반영). **UI**: `components/planner/LikeButton`(하트, 사이즈는 호출부 지정) → `POICard`(stacked=이미지 우상단·horizontal=상단 행 우측)·`PoiDrawer`(이미지 우하단, CatBadge 반대편). **게이트 이관**: `Planner`의 로컬 `gate` state 제거→`loginGateStore` 구독(저장 게이트도 `openGate('저장')`로 이관, 모달은 Planner 단일 렌더 유지). **성격**: UI/함수/낙관 메커니즘 완성. lint·build 통과. ⚠️ 실동작(서버 반영·재조회 유지) 라이브 검증은 코스 생성이 필요한데 백엔드 AI 생성 500 아웃티지로 플래너 진입 불가 → 보류(P1 사양이 실동작 검증을 P2/P3+백엔드 복구로 위임). GBC019는 스펙 `완료`라 계약 추적표 무관.
- 2026-08-08 · P0 완료 · POI 목→API 교체 준비(섬 P 착수점). **목표**: UI/타입은 목 그대로 두고 데이터 소스만 훅으로 캡슐화해 P2/P3에서 훅 내부만 API로 갈아끼우면 소비처 무수정이 되도록 함. **신설**: `hooks/usePoiList.ts`(검색조건→POI 목록, 반환을 비동기 3-상태 `{pois,ready,loading,error,reload}`로 미리 맞춰 P2 `getPois`+`useAsync` 교체 대비; 현재는 목 동기 필터·`filterMockPois`를 ResultsPanel에서 이관)·`hooks/usePoi.ts`(단일 POI, 현재 `plannerStore.resolvePoi` 위임, P3 `getPoi` 병합 대비). **전환**: `ResultsPanel`이 `@/mocks/planner`(`POIS`/`REGIONS`/`paxBucket`) 직접 import·로컬 `filterPois` 제거→`usePoiList` 경유(`pois`/`ready`), `regionName`은 sigunguStore 유지. `PoiDrawer`가 `resolvePoi` 직접 호출→`usePoi(drawer.poiId)`(훅이라 최상단 호출·early return 앞으로 이동, rules-of-hooks 준수). **매핑**: `api/poi.ts`에 역방향 `CAT_BY_CONTENT_TYPE`(12/14/32/39→cat) 추가 + `CONTENT_TYPE_BY_CAT` 키 타입을 `Record<PoiCat,…>`로 강화. **성격**: 순수 리팩터(로직·목 데이터·화면 동일, 시각 변화 없음). lint·build 통과. ⚠️ 실데이터 렌더는 P2/P3(백엔드 `보류`) 이후. 계약 추적표 무관(POI 계약 미확정 유지).
- 2026-08-08 · A3 완료 · 문서 드리프트 최신화. **드리프트 원인**: `PRD_FRONT.md`·`FEATURES_FRONT.md`는 기준일 2026-06-06 상위 기획 문서로 "셸 수준·대부분 미구현" 상태를 서술 → 실제 코드는 S1~S7(코스 도메인 API 연동)까지 진척. **정정**: ①`CLAUDE.md` 디렉터리 트리의 `api/`·`hooks/` "(현재 비어 있음)" → 실제 모듈 목록으로 교체 + 핵심 파일에 `api/client.ts`(인터셉터)·`hooks/useAsync.ts` 추가(표준 패턴 노출). ②`PRD_FRONT.md` §5 구현 매핑 표를 실제 파일/라우트/스토어/훅·완료(GBC010~016)·백엔드 대기(섬 P/M) 반영으로 재작성, 상단·§5에 "구현 현황 정본=보드" 포인터 추가, `/home` 제거 완료 반영. ③`FEATURES_FRONT.md` 상단에 인라인 구현 표기가 기준일 스냅샷임을 경고하고 보드로 위임(40여 블록 개별 정정 대신 정본 위임 — 저효율 회피). **소스 무변경(문서만)** → lint·build 영향 없음(A1 통과 상태 유효). 계약 추적표 무관.
- 2026-08-08 · A1 완료 · `/home` 더미 라우트 제거. `router.tsx`에서 `Home` lazy import(구 :14)와 `home` 라우트 블록(구 :32~39) 삭제 + `src/pages/Home.tsx`(반응형 Layout 테스트용 더미 페이지) 삭제. `/home`로의 `Link`/`navigate` 없음 확인(코드베이스 grep — 다른 `Home` 참조는 `NotFoundLayout`/`BudgetDashboard`의 lucide 아이콘으로 무관). lint·build 통과(빌드 산출물에서 Home 청크 사라짐). 파괴적 영향 없음(더미 페이지, 외부 진입점 아님).
- 2026-08-15 · 보류 라이브 검증 마무리(비로그인 범위) · 백엔드 AI 생성(`POST /tour-course`)이 복구돼(경주 1박2일 실생성 성공, 소요 ≈20~30초) 그간 "백엔드 아웃티지로 보류"로 남아 있던 항목을 실제로 닫았다. **①S7 공유 해피패스 ✅**: 플래너 공유 버튼 → 카카오 JS SDK(`kakao.min.js` 200) 로드·`init` 성공 → `Share.sendDefault`가 **실제 공유 피커(`https://sharer.kakao.com/picker/link`)를 팝업으로 오픈**(`window.open` 후킹으로 확증). 클립보드 폴백은 발동하지 않음 = 카카오 경로 자체가 살아 있음(도메인 등록 완료 상태). **②S7 공개뷰 해피패스 ✅**: `/share/37`(직전에 만든 실 코스) 비로그인 열람 → 배너("공유받은 여행 코스예요")·요약(경상북도 · 2026.08.20~08.21 · 1박 2일 · 1명 · 자동차 · 문화 배지)·**Day 1 6곳 / Day 2 5곳 실장소명**(경주 원성왕릉 09:00 관광지, 경주 블리스커피 12:00 음식점, 한국대중음악박물관 13:30 문화시설, 경주 춘추관 21:00 숙박, Day2 경주 포석정지·한우리가든·곤달비캠핑장·고도 커피·경주애견펜션 노비오스)·CTA 렌더. 로그인 리다이렉트 없음(가드 밖 재확인), 콘솔 에러 0. 제목이 'AI 추천 코스'로 뜨는 건 정상(백엔드 저장 title 이 빈 값 — 제목 지정은 S6 편집 이후). **③P1 비로그인 게이트 ✅**: 실 POI 카드 하트 클릭 → "찜하려면 로그인" 모달, 서버 호출 없이 차단. **미검증으로 남는 것(로그인 필요)**: S2 저장(`PATCH .../assign`) 게스트→로그인 복귀 자동 저장, S6 제목 수정 영속, P1 좋아요 서버 반영. **조직 지침상 비밀번호 입력이 금지**돼 에이전트가 로그인할 수 없다 → 사용자가 브라우저에서 직접 로그인한 세션에서만 확인 가능. 소스 무변경(검증만) → lint·build 영향 없음.
- 2026-08-15 · 지도 카카오맵 실연동 · 사용자 요청("현재 지도쪽 카카오맵으로 변경"). 남은 6개 Task(S8·M1~M4·P3)가 전부 백엔드 미구현이라(v0.5.2 소스 실측: `PATCH /tour-course/{id}`·`GET /poi/{contentId}` 없음, `LoginResponseDto`는 `accessToken` 단일·JWT subject=email이라 **userId 여전히 미제공**) 착수 가능한 Task가 없던 상황. **구조**: `MapView`는 스토어 구독 + 마커 모델 계산만 하고 렌더는 자식에 위임 — `parts/KakaoMap`(실지도) ↔ `parts/PlaceholderMap`(기존 % 좌표 지도, 폴백). 공유 모델 `mapModel.ts`(`MapMarker`·`hasLatLng`), 공유 스타일 `parts/markerStyle.ts`(`markerBadgeClass`·`MAP_PIN_SVG` — React 마커와 DOM 오버레이가 같은 배지를 쓰도록 컴포넌트 파일 밖에 둠, `only-export-components` 회피). **SDK**: `utils/kakaoMap.ts`가 `dapi.kakao.com/v2/maps/sdk.js?autoload=false`를 1회 주입 후 `kakao.maps.load()`. 공유용 `utils/kakaoShare.ts`(`window.Kakao`, kakao.min.js)와 **다른 전역**(`window.kakao.maps`, 소문자)이라 공존한다. 키 없음·로드 실패·`maps` 미노출을 전부 `null`로 삼켜 상위가 폴백(지도 영역이 비는 일 없음). **좌표**: `Poi.lat/lng`(실경위도) 신설 — `usePoiList`가 GBC017 `mapy/mapx`를 `validCoord` 통과 시 그대로 싣는다(기존 `x/y` %는 폴백 지도 전용으로 존치). 코스 장소는 `mergePoi`가 카탈로그를 깔아주므로 좌표를 물려받고, 카탈로그에 없으면 좌표 없음 → 지도에서 건너뛴다. **마커**: 종전엔 결과 목록만 찍고 코스 경로선은 마커 없이 그려졌다 → **결과 ∪ 활성 Day 코스** union 으로 교정(코스는 순번 배지·z-index 우선). `CustomOverlay`로 앱 카테고리 색 배지를 그대로 쓰고 id 기준 diff(생성/제거/재도색), 시야는 좌표 집합(`boundsKey`)이 바뀔 때만 `setBounds`(드로어 열기로 재이동하지 않게), 경로선은 `Polyline`(색은 `--color-primary` 런타임 조회, SDK가 CSS 변수를 못 받음). 컨트롤 3종을 실제 동작으로 연결(줌 ±1·현위치 geolocation, 실패 시 toast). **드로어**: "카카오맵에서 보기" 데모 토스트 → `<a>` 실링크(`map.kakao.com/link/map/{name},{lat},{lng}`, 좌표 없으면 `link/search/{name}`) 새 탭+`noopener`. lint·build 통과. **5173 라이브 1차**(실백엔드, 실제 AI 생성 경주 1박2일): `GET dapi.kakao.com/v2/maps/sdk.js` **403** `{"errorType":"NotAuthorizedError","message":"App(OAuth2Spring) disabled OPEN_MAP_AND_LOCAL service."}` — 카카오 콘솔에서 앱의 **지도/로컬 서비스가 비활성**이었다(코드 문제 아님). 설계대로 폴백이 동작해 "지도 미리보기 (카카오맵 연결 실패)" 배지와 함께 종전 지도가 떴고, 그 상태로 마커 **65개**(코스 6곳 순번 1~6 + 결과 59곳 = union 검증)·드로어 링크 `.../link/map/경주 양동마을 송첨종택,36.0038143817,129.2536260037`(실좌표 전달 검증)를 확인. **⇒ 사용자가 콘솔에서 지도/로컬 서비스 활성화**(SDK 200 재확인) 후 **2차 라이브에서 실지도 검증 완료**: ✓카카오 실타일 렌더(경주·포항·영천 지명, 도로), ✓카테고리 색 CustomOverlay 마커 + 코스 순번 배지(1~7), ✓`Polyline` 점선 경로선(브랜드 teal), ✓줌 + 버튼 동작(레벨 축소·마커 재배치), ✓마커 클릭 → 드로어(월성원자력홍보관 실운영시간·'Day 1에 추가됨'), ✓카테고리 필터(음식점) 시 마커 diff 갱신되고 코스 마커는 유지, ✓다크 테마 전환 정상, ✓**콘솔 에러 0**. **성격**: 별도 Task 아님(진행률 24개 불변) — P3(GBC018)의 "카카오맵 실연동" 조각을 백엔드와 무관한 범위에서 선반영. 계약 추적표 무관.
- 2026-08-15 · 코스 장소 실데이터 반영 · **① `contentName` 선반영**(백엔드가 생성 응답에 추가하기로 합의) — `CoursePlace.contentName?`, 이름 폴백을 `placeName || contentName || '장소 #id'` 순으로. 백엔드 배포 즉시 카탈로그 없이도 실명이 뜬다. **② 함께 발견·수정: 이미 오고 있던 실데이터를 FE가 버리고 있었다.** 백엔드 `PlaceInfo`(생성·상세 공통)는 `thumbnailImg`·`operatingHours`·`cost`·`durationMinutes`를 채워 보내는데 FE `CoursePlace` 타입이 `seq/time/type/contentId/placeName`만 선언해 무시 중이었음 → **예산이 항상 ₩0**, 코스 카드에 썸네일 없음, 운영시간 '정보 준비 중'의 원인. 네 필드를 타입에 추가하고 `synthesizePoi`가 그대로 싣도록 함. **③ `Poi.hours` 의미 분리**: 기존엔 코스 장소의 `hours`에 **방문 시각**을 넣어 드로어의 '운영시간' 라벨과 어긋났다 → `visitTime`(일정상 방문 시각) 필드 신설, `hours`는 운영시간 전용(빈 문자열=정보 없음). `CourseItem`은 `visitTime || hours`, 드로어는 `hours || '정보 준비 중'`. **④ 병합 규칙 정리**: `mergePoi`가 카탈로그(좌표·분류·이름)를 깔고 코스 값이 **있을 때만** 덮어쓰도록(가격·운영시간·썸네일·방문시각), 이름은 placeholder 상수 비교로 판정. **⑤ 결과 카드도 해석기 경유** — 같은 장소가 카드는 '가격 미정', 드로어는 '₩5,000'으로 어긋나던 불일치 제거. `httpsUrl()`을 `utils/format`으로 공용화. lint·build 통과. **5173 라이브**(실백엔드, 실제 AI 생성 안동 코스): 코스 패널이 `광흥사(안동) ₩5,000`·`옥야식당 ₩12,000` 실명+실썸네일+방문시각으로, **예산 총액 ₩95,000**(식비 48,000·입장관람 30,000·교통 17,000)으로 계산됨(이전엔 교통 외 전부 ₩0), 드로어 운영시간 `09:00~18:00`, 카드/드로어 가격 일치 확인. (`cost`가 null인 숙박·일부 장소는 '가격 미정'으로 정상 표기.)
- 2026-08-15 · P2 후속 수정 · **코스 패널에 `장소 #2597551` placeholder 가 그대로 남던 결함**(사용자 제보, 안동 코스 스크린샷). **원인**: P2에서 넣은 카탈로그 병합 자체는 정상이었으나(해당 contentId 4건 모두 안동 큐레이션 목록에 존재 확인), 컴포넌트가 `usePlannerStore((s) => s.resolvePoi)`로 **함수 참조를 구독**해 `poiCatalog` 가 채워져도 셀렉터 값이 안 바뀌어 **리렌더가 일어나지 않았다**(이름이 stale). 직전 세션에서 "승격 확인"이라고 기록한 건 코스에 POI를 추가해 발생한 **별개 리렌더 덕분**이었다 — 잘못된 근거로 통과 판정한 것. **수정**: 병합 로직을 `mergePoi()` 순수 함수로 추출하고 `hooks/usePoiResolver`(= `apiPois`·`poiCatalog` 두 레지스트리를 구독하는 해석기 훅) 신설 → React 소비처 5곳(`CoursePanel`·`BudgetDashboard`·`MapView`·`PlannerDndProvider`·`usePoi`)을 훅으로 전환. 스토어의 `resolvePoi` 는 비React 호출부(`addPoi` 토스트)용으로 남기고 "컴포넌트에서 구독 금지" 주석 명시. **함께 수정**: 병합 시 `cat`을 코스 응답에서 덮어써 **배지(관광지)와 썸네일 라벨(문화시설)이 어긋나던 불일치** → 분류도 카탈로그(`contentTypeId` 직결) 기준으로 통일. lint·build 통과. **5173 라이브 재검증**(실백엔드, 실제 AI 생성 코스): 안동 1박2일 생성 → 코스 패널이 `안동호반자연휴양림`·`396커피컴퍼니`·`국립경국대학교 박물관` 등 **실명**으로 렌더, 배지·라벨 일치 확인. (참고: `POST /tour-course`는 여전히 간헐 실패 — "존재하지 않는 장소 ID" 400·500이 섞여 나오고 재시도하면 성공. 백엔드 이슈로 기존 기록과 동일.)
- 2026-08-15 · P2 완료 · GBC017 큐레이션 POI 목록(섬 P 3/4). **착수 근거**: 백엔드 `../back` v0.5.1(`9597ab7`)이 `GET /api/v1/poi`를 구현 → 보드의 ⏸ 해제. (같이 확인: `GET /poi/{contentId}`·`PATCH /tour-course/{id}`·`userId` 제공은 여전히 없음 → P3·S8·섬M은 ⏸ 유지.) **API**: `api/poi.ts` 잠정 타입 폐기하고 백엔드 실측 1:1로 교체 — `PoiSummary{contentId,contentTypeId,title,mapx,mapy,thumbnail,avgPrice}`·`PoiListResponse{available,items}`·`getPois(params)`. `ContentTypeId`를 TourAPI 8종(12/14/15/25/28/32/38/39)으로 확장(경주 실측에 8종 전부 등장)하고 `CAT_BY_CONTENT_TYPE`+`catOfContentType()`로 UI 4종(`PoiCat`)에 접음(레포츠·쇼핑·여행코스→sight, 축제→culture — 백엔드 PlaceType 규칙과 동일). **훅**: `usePoiList`가 목 필터 대신 `useAsync`+`getPois`. 백엔드 `sigunguCode`가 **단수·필수**라 선택 시군구마다 **병렬 호출 후 합침**(사용자 선택 지역 전부 노출, 코스 생성의 복수 `sigunguCodes`와 일관)·contentId dedup·모듈 레벨 **in-flight dedup**(Planner가 데스크톱/모바일 ResultsPanel을 동시 마운트 + StrictMode 이중 호출 → 동일 요청 2~4회 발사, `GET /poi`는 TourAPI 라이브라 동시 호출 시 503 실측). 응답→`Poi` 매핑(실 이름·썸네일·좌표, desc=시군구명). **좌표**: 실경위도를 플레이스홀더 지도 %로 bbox 정규화(10~90%), **경주 데이터에 `mapx/mapy=0`인 항목 1건이 섞여 bbox를 폭발시켜 전 마커가 한 점에 뭉치던 결함**을 한국 경위도 범위 검사로 방어(라이브에서 발견·수정). **스토어**: `poiCatalog`+`registerPois` 신설(`apiPois`와 분리 — 코스 로드가 `apiPois`를 통째 교체하므로 같이 두면 담은 POI가 날아감), `resolvePoi`가 코스 장소+카탈로그 **병합**(카탈로그 실 이름·좌표·썸네일 + 코스의 방문시각·유형) → 생성 응답의 `장소 #id` placeholder가 결과 목록 조회 시 실명·실사진으로 승격됨(라이브 확인). 목 fallback(`poiById`) 제거 — `MapView`·`budget.ts`도 스토어 해석기 사용. **UI**: `ResultsPanel` 4상태(지역 미선택·최초 로딩 스켈레톤·`ErrorState`+`getApiErrorMessage`+재시도·`available:false` 데이터 준비 중·카테고리 결과 없음) + 60개씩 **더 보기** 페이징(경주 단독 324곳·2지역 411곳이라 전량 렌더는 과함, 무언의 절단 대신 명시 버튼) + 지역 라벨 복수 표기. 실 썸네일 렌더(`ImgPlaceholder`가 `src` 있으면 `<img loading=lazy>` + 실패 시 텍스트 플레이스홀더 폴백, `http://`→`https://` 승격으로 https 배포 mixed-content 방어) → POICard·PoiDrawer. `avgPrice`가 항상 null이라 가격 0을 '무료'로 오표기하던 문제를 `priceNote`('가격 미정') 표시로 교정(목의 진짜 무료 항목은 priceNote='무료'라 그대로). **계약 차이(문서와 실제)**: 가이드 §3의 `theme` 파라미터가 **백엔드에 없음** → 테마 필터/정렬 불가(P0의 목 테마 우선정렬 제거). `peopleCount`는 필수지만 백엔드가 검증만 하고 필터엔 미사용. lint·build 통과. **5173 라이브 검증**(실백엔드 8080): 경주(130)+포항남구(111) 병렬 200 → 411곳 실장소명·실사진, 카테고리 칩(음식점 129곳), 더 보기 60→120, 지도 마커 분산(x 17~89%·y 14~90%), 드로어 실데이터, 결과→코스 추가 시 코스·예산 패널 정상 해석, 다크테마 정상, 콘솔 에러 0, dedup 후 지역당 요청 1건. ⚠️ **검증 우회**: `POST /tour-course`(AI 생성)가 여전히 500이라 플래너 진입용 코스 생성만 브라우저에서 XHR 스텁으로 대체(소스 무변경, `GET /poi`는 전부 실백엔드). ⚠️ 미검증: `available:false` 빈 상태(경북 23개 시군구 전부 데이터 있음 — 코드 경로만 커버). ⚠️ **백엔드 CORS는 `FRONT_ORIGIN`(5173)만 허용** — dev 서버가 5174로 뜨면 모든 API가 막힌다(이번에 겪음, 5173 확보 필요).
- 2026-08-08 · S2 완료 · GBC016 코스 소유권 이전(="저장"). `api/tourCourse.ts` `assignCourse(courseId)`(`PATCH /tour-course/{id}/assign`) + `hooks/useCourseSave.ts`(저장 흐름 캡슐화) 신설. 흐름: 게스트 저장 클릭→`courseId`를 `sessionStorage`에 stash + 로그인 게이트→로그인 성공(클라 내비게이션, zustand 싱글턴이라 스토어 유지)→복귀 시 effect가 대기 저장 감지→`assignCourse` 자동 실행. `Planner.onSave` toast-only 목업 제거, 저장 버튼 진행(spinner)/완료("저장됨") 반영(요약+`BudgetDashboard`). 중복 저장 가드: 백엔드가 이미 소유자 있는 코스에 403 반환→`saved`/`saving` 가드 + `runAssign` 시작 시 `clearPending()`(StrictMode 이중발사 차단). 대기값이 현재 `courseId`와 다르면 폐기(오래된 값). 백엔드 실측 대조(`TourCourseController`·`TourCourseServiceImpl`) 완료. lint·build 통과. ⚠️ 실백엔드 E2E는 dev+백엔드 기동 시 확인.
- 2026-08-15 · S8 완료 · GBC020 코스 수정 영속화(메인 스파인 8/8). **착수 근거**: 백엔드가 개발 중인 `PATCH /api/v1/tour-course/{courseId}` 상세 명세를 사용자에게서 수령 — 요청 바디 `{schedule:[{date, places:[{seq,time,type,contentId,contentName,durationMinutes,thumbnailImg,operatingHours,cost}]}]}` **전체 교체**(diff 아님), 장소명 키는 조회 응답의 `placeName` 이 아니라 **`contentName`**, 응답은 공통 봉투(`data`="완료 데이터"라 형태 미확정 → 값 미사용). **핵심 문제**: 편집 정본인 UI 코스(`course.days` = poiId 배열)에는 서버가 요구하는 날짜·방문시각·PlaceType·체류시간·썸네일·운영시간이 없다 → `plannerStore.baseSchedule`(생성/상세 응답 원본)을 보관해 복원 근거로 삼고, `utils/coursePayload.buildSchedulePayload`(신설·순수함수)가 ①원본 메타(contentId 로 조회 — 다른 Day 로 옮겨도 따라감) ②카탈로그 Poi(새로 담은 장소의 이름·분류·사진) ③예산 override 를 합쳐 조립. **시각 규칙**: 그 날 원본 시각들을 오름차순으로 모아 순번대로 배분 → 무편집이면 원본과 바이트 동일, 재정렬해도 1번이 늘 가장 이른 시각, 슬롯이 모자라면 `직전 시각+체류시간+이동여유(60분)`로 이어붙임. **비용**: 화면 override 는 총액이라 `defaultCost` 역함수로 1인 기준 `cost` 환산(숙박은 1박 객실 총액이라 그대로). **제외**: 실 contentId 가 없는 목 슬러그 id 는 전송 불가라 건너뛰고 개수를 toast 로 고지. **트리거**: debounce 자동저장 대신 명시적 버튼 — 저장 버튼 하나가 미소유(게스트·미저장)면 assign(GBC016), 소유면 변경 저장(GBC020)을 수행하고 `dirty` 없으면 '저장됨'으로 비활성. 게스트가 편집까지 한 코스는 assign 성공 직후 `onAssigned` 로 편집분을 이어서 flush(참조 고정 콜백, 변경 없으면 no-op). `dirty` 는 제자리 드롭(from===to)·없는 장소 제거·동일 금액 입력 같은 no-op 에서는 세우지 않도록 스토어 액션을 정리했다. lint·build 통과 + jiti 로 페이로드 조립 단위 검증(무편집 동일성·재정렬 시각 배분·새 장소 이어붙임·목 id skip·Day 신설 시 `startDate+n` 날짜 생성). ⚠️ **라이브 E2E 보류** — 백엔드 `TourCourseController`(v0.5.2, `0ecff26`)에 아직 해당 `@PatchMapping` 이 없어 호출 대상이 없다. 배포되면 편집→변경 저장→재진입 유지로 DoD 최종 확인 필요(로그인 필요 구간이라 조직 지침상 에이전트 검증 불가 가능성 있음).
- 2026-08-15 · S8 라이브 E2E 검증 · 실백엔드(`localhost:8080`)+실세션으로 코스 39("경주 1박2일 문화기행") 대상 전 구간 확인. **정정**: 직전 로그의 "백엔드 미구현" 판단은 틀렸다 — 인증 없이 찌른 `PATCH /tour-course/1` 이 401 이 아니라 **500** 을 반환해 미매핑으로 오판했으나, 실제로는 `TourCourseController.updateCourse` + `TourCourseServiceImpl.updateCourse` 가 구현돼 있었다(상태코드로 매핑 존재를 추정하지 말 것). **검증 절차**: ①소유 코스 진입 시 저장 버튼 '저장됨'(비활성) ②X 버튼으로 한국대중음악박물관 제거 → 즉시 '변경 저장'(활성) 전환 ③선덕여왕릉 비용 5000→7000 인라인 편집 ④dnd-kit 실경로로 블리스커피↔선덕여왕릉 재정렬(합성 드래그는 활성화 임계치를 못 넘어 pointerdown→점진 pointermove→pointerup 시퀀스로 구동) ⑤'변경 저장' → `PATCH /api/v1/tour-course/39` **200 "코스 수정 완료했습니다."**, XHR 캡처로 바디 실측: Day1 4곳 seq 1~4·시각 09:00/12:00/14:00/17:00(원본 슬롯 순번 배분)·type/dur/hours/img 원본 복원, Day2 는 미편집이라 원본과 동일 ⑥재진입(`/collection`→`/planner/39`) 시 구성·순서·시각·체류시간 **모두 유지**, dirty=false, 콘솔 에러 0. **발견 1(계약)**: 백엔드 `TourCourseUpdateRequestDto` 는 `seq/time/type/contentId/durationMinutes` 만 저장하고 `contentName·thumbnailImg·operatingHours·cost` 는 "조회 전용 표시 필드"라 의도적으로 무시(주석 실측) → **사용자 비용 편집이 영속되지 않는다**(7000 전송 → 재조회 5000). 예산 편집 영속이 필요하면 백엔드에 `cost` 저장 요청 필요. **발견 2(결함·수정)**: `validateUpdateRequest`/`@NotEmpty` 상 **Day 가 비면 400**("일정에 최소 한 개의 장소가 필요합니다")인데 어느 Day 인지 알려주지 않는다 → `useCourseUpdate` 에 전송 전 가드 추가(빈 Day 지목 toast, 요청 미발사). 라이브 확인: Day2 를 비우고 저장 시도 → toast "Day 2에 장소를 최소 한 곳은 넣어 주세요", PATCH 0건. **부수효과**: 검증 과정에서 코스 39 의 Day1 이 실제로 변경됨(한국대중음악박물관 제거 + 블리스커피/선덕여왕릉 순서 교체).
- 2026-08-22 · 그룹 F 등록 · 사용자 피드백(플래너 4항목 + 컬렉션 1항목)을 F1~F6 Task 로 분해해 보드·`FE_개발순서.md` §그룹 F 에 등록. 구현은 미착수(전부 `☐`). "내 코스 클릭 → 상세 조회"는 S4 로 이미 완료 확인되어 제외. F5(컬렉션 진입 시 지도 미표시)는 원인까지 확정해 기록: `loadDetail` 의 `search.dests=[]` + `ResultsPanel` 지역 미선택 early return. F2(`transport`·교통비 영속)와 F1(`durationMinutes` 영속)은 GBC020 바디가 `{schedule}` 뿐이라 백엔드 확인이 필요한 항목으로 표기.
- 2026-08-22 · F1 `◐` · 코스 시각·체류시간 편집 FE 구현. `PlaceTimeEdit` 타입 + `plannerStore.placeTimes`/`setPlaceTime`/`setPlaceDuration`/`resetPlaceTime`(no-op 은 dirty 미세움, 코스 로드 시 초기화) + `CourseItem` 시간 칩 인라인 편집(time input·분 입력·완료/되돌리기·Escape·수정됨 배지) + `coursePayload`(지정 시각·체류시간 최우선, `resolveDurationMinutes`/`placeTypeOfCat`/`findBasePlace` export) + `useCourseUpdate` 가 `placeTimes` 전달. 같은 Day 시각 역전은 toast 안내만(자동 정렬 없음). 카드 내부 컨트롤 클릭 → 드로어 동시 오픈 버그 수정. lint·build 통과 + 페이로드 8케이스 node 검증. 라이브 E2E 는 백엔드 미기동·로그인 제약으로 보류(계약 추적표 #5 에 `durationMinutes` 영속 확인 요청 등록).
- 2026-08-22 · F5 `◐` · 지도 코스 전용 표시 + 컬렉션 진입 지도 미표시 수정. `ResultsPanel` 지도 모드를 지역 미선택 early return 앞으로 올리고 `내 코스만 보기` 토글 추가(지역 미선택 시 기본 on, 켜면 카테고리 칩 숨김). 좌표 복구 = `utils/kakaoGeocode`(카카오 로컬 키워드 검색, 경북 bbox 밖 배제, 이름별 1회 캐시, 6초 타임아웃) + `hooks/useCourseCoords` + `plannerStore.placeCoords`/`setPlaceCoords` + `mergePoi(…, coords)`(좌표 없는 장소에만 얹음, 카탈로그 실좌표 우선) + `usePoiResolver` 가 `placeCoords` 구독. 폴백 지도는 `mapModel.projectToPlaceholder` 로 실좌표 bbox 를 10~90% 로 재투영(코스 장소가 전부 중앙에 겹치던 문제). lint·build 통과 + dev 서버에서 상세 응답 주입으로 컬렉션 진입 재현 검증(지도 즉시 렌더·토글·마커 분산·경로선·리스트 모드 회귀 없음·콘솔 에러 0). 지오코딩 실동작은 카카오 SDK 503(외부 장애, `libraries=services` 무관 확인)으로 미검증 → 계약 추적표 #8 등록.
- 2026-08-22 · F2 완료(FE) · 이동수단 상태화 + 예산 교통비 편집. `plannerStore.transport`/`transportOverride` 신설(생성·상세 주입) + `computeBudget` 을 옵션 객체로 정리하고 수단별 계수(`TRANSPORT_RATE`)로 교통비 산출 + `BudgetDashboard` 교통 행에 수단 select·금액 인라인 입력·되돌리기·수단 기준 안내문. **백엔드 확인 결론(소스 실측): `transport` 수정 경로 없음 + 교통비는 0.5.9 에서 FE 전담으로 이전 → 영속 불가** 이므로 `dirty` 를 세우지 않고 UI 에 미저장 사실을 명시(계약 추적표 #6 ⛔ 불가 확정). lint·build 통과, 계산 30케이스 node 검증, 라이브 확인(수단 전환 즉시 재계산·5인=2대·커스텀 금액·되돌리기·다크·좁은 폭 줄바꿈·콘솔 에러 0).
- 2026-08-22 · 백엔드 실측 정정 3건 · ①GBC020 은 `durationMinutes`·`cost` 를 **저장한다**(0.5.9 에서 `cost` 컬럼 신설, 조회 시 저장값 우선) → S8·F1 의 "서버가 비용/체류시간을 저장하지 않는다" 메모는 그 시점 기준이고 현재는 유효하지 않다(추적표 #5 ☑). ②`GET /poi/{contentId}` 구현 완료 → **P3 블로커 해제**(`⏸`→`☐`). ③POI 좋아요 저장 유실이 백엔드 0.5.11 에서 수정됨 → F4 는 그 배포 기준으로 재현부터 확인(추적표 #7 ◐).
- 2026-08-22 · F3 완료 · 이동수단별 코스 간 이동시간 표시. `utils/travelTime.ts`(haversine × 우회 1.3 × 수단별 실효 속도, 5분 단위·하한 5분, 좌표 없으면 null) + `parts/TravelConnector`(항목 사이 `약 N분 이동` + 수단 아이콘) + `CoursePanel`(항목 해석 1회 + 구간별 useMemo 계산 + 추정 안내 한 줄). 코스 응답에 좌표가 없어 지도를 열지 않으면 계산이 안 되던 문제는 `CoursePanel` 도 `useCourseCoords` 를 마운트해 해결(같은 캐시라 중복 조회 없음). lint·build 통과, 28케이스 node 검증, 라이브 확인(수단 전환 즉시 갱신·실제 드래그 재정렬 후 재계산·좌표 결측 구간 표기 없음·다크·콘솔 에러 0).
- 2026-08-22 · F4 완료 · 좋아요 토글 원인 확정(백엔드는 진짜 토글, 증상 원인은 백엔드 INSERT 유실 버그 `6e4e682` 로 기수정 / 남은 공백은 찜 상태 **조회 경로 부재** → 추적표 #7 확정·#9 신설) + FE 결함 2건 수정(세션 전환 시 `poiLikeStore` 리셋, 서버 응답이 기대와 다를 때 toast 안내). lint·build 통과 + node 13케이스. 브라우저 실측(로그인 필요)은 사용자 대행 대기.
- 2026-08-22 · P3 FE 완료(`◐`) · GBC018 상세 연동 — `getPoi` + `usePoi` 실조회(캐시·dedup·stale 차단) + `utils/poiDetail`(상세는 빈 칸만 채움) + `utils/coords` 공유 + 드로어 소개·주소·전화·홈페이지·부가정보 렌더. lint·build 통과 + node 39케이스. 백엔드(:8080) 미기동으로 라이브 왕복은 대기(코스 생성 API 미호출).
- 2026-08-22 · F6 검토 완료 + (a)안 구현(`◐`) · 드래그 3안 기각 근거 기록(마커=React 밖 DOM / 모바일은 지도·코스 동시 표시 불가 + 지도 쪽 DndProvider 없음 / pan 포인터 경쟁 / 좌표 없는 장소는 마커 없음) → 마커 +/− 토글로 코스 담기·빼기(`markerStyle` 토글 + `KakaoMap`·`Marker` 동일 규약 + `MapView.toggleInCourse`). lint·build 통과 + 빌드 CSS 유틸 확인 + node 14케이스. 라이브 클릭·모바일 터치는 백엔드 미기동으로 대기.
- 2026-08-22 · 백엔드 계약 요청서 작성 · 보드에 `☐` Task 가 없어 블로커 해소로 전환. `docs/BE_계약_요청서.md` 신설 — **R1** `userId` 획득 경로(#4, 섬 M 블로커) · **R2** 코스 장소 `mapx`/`mapy`(#8) · **R3** 찜 상태 조회(#9) + 부록 B 로 #6(`transport` 영속) 판단 요청. 백엔드 `aa2a555`(0.5.12) 재실측으로 3건 미해결 확인, R2 는 `PoiSummary` 가 이미 좌표를 들고 있어 **추가 조회 없이 가능**·R3 은 `existsByUserIdAndContentId` 가 미사용 상태로 존재함을 새로 확인. 추적표 #4·#6·#8·#9 를 `📤 요청`으로 갱신 + `AuthController` 라인 참조 정정. 코드 변경 없음(문서만).

- 2026-08-22 · 🧪 목 API 대역으로 라이브 검증 수행(코드는 미보관) · 실 백엔드가 Java·DB 접속정보 없이는 못 뜨고 이 환경엔 **Java 자체가 없어** `◐` 5개의 라이브 검증이 막혀 있었다. 백엔드 소스(`aa2a555`) 실측 DTO 에 1:1 로 맞춘 목 대역을 임시로 띄워 아래 검증을 수행했다(계약 공백 R1 `userId` 없음 · R2 코스 좌표 없음 · R3 `liked` 없음 · `avgPrice` null · 생성 시 `title` null · `placeName` 빈 문자열까지 그대로 재현해 거짓 통과 방지). **대역 코드는 사용자 요청으로 저장소에 남기지 않았다** — 재현이 필요하면 다시 만들어야 한다.
- 2026-08-22 · P3 완료(`☑`) · 목 대역 브라우저 실측으로 DoD 충족 — **카드 클릭·마커 클릭 모두** 실상세 렌더, 데이터 3종(rich / 홈페이지 없음 / 전부 null) + 줄바꿈 보존 + 운영시간 중복 제거 + `contentTypeId 25` 접기 확인. 실 백엔드 왕복은 별도로 남는다.
- 2026-08-22 · F1·F2·F3·F5·F6 라이브 실측(목 대역, `◐` 유지) · F1 `11:15 · 150분`+`수정됨` / F2 대중교통 ₩54,000 → 자차 ₩66,000(총액·1인당·안내문 동반) / F3 커넥터 45분 → 20분 + 아이콘 즉시 갱신 / F5 지도 즉시 렌더 + `내 코스만 보기` + bbox 재투영 / F6 마커 `+` → Day1 6→7·경로선 확장 → `−` 로 복귀(드로어 안 열림). 추가로 GBC014 공개뷰(비로그인)·GBC017 2개 시군구 병렬 합침(22곳) 확인. 남은 미검증: 로그인 필요 흐름(사용자 대행)·모바일 폭(창 리사이즈 불가)·카카오 SDK 503(외부).
- 2026-08-22 · 실결함 수정 · (목 대역으로 공개뷰를 처음 열어 보고 발견) 공유(`Share.tsx`)·컬렉션(`Collection.tsx`) 화면이 테마를 **코드 그대로**(`003`) 노출하던 문제 → `travelThemeStore.getThemeLabel` 로 이름 표시(`문화`), 마스터에 없는 코드는 코드 폴백. 홈(`Index.tsx`)만 매핑하고 있어 세 화면이 달랐다. lint·build 통과.
- 2026-08-22 · **R2 계약 수용 반영 · 코스 좌표 실연동** · 백엔드 `cd6c7ec`(0.6.3)가 코스 상세·공개뷰 응답에 `mapx`/`mapy` 를 추가 → `CoursePlace.mapx/mapy` 타입 추가, `plannerStore.synthesizePoi` 가 `isValidTourCoord` 통과 시 `Poi.lat/lng` 주입, `mergeSources` 는 좌표를 **쌍으로** 선택(카탈로그 우선 → 코스 응답). `utils/kakaoGeocode`·`useCourseCoords` 는 좌표 null 전용 폴백으로 격하(제거 안 함). **실 백엔드 라이브 검증**(사용자가 로컬 기동): 좌표는 JSON number, 캐시 미스는 null · 코스 1의 18곳 중 12곳 즉시 렌더 · 지오코딩 왕복 0회 · 지도 탭에서 실좌표 4곳 bbox 재투영 · F3 이동시간도 실좌표 기반. ⚠️ 생성(GBC010)은 다른 DTO 라 여전히 좌표 없음(GBC017 로 커버 → 추가 요청 안 함). 추적표 #8 `☑ 확정(라이브)` · #9(찜 상태 조회)는 "추가 예정" 회신으로 `🕒 예정`. lint·build 통과.

- 2026-08-22 · **모바일 폭 검증 · F5 완료(`☑`) · F6 의 pan 충돌 조건 해소 · F7 신설** · 브라우저 창 리사이즈가 여전히 무효(`resize_window` 는 성공을 보고하나 `innerWidth` 1440 고정)라 **동일 출처 iframe 390×731 + Vite dev 모듈 그래프에서 `plannerStore` 직접 주입**이라는 우회로를 세워 컬렉션 진입 상태(`loadDetail` + `dests: []`)를 재현했다. 카카오 지도 SDK 는 이번엔 정상 로드(이전 503 은 외부 일시 장애) → 실지도 위에서 검증. F5: 모바일 탭 레이아웃 → '지도' 즉시 렌더 · `내 코스만 보기` 기본 on → `dests` 채우면 자동 off · 좌표 null 장소는 마커 없음 · 콘솔 에러 0 → **DoD 충족**. F6: 토글 위 `touchstart`/`touchend` 의 `defaultPrevented` 가 **둘 다 false**(카카오맵이 터치를 삼키지 않음) + 탭으로 마커 앵커 불변 + 실 포인터 드래그 pan 이 코스·드로어 불변 + `−`/`+` 양방향 동작 → **pan 충돌 조건 해소**, 저장 왕복(로그인)만 남아 `◐` 유지. 부수 확인: 카카오 `Polyline` SVG 가 지도 전체를 덮지만(1715×2115, `pointer-events:auto`) 이벤트가 버블해 pan 을 막지 않음(막을 후보로 의심했으나 기각). **신규 결함 F7**: 마커 겹침으로 토글 도달 불가(6개 중 1개 재현, 오조작 시 옆 POI 드로어 열림). 백엔드 `ddcdbb2` 변동 없음 → R1·R3 미도착, 섬 M 은 `⏸` 유지.

- 2026-08-22 · **F7 구현(`◐`) — 지도 마커 클러스터링 + 잔여 겹침 해소** · 승인안(POI 클러스터링 + 코스 마커 예외)대로 `components/planner/mapCluster.ts` 신설(`clusterMarkers`·`spreadOverlaps`, 순수 함수) + `markerStyle` 클러스터 배지 + `KakaoMap` 배율 추적(`idle`)·클러스터 오버레이·경로선 변위 동기화 + `utils/kakaoMap.ts` 타입 확장. 구현 중 두 가지를 실측으로 바로잡았다: ①클러스터링만으로는 DoD 미달(코스↔코스·그룹↔코스 겹침이 남아 13개 중 3개 도달 불가) → 겹침 해소 패스 추가. ②변위 상한을 0.01° 로 뒀다가 **광역 시야의 정당한 변위까지 막혀** 0.5° 로 완화. 검증: lint·build·tsc 통과 + node 50케이스 + 빌드 CSS 유틸 확인 + 라이브 390px(실 백엔드 경주 318곳)에서 POI 60곳 → 2그룹·컨테이너 안 8개 전부 도달 가능·그룹 클릭으로 56곳 → 40+3+개별로 풀림. ⏳ 펼친 뒤 상태는 하니스 신뢰 문제(레이아웃 오감·HMR 반쪽 갱신·CDP 타임아웃)로 미판정 → 보드 F7 의 🧭 재개 방법에 측정 절차를 남겼다. 진단 부산물로 확인한 사실: 카카오 `CustomOverlay` 에 `setPosition` 은 있고 오버레이 제거도 정상(유령 없음), 지도 컨테이너가 뷰포트 아래로 잘리면 `elementFromPoint` 측정이 오탐을 낸다.

---

## 3. 헤더 "이전 업데이트" 이력 (2026-08-08 ~ 2026-08-29)

> 보드 상단에 회차마다 누적되던 `> 이전 업데이트:` 줄 전량. 내용이 위 §2 진행 로그와 겹치지만
> 서술 시점·요약 각도가 달라 원문을 보존한다. 보드에는 **최신 1건만** 남긴다.

> 이전 업데이트: 2026-08-29 (**F8 ㉠ 완료 `◐` — 지도 크롬이 마커를 가리는 문제 해소**. 승인 범위(㉠ 컨트롤 겹침)만 구현했다. `spreadOverlaps` 에 **금지 구역**(`ForbiddenBox[]`)을 더하고, `anchorKeepOut` 이 컨트롤 사각형을 **클릭 대상(배지·토글) 중심 기준**으로 부풀려 앵커 금지 영역으로 바꾼다. `KakaoMap` 은 `idle` 마다 `getBounds()` 한 번으로 배율과 금지 구역을 **함께** 읽고(따로 읽으면 이동 중 두 값이 어긋난다) `chromeRects` 로 **우리 컨트롤 + SDK 가 컨테이너에 직접 붙이는 축척·로고**를 함께 잡는다 — 검증 중 **축척(좌하단 135×19)도 마커를 가리는 것을 실측으로 새로 발견**했고 같은 경로로 해소했다. 검증: lint·build 통과 · node **14케이스** 통과 · 금지 구역 **전수 스윕 1218자리 전부 해소** · 앱 줌 흐름 82상태에서 **컨트롤 가려짐 14→0**, **그 외 겹침 108→108**(㉡ 을 악화시키지 않음). 라이브(실 백엔드 경주, 코스 #77·#78·#79·#84): 축척에 가려졌던 `경주 재매정` 이 **43px 비켜나 도달 가능**으로 바뀌는 것을 확인. ⏳ **남은 것**: ㉡(밀집 코스 마커) 미착수 · 합성 드래그로 만든 상태에서 컨트롤 겹침 1건이 관찰돼 **실제 손 제스처로 재확인 필요**.)
> 이전 업데이트: 2026-08-29 (**F7 완료 `☑` — 그룹을 펼친 뒤 도달 가능성 실측 통과 · 잔여 결함 2건은 F8 로 분리**. 사용자가 로컬 백엔드를 띄워 줘 **실 백엔드 경주 316곳**으로 라이브 실측했다. 390px iframe 하니스에서 코스를 3회 새로 생성하고(#70 Day1 5곳 · #74 Day1 7곳 · #75 Day1 8곳) **초기 뷰 → 가장 큰 그룹 클릭 → 그룹이 없어질 때까지** 반복하며 매 단계 지도 컨테이너 안 모든 마커·토글·그룹 배지 중심에서 `elementFromPoint` 를 찍었다: 모바일 390px **14/14 → 19/19 → 20/20 → 10/10**, 재현 **16/16 → 21/21 → 22/22 → 13/13** — **가려짐 0**. 지난 세션의 "펼친 뒤 마커 DOM 이 빈다"는 관찰은 **하니스 탓으로 확정**(DoD 흐름 밖인 `축소` 연타 상태에서만 재현, 그때 카카오 오버레이 레이어가 `display:none`). 보강으로 `mapCluster` 를 node 로 돌려 **실제 DOM 기하·z 규칙을 그대로 옮긴 도달 가능성 시뮬레이터**를 만들어 앱의 줌 흐름을 **392상태** 판정 — 현실형 데이터셋은 전 단계 통과. **신설 F8 `☐`**(검증에서 발견한 잔여 2건): ㉠ 데스크톱 실측에서 마커가 **지도 컨트롤(확대/축소/현위치 · `z-[3]` 32×108px)에 가려져** 누르면 확대가 실행됨 ㉡ 시뮬레이터가 찾은 하한 — 한 Day 코스가 **5곳 이상 반경 300m 안**에 몰리면 `spreadOverlaps` 가 빈 자리를 못 찾아 겹침이 남음(라이브 3회에서는 코스가 시내 전역에 흩어져 미재현). 백엔드 계약은 변동 없어 **R1 `userId`·R3 `liked` 미도착** → 섬 M 4개는 `⏸` 유지.)
> 이전 업데이트: 2026-08-22 (**백엔드 계약 요청서 작성** — 보드에 `☐`(대기) Task 가 남지 않아, 남은 진행을 막고 있는 계약 공백 3건을 백엔드에 넘길 문서 [`BE_계약_요청서.md`](./BE_계약_요청서.md) 로 정리했다: **R1** 로그인 사용자 `userId` 획득 경로(추적표 #4 → 섬 M 4개 Task 블로커), **R2** 코스 장소 좌표 `mapx`/`mapy`(#8 → 컬렉션 진입 시 지도 마커 0개), **R3** 찜 상태 조회 경로(#9 → 새로고침 후 하트가 비어 보이고 그 클릭이 실제로는 해제). 백엔드 `aa2a555`(0.5.12) 재실측으로 **3건 모두 미해결 확인** + 새 사실 2건: ①`service/PoiSummary.java` 가 이미 `mapx`/`mapy` 를 들고 있고 `TourCourseServiceImpl:269~279`·`:315~325` 가 그 `summaryMap` 으로 `placeName`·`thumbnailImg` 를 채우므로 **R2 는 TourAPI 추가 호출 없이 헬퍼 1개 + DTO 2필드로 가능**, ②`UserPoiLikeRepository.existsByUserIdAndContentId` 는 **선언돼 있으나 호출처가 0곳**이라 R3 도 판정 로직이 이미 있다. 요청서엔 각 건의 근거(`파일:라인`)·요청안 택1·권장안과 이유·백엔드 변경 지점·수용 시 FE 대응·회신 양식을 담았고, #6(`transport` 영속)은 부록 B 로 판단 요청. 추적표 #4·#6·#8·#9 상태를 `📤 요청`으로 갱신하고 `AuthController` 라인 참조를 현재 소스 기준으로 정정. 코드 변경 없음(문서만).)
> 이전 업데이트: 2026-08-22 (**F6 · 지도 코스 편집 타당성 검토 완료 + (a)안 구현(`◐`)** — 드래그 3안을 코드 제약으로 평가해 **드래그를 기각**했다: 마커가 React 밖 DOM(포털 필요·결과 411곳), **모바일은 지도와 코스 패널이 동시에 안 보이고 지도 쪽엔 DndProvider 자체가 없다**(모바일 DoD 충족 불가), 지도 pan 과 포인터 경쟁, 좌표 없는 장소는 마커가 없어 순서 편집이 예측 불가. 승인받은 **(a) 마커 +/− 토글**을 구현 — 배지 오른쪽 아래에 `absolute` 로 겹쳐 앵커를 지키고, 활성 Day 에 있으면 제거·없으면 추가(`dirty` → '변경 저장'), 카카오맵·폴백 지도 동일 규약. 지도에서 **제거가 아예 불가능**했던 결함도 해소. lint·build 통과 + 빌드 CSS 유틸 생성 확인 + 14케이스 node 검증. ⏳ 백엔드 미기동으로 라이브 클릭·모바일 터치 미검증.)
> 이전 업데이트: 2026-08-22 (**P3 · GBC018 POI 상세 통합 FE 완료(`◐`)** — `PoiDetail` 을 백엔드 DTO 실측 1:1 로 교체 + `getPoi(contentId)` 신설, `usePoi` 를 실상세 조회로 교체(contentId 캐시·in-flight dedup·동기 시드·응답 id 대조로 stale 차단), 병합 규칙을 `utils/poiDetail.ts` 로 분리(**상세는 빈 칸만 채운다** — 코스의 시각·비용·운영시간과 목록 실좌표를 덮지 않고, `장소 #id` placeholder 이름만 승격), 좌표 검증을 `utils/coords.ts` 로 공유. 드로어는 점진적 보강 렌더(소개·주소·전화·홈페이지·부가정보 + 로딩 스켈레톤 + 인라인 재시도). lint·build 통과 + 39케이스 node 검증. ⏳ 백엔드 미기동으로 라이브 왕복 미검증, 코스 생성 API 는 사용자 요청에 따라 미호출.)
> 이전 업데이트: 2026-08-22 (**F4 · POI 좋아요 토글 정합성 조사·수정 완료(`☑`)** — 원인 확정: 백엔드는 **진짜 토글**이고(insert-only 기각) 사용자 증상의 실원인은 백엔드 저장 유실 버그로 `6e4e682`(2026-08-16)에서 이미 수정됨. 남은 계약 공백은 **찜 상태 조회 경로가 없다는 것**(새로고침하면 하트가 비어 보이고 그 클릭이 실제로는 해제가 된다) → 계약 추적표 #7 확정·**#9 신설**. FE 결함 2건 수정: `poiLikeStore.reset()`+`authStore` 구독으로 **세션 전환 시 이전 사용자 찜 상태 제거**(로그아웃 후 하트가 남던 실결함), `usePoiLike` 가 응답 `liked` 와 낙관적 기대가 다를 때 toast 로 안내. lint·build 통과 + 13케이스 node 검증. 브라우저 실측은 로그인 필요로 사용자 대행 항목.)
> 이전 업데이트: 2026-08-22 (**F3 · 이동수단별 코스 간 이동시간 표시 완료(`☑`)** — `utils/travelTime.ts` 신설(haversine 직선거리 × 우회 보정 1.3 × 수단별 실효 속도(도보 4·대중교통 20·자차 40km/h), 5분 단위·하한 5분, 좌표 없으면 `null`) + `parts/TravelConnector` 로 코스 항목 사이에 `약 20분 이동`(수단별 lucide 아이콘) 표시 + `CoursePanel` 이 좌표를 `useCourseCoords` 로 채워 지도를 열지 않아도 계산된다. 추정임을 패널에 한 줄로 명시. lint·build 통과 + 28케이스 node 검증 + 라이브 확인(수단 전환 즉시 갱신·실제 드래그 재정렬 후 재계산·좌표 없는 구간 표기 없음·다크·콘솔 에러 0).)
> 이전 업데이트: 2026-08-22 (**F2 · 이동수단 상태화 + 예산 교통비 편집 FE 완료(`◐`)** — `plannerStore.transport`·`transportOverride` 신설(생성·상세 응답에서 주입) + `BudgetDashboard` 교통 행에 수단 `<select>`·금액 직접 입력·되돌리기 + `utils/budget.computeBudget` 을 수단별 계수(`TRANSPORT_RATE`: 도보 0 / 대중교통 1인 1일 9,000 / 자차 1대(4인) 1일 22,000)로 분기. **백엔드 확인 결론: `transport`·교통비는 영속 불가**(수정 경로 없음 + 0.5.9 에서 교통비 산정 자체를 FE 전담으로 이전) → 저장은 `⏸`, `dirty` 를 세우지 않고 UI 에 그 사실을 명시. 부수 실측: `durationMinutes`·`cost` 는 **저장된다**(S8·F1 의 "저장 안 됨" 메모는 낡았다), `GET /poi/{contentId}` 도 구현돼 **P3 블로커 해제**. lint·build 통과 + 계산 30케이스 node 검증 + 라이브 확인(수단 전환 시 즉시 재계산·커스텀 금액·되돌리기·다크·좁은 폭 줄바꿈·콘솔 에러 0).)
> 이전 업데이트: 2026-08-22 (**F5 · 지도 코스 전용 표시 + 컬렉션 진입 지도 미표시 수정 FE 완료(`◐`)** — `ResultsPanel` 이 지도 모드를 지역 미선택 early return **앞에서** 렌더하도록 바꿔 "지도를 눌러도 안 열리던" 원인을 제거하고, `내 코스만 보기` 토글 추가. 코스 응답에 좌표가 없는 문제는 `utils/kakaoGeocode`(장소명→좌표, 경북 bbox 필터) + `hooks/useCourseCoords` + `plannerStore.placeCoords` + `mergePoi(…, coords)` 로 메우고, 폴백 지도는 `projectToPlaceholder` 로 재투영해 마커가 중앙에 겹치지 않게 했다. 스토어에 상세 응답 형태를 주입해 컬렉션 진입을 재현·라이브 확인(지도 즉시 렌더·토글·경로선·콘솔 에러 0). ⏳ 지오코딩 실동작은 이 환경의 카카오 SDK **503**(이번 변경과 무관) 때문에 미검증, 근본 해결은 계약 추적표 #8(코스 응답에 `mapx`/`mapy`).)
> 이전 업데이트: 2026-08-22 (**F1 · 코스 시각·체류시간 편집 FE 구현 완료(`◐`)** — `plannerStore.placeTimes`(시각·체류시간 지정) + `CourseItem` 인라인 편집 + `coursePayload` 가 **지정값을 원본 슬롯보다 우선**하도록 교정 + `useCourseUpdate` 전달. 카드 내부 컨트롤 클릭이 상세 드로어를 같이 열던 전파 버그도 함께 수정. lint·build 통과, 페이로드는 node 하네스로 8케이스 검증. ⏳ 라이브 E2E 는 **백엔드 :8080 미기동**(코스 생성 실패)으로 미완, 저장 왕복은 로그인 필요라 사용자 대행 필요. ⚠️ `durationMinutes` 서버 저장 여부 미확인 → 계약 추적표 #5·#6·#7 신설.)
> 이전 업데이트: 2026-08-22 (**그룹 F · 사용자 피드백 반영 6개 Task 신규 등록** — 사용자 실사용 피드백(플래너: 코스 시각·체류시간 편집 / 이동수단별 이동시간 / POI 좋아요가 추가만 됨 / 지도 DnD·내 코스만 보기 / 예산 교통비 편집 · 컬렉션: 진입 직후 지도가 열리지 않음)을 착수 가능한 Task 로 분해해 **F1~F6** 으로 등록했다(구현 착수 전, 전부 `☐`). 접수 원문 중 "내 코스 클릭 시 상세 조회"는 **S4(GBC012)로 이미 완료**되어 신규 Task 없음. 조사로 원인을 확정한 항목: **컬렉션→플래너 진입 시 지도 미표시 = `loadDetail` 이 `search.dests` 를 빈 배열로 두고(상세 응답에 시군구 코드가 없음) `ResultsPanel` 이 지역 미선택 EmptyState 로 early return 하기 때문**(F5). 상세 사양은 `FE_개발순서.md` §그룹 F.)
> 이전 업데이트: 2026-08-15 (**S8 · GBC020 코스 수정 영속화 FE 완료** — 백엔드가 개발 중인 `PATCH /tour-course/{courseId}` 상세 명세(요청 바디 = `schedule` **전체 교체**, 장소명 키 `contentName`)를 수령해 ⏸ 해제. `api/tourCourse.updateCourse` + `utils/coursePayload.buildSchedulePayload`(신설: 원본 응답 `baseSchedule` 에서 날짜·시각·PlaceType·체류시간·썸네일·운영시간 복원 + 새로 담은 장소는 카탈로그 Poi 로 채움 + 예산 override(총액)→`cost`(1인) 환산 + 실 contentId 없는 목 슬러그 id 제외) + `hooks/useCourseUpdate`(명시적 저장·실패 시 dirty 유지) + `plannerStore.baseSchedule`/`dirty`/`markPristine`(no-op 편집은 dirty 미세움). 저장 버튼 하나가 **미소유=소유권이전(GBC016) / 소유=변경 저장(GBC020)** 을 겸하고, 게스트가 편집한 코스는 assign 성공 직후 편집분을 자동 flush. lint·build 통과. ✅ **라이브 E2E 완료**(코스 39, 실백엔드): 장소 제거 → 버튼 '저장됨'→'변경 저장' 전환, dnd 재정렬, 비용 편집 → 변경 저장 시 `PATCH /tour-course/39` **200 "코스 수정 완료했습니다."** → 재진입 시 구성·순서·시각·체류시간 유지, 콘솔 에러 0. ⚠️ **비용(cost)은 서버가 저장하지 않는다** — 백엔드가 `contentName`·`thumbnailImg`·`operatingHours`·`cost` 를 표시 전용으로 무시(실측 DTO 주석)하므로 예산 편집은 재진입 시 기본값으로 복귀. 검증 중 발견한 **빈 Day → 400**(`@NotEmpty`)은 전송 전에 막고 어느 Day 인지 toast 로 안내하도록 보강(라이브 확인: 요청 0건))
> 이전 업데이트: 2026-08-15 (**보류 라이브 검증 마무리(비로그인 범위)** — 백엔드 AI 생성이 복구돼 그간 아웃티지로 미뤄둔 항목을 실측으로 닫음. **S7 공유** = 카카오 공유 피커 실제 오픈(폴백 미발동), **S7 공개뷰** = `/share/37` 비로그인 실데이터 렌더(Day1 6곳·Day2 5곳 실장소명), **P1** = 비로그인 찜 게이트 모달 확인. 콘솔 에러 0. ⚠️ 로그인이 필요한 S2 저장·S6 제목 영속·P1 좋아요 서버 반영은 조직 지침(비밀번호 입력 금지)상 에이전트가 로그인할 수 없어 미검증으로 남음)
> 이전 업데이트: 2026-08-15 (**지도를 카카오맵 실연동으로 교체** — 사용자 요청. `MapView`가 SDK 온디맨드 로드(`utils/kakaoMap`) → `parts/KakaoMap`(CustomOverlay 마커·Polyline 경로·줌/현위치 컨트롤) 렌더, 실패 시 기존 `parts/PlaceholderMap`로 자동 폴백. `Poi.lat/lng`(실경위도) 신설 + `usePoiList`가 GBC017 `mapx/mapy`를 그대로 실음. 마커는 **결과 ∪ 활성 Day 코스**(코스는 순번 배지). 드로어 "카카오맵에서 보기" 데모 토스트 → 실제 `map.kakao.com/link/map/{name},{lat},{lng}` 새 탭. ✅ 5173 라이브 실지도 확인(카카오 콘솔에서 지도/로컬 서비스 활성화 후 SDK 200): 실타일·마커·경로선·줌·마커→드로어·카테고리 필터 갱신·다크테마·콘솔 에러 0)
> 이전 업데이트: 2026-08-15 (코스 장소 실데이터 반영 — 백엔드가 생성 응답에 넣기로 한 **`contentName` 선반영**(이름 폴백 `placeName || contentName || '장소 #id'`) + **이미 오고 있었지만 FE가 버리던 `thumbnailImg`·`operatingHours`·`cost`·`durationMinutes` 연결**(예산이 항상 ₩0이던 원인) + `Poi.hours`(운영시간)와 `visitTime`(방문 시각) 의미 분리 + 병합 규칙·결과 카드 일관성 정리. 라이브에서 예산 ₩95,000 실계산·실썸네일·운영시간 확인)
> 이전 업데이트: 2026-08-15 (P2 후속 수정 — 코스 패널에 `장소 #id` placeholder 가 남던 결함 해결. 카탈로그 병합은 정상이었으나 컴포넌트가 스토어의 `resolvePoi` **함수 참조**를 구독해 `poiCatalog` 갱신 시 리렌더되지 않은 것이 원인. `mergePoi()` 추출 + `hooks/usePoiResolver`(두 레지스트리 구독) 신설 → 소비처 5곳 전환. 병합 시 분류(cat)도 카탈로그 기준으로 통일해 배지/썸네일 라벨 불일치 제거. 5173 라이브에서 실제 AI 생성 코스가 실명으로 렌더됨을 재확인)
> 이전 업데이트: 2026-08-15 (**P2 · GBC017 큐레이션 POI 목록 완료** — 백엔드 v0.5.1이 `GET /api/v1/poi`를 구현해 P2 착수 가능해짐(S8·P3·섬M은 여전히 미구현/⏸). `api/poi.ts` `getPois` + 응답 타입을 백엔드 `PoiCurationResponseDto`/`ItemDto` 실측 1:1로 교체(잠정안 폐기), `usePoiList` 내부를 목→API 전환(선택 시군구 **병렬 조회 후 합침**·contentId dedup·in-flight dedup), `plannerStore.poiCatalog`+`registerPois`+`resolvePoi` 병합(코스 장소 placeholder가 카탈로그 실데이터로 승격), `ResultsPanel` 4상태(스켈레톤·ErrorState·데이터없음·카테고리없음)+더 보기 페이징, 실 썸네일 렌더(`ImgPlaceholder` img+실패 폴백, http→https 승격), 좌표 bbox 투영(잘못된 0 좌표 방어). lint·build 통과. 5173 라이브(실백엔드): 경주+포항남구 병렬 조회 **411곳** 실장소명·실사진 렌더, 카테고리 필터(음식점 129곳)·더 보기(60→120)·지도 마커 분산·드로어·코스 추가·다크테마 확인, 콘솔 에러 0. ⚠️ 백엔드 계약 차이 2건: `theme` 파라미터 없음(테마 필터 불가)·`avgPrice` 항상 null(BOQ14)→'가격 미정'. AI 코스 생성(`POST /tour-course`)은 여전히 500이라 코스 진입만 XHR 스텁으로 우회해 검증)
> 이전 업데이트: 2026-08-08 (Index 검색바 lg 구간 개선 + 일정 날짜 축약 — 사용자 요청 UI/UX 정비. **①일정 날짜 잘림 해결(핵심)**: `DatePicker` `dateFormat="yyyy/MM/dd"`(범위 시 "2026/08/12 - 2026/08/14" ≈23자 → 좁은 칸에서 잘림)를 **커스텀 입력 `DateTrigger`**(`forwardRef`)+`dateFormat="MM/dd"`로 교체 → **"MM/dd ~ MM/dd"**(연도 제거·구분자 `-`→`~`, 예 "08/12 ~ 08/22")로 짧게 표시. `index.css`에 `.react-datepicker-wrapper{display:block;width:100%}` 추가(커스텀 버튼 칸 전체 너비). **②lg 구간 여백/비중**: 필드 좌우 패딩 `lg:px-4`→`lg:px-3`(5필드)·컨테이너 `lg:gap-4`→`lg:gap-2`로 공간 확보 + 텍스트 많은 목적지·테마(콤마 라벨)에 `lg:flex-[1.3]` 비중 부여. lint·build 통과. 5173 라이브: 날짜 선택 시 "08/12 ~ 08/22" 잘림 없이 정상 표시·캘린더 정상·검색바 행 무결 확인. ⚠️ lg 실폭(1024~1280) 렌더는 자동화 창 폭 락(리사이즈해도 스크린샷 1545px 고정)으로 라이브 불가 → 정적 검증으로 커버. 계약 추적표 무관)
> 이전 업데이트: 2026-08-08 (Planner 데스크톱 패널 반응성 개선 — 사용자 요청 UI/UX 정비 후속. `Planner.tsx` 데스크톱 레이아웃의 **①패널 고정 높이 `h-[560px]`→뷰포트 기반** `h-[clamp(500px,calc(100vh_-_15rem),780px)]`(헤더·요약·여백 ≈15rem 차감, 하한 500·상한 780px clamp — 작은 화면 답답함/큰 모니터 과잉 여백 동시 완화), **②사이드바 너비 고정 `360px`→유연** `grid-cols-[clamp(320px,26%,400px)_1fr]`(중간 폭 1024~1280에서 결과 패널에 여유). 상수 `PANEL_H`·`PANEL_GRID`로 추출해 두 패널 공유. lint·build 통과 + 빌드 산출 CSS에 두 clamp 유틸 정상 생성 확인(arbitrary value 파싱 검증). ⚠️ **패널 실제 렌더 라이브 확인은 보류** — 코스 생성 `POST /tour-course`가 여전히 500(백엔드 AI 아웃티지, 5173 실측 재확인)이라 게스트 생성으로 플래너 진입 불가·로그인은 조직 지침상 비밀번호 입력 불가(S1·S7과 동일 제약). CSS-only 저위험 변경이라 build+CSS 생성으로 커버. 계약 추적표 무관)
> 이전 업데이트: 2026-08-08 (헤더 UI/UX 정비 — 남은 24개 Task가 모두 백엔드 대기(⏸/⛔)라 사용자 요청으로 전역 헤더 손질. **①죽은 알림 벨 제거**(`HeaderLayout` `<Bell>` 버튼에 onClick·기능·aria 전무한 dead affordance → 제거, 우측 아이콘 촘촘함도 해소), **②아이콘 접근성/시맨틱 정비**(테마 래퍼 `div role="button"` → role 제거(내부 label+checkbox가 상호작용 담당, 중첩 인터랙티브 회피)·checkbox에 상태 가변 `aria-label`, 계정 트리거에 `aria-label`+`aria-haspopup="menu"`, 테마 SVG `aria-hidden`), **③아이콘 크기 통일**(테마 SVG 24→20px로 계정 User(20)와 정렬)·간격 `gap-1 md:gap-2`→`gap-0.5 md:gap-1`. lint·build 통과. 5174 라이브: 벨 제거·아이콘 2개 정렬 확인, 테마 토글 라이트↔다크 정상(다크 배경 렌더 양호), 계정 드롭다운(로그인/회원가입) 정상 오픈. 계약 추적표 무관)
> 이전 업데이트: 2026-08-08 (반응형 전면 점검 — 사용자 요청으로 모바일·데스크톱 반응형 전수 점검. 반응형 리스크 패턴(고정폭·오버플로·grid·flex) 전수 grep + 핵심 화면(Layout/Header/Footer·Index 검색바·Planner·Collection·BudgetDashboard·About·AuthScreen) 정독 결과 **이미 mobile-first로 견고**(grid는 `grid-cols-1`→`sm:`/`lg:` 확장, Index 검색바 `flex-col rounded-2xl`↔`lg:flex-row rounded-full`, Planner 데스크톱 그리드↔모바일 세그먼트 탭, Header 버거↔데스크톱 탭). 도달 가능한 전 페이지 데스크톱 가로 오버플로 0(JS scrollWidth 검사). **발견·수정**: `index.html` `lang="en"`→`ko`(한국어 앱), `<title>React</title>`→`경북 CoCo`(기본 Vite 제목 노출). ⚠️ automation 창이 1440px 하드락(screen.availWidth=1440)이라 실제 좁은 폭 라이브 구동은 불가 → mobile-first 클래스 정적 검증 + 데스크톱 오버플로 스캔으로 커버. lint·build 통과)
> 이전 업데이트: 2026-08-08 (디자인 테마 가변성 전면 점검 — 남은 24개 Task가 모두 백엔드 대기(⏸)라 사용자 요청으로 라이트/다크 양 테마 가독성 정비. 고정색 전수 스캔 결과 대부분 이미 시맨틱 토큰화됨 → 남은 실결함 수정: `Layout.tsx` 메인 배경 `bg-[#F8F9FF]`→`bg-base-200`(다크에서 밝은 배경 위 어두운 카드 깨짐 해소, 핵심), `index.css :root` `background-color: #F8F9FF`→`var(--color-base-200)`(overscroll 테마 가변), `Loading.tsx` `text-slate-500`→`text-base-content/60`·`text-indigo-500`(오프브랜드)→`text-primary`. DESIGN.md 드리프트 정정(§2.3·2.4·3·6.4·6.6이 §6.7 "테마 가변 필수"와 모순되던 고정색 권장 제거). Kakao 브랜드색·백드롭 `bg-black/*`·컬러배경 위 `text-white`·지도 플레이스홀더는 정당한 예외로 유지. lint·build 통과. 5173 라이브: /·/about 라이트·다크 양 테마 렌더 확인)
> 이전 업데이트: 2026-08-08 (A2·A4 완료 — A2: `pages/About.tsx` 스텁→서비스 소개(히어로+핵심기능 F1~F3+이용방법 4단계+CTA, 시맨틱 토큰·lucide·반응형), 푸터에 "서비스 소개" 링크 추가+`ACME` placeholder 저작권→경북 CoCo 정정. A4: `RootLayout.tsx`는 CLAUDE.md 규칙상 삭제 금지 → **존치 결정**, 의도된 미연결 플레이스홀더임을 파일 주석으로 문서화. lint·build 통과. 5173(→5175) 라이브: /about 라이트·다크 렌더 확인)
> 이전 업데이트: 2026-08-08 (P1 · GBC019 POI 좋아요 토글 완료 — `api/poi.ts` `togglePoiLike`(`POST /poi/{id}/like`) + `stores/poiLikeStore`(poiId→liked 공유) + `stores/loginGateStore`(게이트 전역 승격, Planner 로컬 게이트 이관) + `hooks/usePoiLike`(로그인 게이트+낙관적 토글+실패 롤백, 실 contentId만 서버 호출·목 슬러그 id는 로컬) + `components/planner/LikeButton`(하트) → POICard(stacked/horizontal)·PoiDrawer에 배치. lint·build 통과. ⚠️ 실동작 라이브 검증은 백엔드 AI 생성 아웃티지로 플래너 진입 불가라 보류(P1 사양이 실동작 검증을 P2/P3로 위임))
> 이전 업데이트: 2026-08-08 (P0 · POI 목→API 교체 준비 완료 — `hooks/usePoiList`(목록, P2 교체점)·`hooks/usePoi`(단일 상세, P3 교체점) 신설 + `ResultsPanel`이 `@/mocks` 직접 import 대신 `usePoiList` 경유·`PoiDrawer`가 `resolvePoi` 직접 호출 대신 `usePoi` 경유 + `api/poi.ts`에 역방향 매핑 `CAT_BY_CONTENT_TYPE` 추가. 순수 리팩터(로직·목 데이터 동일) → lint·build 통과. P2/P3에서 훅 내부만 API로 교체하면 소비처 무수정)
> 이전 업데이트: 2026-08-08 (A3 · 문서 드리프트 최신화 완료 — `CLAUDE.md`(디렉터리 트리 `api/`·`hooks/` "비어 있음" 정정 + 핵심 파일에 `api/client.ts`·`hooks/useAsync.ts` 추가), `PRD_FRONT.md`(§5 구현 매핑 표를 S1~S7 완료·실제 파일/스토어/훅 목록으로 갱신 + 상단·§5에 보드가 구현 현황 정본이라는 포인터), `FEATURES_FRONT.md`(상단에 인라인 구현 표기가 기준일 스냅샷임을 경고 + 보드 포인터). 소스 무변경(문서만) → lint·build 영향 없음)
> 이전 업데이트: 2026-08-08 (A1 · `/home` 더미 라우트 제거 완료 — `router.tsx`에서 `Home` lazy import·`home` 라우트 블록 삭제 + `src/pages/Home.tsx`(반응형 레이아웃 테스트용 더미) 삭제. `/home`로의 링크·네비게이션 없음 확인(다른 `Home` 참조는 lucide 아이콘으로 무관). lint·build 통과)
> 이전 업데이트: 2026-08-08 (S7 · GBC014 공유+공개뷰 완료 — `getPublicCourse`(순수 axios, 인터셉터 우회)+`shareRouter`(가드 밖 `/share/:id`)+`Share` 읽기전용 공개뷰+`useCourseShare`(카카오 공유 SDK 시도→실패 시 클립보드 폴백)+`kakaoShare`/`courseFormat` 유틸. lint·build 통과. 5173 라이브: 가드 밖·비로그인 401 미발생·에러 UI 검증. 해피패스 데이터 렌더는 백엔드 AI 생성 500 아웃티지로 보류(복구 시 재확인))

---

_이 파일은 추가만 한다. 새 Task 가 완료되면 보드에서 상세를 잘라 §1 에, 로그가 20줄을 넘으면 오래된 것을 §2 에 옮긴다._

---

## 4. 진행 중(`◐`) Task 의 구현 상세 (F1·F2·F6·F8)

> 이 4건은 **FE 구현이 끝나고 검증·백엔드 조건만 남은** 상태다. 보드에는 "남은 조건"만 두고
> 착수 당시의 구현 서술은 여기로 옮겼다. 상태 자체는 보드가 정본이다.

- [ ] `◐` **F1 · 코스 시각·체류시간 편집** — **FE 구현 완료 / 라이브 검증 대기**. `types/planner.PlaceTimeEdit` 신설 + `plannerStore.placeTimes`(`setPlaceTime`·`setPlaceDuration`·`resetPlaceTime`, 같은 값 재입력은 no-op 으로 dirty 미세움, 코스 로드 시 초기화) + `CourseItem` 시간 칩을 인라인 편집으로 교체(`<input type="time">` + 체류시간 분 입력, 완료·되돌리기, Escape 닫기, `수정됨` 배지) + `utils/coursePayload`(**사용자 지정 시각·체류시간이 원본 슬롯 배분보다 우선**, `resolveDurationMinutes`·`placeTypeOfCat`·`findBasePlace` 를 export 해 카드 표시와 저장 페이로드가 같은 규칙을 쓰게 함) + `useCourseUpdate` 가 `placeTimes` 를 페이로드에 전달. 같은 Day 시각 역전 시 toast 안내(자동 정렬은 하지 않음 — 코스 순서가 정본). 부수 수정: 카드 내부 컨트롤(삭제·금액·시간) 클릭이 카드 루트로 전파돼 **상세 드로어가 같이 열리던 문제**를 `stopClick` 으로 차단. 검증: `npm run lint`·`npm run build` 통과 + `buildSchedulePayload` 를 rolldown 번들 → node 로 **8케이스 실행 확인**(지정 시각/체류시간 우선, 미지정은 원본 슬롯 유지, 재정렬 후 시각 역전 없음, 슬롯 부족 시 `직전 시각+체류+이동여유`, 체류 0분(숙박) 허용). ✅ **편집 UI 라이브 확인**(2026-08-22, 목 대역): 코스 항목의 시간 칩 클릭 → `<input type="time">`+분 입력 → 완료 → 카드가 `11:15 · 150분` + `수정됨` 배지로 갱신. GBC020 저장 왕복은 **API 레벨로 확인 완료**(목에 `durationMinutes:120`·`cost:7000` PATCH → 재조회에서 저장값 우선, `cost:-5` 는 400 `cost는 0 이상이어야 합니다`). ⏳ **브라우저에서의 저장 왕복은 로그인이 필요**해 사용자 대행(조직 지침상 에이전트가 비밀번호를 입력할 수 없다). ⚠️→☑ 서버가 `durationMinutes` 를 저장하는지는 **확인됐다**(2026-08-22 백엔드 소스 실측 — GBC020 이 `durationMinutes`·`cost` 를 그대로 저장하고 조회 시 저장값 우선, 계약 추적표 #5). 남은 건 라이브 왕복 재확인뿐. → 순서 §그룹F F1
- [ ] `◐` **F2 · 이동수단 상태화 + 예산 교통비 편집** — **FE 완료 / 영속은 백엔드 미지원(`⏸`)**. (1) **상태화** — `plannerStore.transport`(`loadFromApi` = 홈에서 고른 수단, `loadDetail` = 상세 응답 `transport` 주입, `setTransport`) + `transportOverride`(사용자가 직접 입력한 교통비 총액, `setTransportOverride`·`resetTransportOverride`). `LoadFromApiContext.transport` 신설 → `Index.handleSearch` 가 생성 요청에 실어 보낸 값을 그대로 스토어에 넘긴다. (2) **계산** — `utils/budget.computeBudget` 을 **옵션 객체 시그니처로 정리**하고 하드코딩 `8500 × days × ceil(n/4)` 을 `TRANSPORT_RATE` 기반 `estimateTransportCost` 로 교체: 도보 0원 / 대중교통 **1인 1일 ₩9,000** / 자차 **1대(4인) 1일 ₩22,000**(근거는 코드 주석에 명시 — 시외·시내버스 뭉친 값 / 1일 100km 유류비+통행료+주차). `transportOverride` 가 있으면 추정치를 이긴다(0원도 유효, 음수는 0). (3) **UI** — 교통 행에 수단 `<select>`(`courseFormat.TRANSPORT_ORDER`·`TRANSPORT_LABEL` 재사용) + 금액 인라인 입력(`CourseItem` 비용 편집과 같은 규약: 클릭→input→Enter/blur 저장) + `수정됨` 배지·`되돌리기`, 안내문을 수단 기준으로 갱신(`transportRateNote`). 좁은 폭 대비 행은 `flex-wrap`. **⚠️ 영속 = 백엔드 미지원(확인 완료)**: `transport` 수정 경로가 없고(PATCH 는 `/{id}`(schedule)·`/{id}/title`·`/{id}/assign` 뿐, DTO 에 필드 없음) 교통비는 백엔드 0.5.9 에서 **산정·저장 자체를 FE 전담으로 이전**했다 → 그래서 수단·교통비 변경은 **`dirty` 를 세우지 않고**(저장 버튼이 '저장됨'으로 거짓말하지 않게) UI 에 "이동수단·교통비 변경은 아직 코스에 저장되지 않아요"를 명시했다. DoD 중 **"재진입 시 수단 동일"만 미충족**(서버가 생성 시 수단을 그대로 주므로 편집분만 유실) → 계약 추적표 #6. 검증: `npm run lint`·`npm run build` 통과 + `computeBudget`/`estimateTransportCost` 를 rolldown 번들 → node 로 **30케이스 통과**(수단별 계수·정원 경계 4/5인·일수·override 0/음수/null·장소 override 공존·인원 0 방어·문구 일치). ✅ **라이브 재확인**(2026-08-22, 목 대역 코스): 생성 요청의 `PUBLIC_TRANSPORT` 가 그대로 교통 행에 반영되고, `자동차`로 바꾸니 ₩54,000 → ₩66,000 · 총액 ₩358,000 → ₩370,000 · 1인당 ₩179,000 → ₩185,000 · 안내문이 '자동차 1대(4인) 1일 ₩22,000 기준'으로 함께 갱신. **F3 커넥터도 같은 순간 45분 → 20분 + 아이콘 변경**. ✅ **이전 확인**(dev 5173 + 스토어에 상세 응답 주입): 대중교통 2인 3일 ₩54,000 → 자차 ₩66,000 → 도보 ₩0 즉시 재계산(총액·1인당·스택 막대 동반), 5인 자차 = 2대 ₩132,000, 금액 직접 입력 ₩30,000 유지 → 되돌리기로 추정치 복귀, 다크 테마·컨테이너 293px 줄바꿈 정상, 콘솔 에러 0. → 순서 §그룹F F2
- [ ] `◐` **F6 · 지도에서 코스 편집 — 타당성 검토 완료 + (a)안 구현 완료 / 라이브 검증 대기** — **검토 결론: 드래그 방식은 채택하지 않았다.** 코드에서 확인한 제약 4가지 — ①**마커는 React 트리 밖 DOM** (`KakaoMap` 이 `CustomOverlay` content 로 `document.createElement('button')` 를 만들고 `addEventListener` 로 연결, id 기준 수동 diff) → dnd-kit `useDraggable`(React ref/props 기반)을 붙이려면 오버레이마다 `createPortal` 이 필요하고 결과 목록은 실측 411곳까지 나온다. ②**모바일에선 지도와 코스 패널이 동시에 존재하지 않는다** — `Planner.tsx` 데스크톱은 `PlannerDndProvider` 가 CoursePanel+ResultsPanel 을 함께 감싸지만(같은 DndContext), 모바일은 탭 전환식이고 `ResultsPanel mobile` 은 **어떤 DndProvider 에도 들어 있지 않다** → 지도→코스 드래그는 모바일에서 화면상 불가능(=DoD 의 모바일 조건 충족 불가). ③**포인터 경쟁** — 카카오맵이 컨테이너 드래그를 pan 으로 소비하므로 드래그 안은 `setDraggable(false)`/복구가 필수이고 터치에선 pan·pinch·롱프레스 판정이 얽힌다. ④**좌표 없는 장소는 마커가 없다**(`hasLatLng` 필터) → 지도만으로 코스 전체를 편집할 수 없어 지도 편집은 보조 수단이어야 하고, 순서 편집을 얹으면 "지도에 없는 장소"의 순서를 알 수 없다. → 비용·위험·모바일 DoD 를 종합해 **(a) 마커 토글**을 사용자 승인 후 구현(2026-08-22). (b) 롱프레스 드래그 = ②로 모바일 불가·UX 이원화, (c) 순번 배지 드래그 = 저줌 마커 겹침으로 드롭 타깃이 모호하고 ④와 겹쳐 결과가 예측 불가 → 둘 다 기각. **구현**: `markerStyle.markerToggleClass`/`markerToggleLabel`/`MARKER_PLUS_SVG`/`MARKER_MINUS_SVG` 신설 + `KakaoMap` 오버레이에 토글 버튼(배지 오른쪽 아래에 `absolute` 로 겹쳐 **앵커 유지**, `wrap` 은 `relative inline-flex`, 상태 변화는 `paintToggle` 로 재도색, 최신 콜백은 `onToggleRef`) + 폴백 지도(`Marker`·`PlaceholderMap`)에 동일 규약 + `MapView.toggleInCourse`(활성 Day 에 있으면 `removePoi`, 없으면 `addPoi` → 둘 다 `dirty` → 저장 버튼이 '변경 저장'). 마커 본체 클릭은 기존대로 상세 드로어, 토글은 `stopPropagation`. 활성 Day 가 없으면 토글을 감춘다. **부수 효과**: 그전까지 지도에서 코스 추가는 2클릭(마커→드로어→'Day N 에 추가')이었고 **제거는 아예 불가능**했다 — 둘 다 1클릭이 됐다. 검증: `npm run lint`·`npm run build` 통과 + 빌드 산출 CSS 에 토글 유틸(`-bottom-1.5`·`h-[22px]`·`ring-base-300`·`inline-flex`) 생성 확인(**imperative DOM 이라 Tailwind 스캔 누락이 치명적**) + `markerStyle` 을 번들 → node 로 **14케이스 통과**(앵커 보존용 absolute·22px 고정·추가/제거 색 분리·고정색 미사용·라벨 문구·아이콘 `currentColor`/`aria-hidden`·배지 회귀). 구현 중 발견·수정: `hidden` 속성이 클래스의 `display:flex` 에 밀려 토글이 감춰지지 않던 문제 → 인라인 `style.display`. ✅ **데스크톱 라이브 확인**(2026-08-22, 목 대역): 마커 `+` 클릭 → **Day 1 이 6 → 7 로 늘고 경로선이 그 마커까지 확장**, 같은 자리를 다시 클릭(`−`) → 6 으로 복귀·경로선 원복. 두 번 모두 **상세 드로어가 열리지 않았다**(`stopPropagation` 동작). 마커 본체 클릭은 정상적으로 드로어를 연다. ✅ **모바일 폭(390px) 라이브 확인**(2026-08-22, F5 와 같은 iframe 하니스 · 카카오 실지도): **DoD 의 "지도 pan 과 충돌하지 않는다" 를 두 방향으로 측정**했다. **①토글 탭이 지도에 삼켜지지 않는가** — 토글 위에 취소가능 `touchstart`/`touchend` 를 던져 `defaultPrevented` 를 읽었더니 **둘 다 `false`** 였다(카카오맵이 `preventDefault()` 하지 않는다) → 브라우저가 tap→click 을 합성하므로 터치에서 토글이 살아 있다. 이것이 터치에서 토글이 죽는 **유일한 메커니즘**이었다. 같은 탭 동작으로 다른 마커들의 화면 앵커 좌표가 **1px 도 움직이지 않아** 탭이 pan 으로 오해되지 않음도 확인. **②지도 pan 이 토글을 오작동시키지 않는가** — 실제 포인터 드래그로 지도를 pan 했고(경주→토함산·석굴암 일대로 이동) `course.days` · `drawer` 가 **불변**이었다. 조사 중 확인한 것: 카카오 `Polyline` 이 그리는 SVG 는 지도 표면 전체를 덮는 1715×2115 크기에 `pointer-events: auto` 지만 이벤트가 지도 컨테이너로 버블해 **pan 을 막지 않는다**(막을 후보로 의심했으나 기각). **③담기/빼기 양방향** — `−` 탭 → Day 1 이 4 → 3곳·`dirty` 세워짐·드로어 안 열림, `+` 탭 → 3 → 4곳·탭 라벨 `코스 5` → `코스 6`·토글이 `제거`로 재도색·드로어 안 열림. 모바일 폭 콘솔 에러 0건. ⚠️ **한계**: OS 수준의 실제 손가락 터치는 이 툴체인으로 발생시킬 수 없다 — 위 ①은 "터치를 삼키는지"를 이벤트 레벨에서 판정한 것이고, pinch-zoom·롱프레스 판정까지 포함한 실기기 확인은 별도다. 🆕 **모바일 폭에서 새로 발견한 결함(F7 후보)**: 390px 의 좁은 지도에서는 마커가 심하게 겹쳐 **가려진 마커의 토글이 자기 중심점에서도 도달 불가**하다. 실측(코스 3곳 + POI 4곳 = 토글 6개)에서 `elementFromPoint(토글 중심)` 이 **6개 중 1개**(대릉원, 교리김밥과 CSS 9px 간격)에서 **옆 마커 본체**를 되돌려 줬다 → 그 자리를 누르면 담기 대신 **엉뚱한 POI 의 상세 드로어가 열린다**(실제로 재현됨). 데스크톱은 지도가 넓어 덜하지만 원인은 같다(마커 클러스터링·겹침 시 z-order 규칙 없음). F6 의 DoD 조건은 아니므로 이 Task 를 막지는 않는다. ⏳ **남은 것**: **저장 후 재진입 유지**(DoD 의 영속 절반)는 로그인이 필요해 사용자 대행이다. 비로그인 상태에선 저장 버튼 라벨이 규칙대로 `저장`(`owned`=false)이고 `변경 저장`/`저장됨` 전이는 소유 코스에서만 나타난다. → 순서 §그룹F F6
- [ ] `◐` **F8 · 남은 도달 불가 2건 — 지도 컨트롤 겹침 · 밀집 코스 마커** — **㉠ 완료 / ㉡ 미착수**(사용자가 ㉠ 만 먼저 진행하기로 승인). 의존: F7.  **F7 실측 검증에서 발견**했고 둘 다 F7 채택안(POI 클러스터링 + 코스 예외)이 다루지 않는 원인이라 별도 Task 로 뺐다. **㉠ 지도 컨트롤에 마커가 가려짐** — `KakaoMap` 의 확대·축소·현위치 버튼 묶음은 `absolute bottom-3 right-3 z-[3]` 의 **32×108px** 이고 카카오 `CustomOverlay` 는 그보다 아래 레이어라, 지도 오른쪽 아래 구석에 놓인 마커는 배지·토글 중심이 컨트롤에 덮인다 → 담기 대신 **지도가 확대된다**. 데스크톱 1280px 실측에서 재현(`경주 장항리 서 오층석탑` 의 배지와 토글 **둘 다** `elementFromPoint` 가 `확대` 버튼을 되돌려 줬다). 폭과 무관한 구조적 원인이라 모바일에서도 같은 자리면 재현된다. **㉡ 밀집 코스 마커** — 코스 마커는 접지 않으므로(F5 DoD: 순번 배지·경로선 유지) 한 Day 의 코스가 **5곳 이상 반경 300m 안**에 몰리면 `spreadOverlaps` 의 고리 탐색(반지름 `minSep`~`minSep×1.2`, 12슬롯 = 중심 포함 약 7자리)이 포화돼 빈 자리를 못 찾고 **겹침을 남긴다**(설계상 "위치 왜곡 < 겹침" 이라 옮기지 않는 쪽을 택했다). 시뮬레이터가 잰 경계: 코스 **5~6곳**이면 **그룹 배지**가 가려져 펼칠 수 없고, **7곳 이상**이면 **코스 마커의 토글 자체**가 가려진다. 라이브 3회(경주 실코스, Day1 5~8곳)에서는 코스가 시내 전역에 흩어져 **재현되지 않았다** — 황리단길처럼 도보 반경에 몰린 하루 일정에서 드러나는 조건이다. **후보안**: ㉠ 컨트롤 rect 를 `getBounds()`+컨테이너 크기로 좌표로 환산해 **금지 구역(가상 항목)** 으로 `spreadOverlaps` 에 넣는다 / 컨트롤을 지도 밖(패널 헤더)으로 옮긴다. ㉡ 고리를 필요한 만큼 넓히되 상한을 둔다(위치 왜곡 증가) / 코스 마커도 **접되 순번을 유지하는 전용 그룹 배지**로 보여 준다. **DoD**: 위 두 상황에서 각 마커의 토글이 눌리고 오조작이 없다 — 모바일·데스크톱 폭 라이브 실측 + 결정적 시뮬레이터에서 해당 데이터셋 통과. ✅ **㉠ 구현·검증 완료(2026-08-29)**: `mapCluster` 에 **금지 구역**을 더했다 — `ForbiddenBox`(겹침 해소 좌표계 사각형)와 `anchorKeepOut(rect)`(크롬 사각형을 **클릭 대상 중심** 기준으로 부풀려 *앵커가 들어가면 안 되는* 영역으로 변환: `left−9 · right · top+5 · bottom+16`), 그리고 `spreadOverlaps(items, scale, forbidden)` 의 후보 판정에 `canPlace = isFree && !isBlocked` 를 끼웠다 — **겹침 회피와 같은 탐색 경로를 쓰므로 규칙이 하나로 유지된다**. 항목이 하나뿐이어도 금지 구역이 있으면 비키도록 조기 반환 조건도 고쳤다(마커 하나만 뜬 지도에서 그게 하필 컨트롤 자리면 담기가 아예 불가능해진다 — 단위 케이스가 잡아낸 실결함). `KakaoMap` 은 `readScale` 을 **`readView`** 로 바꿔 `getBounds()` 한 번으로 배율과 금지 구역을 **함께** 읽고(따로 읽으면 이동 중 두 값이 다른 시야를 가리켜 마커가 엉뚱한 자리로 밀린다) `idle` 마다 갱신한다 — 배율과 달리 금지 구역은 **이동(pan)만으로도 바뀌기 때문**이다. 🔎 **검증 중 새로 발견**: 가리는 것은 우리 컨트롤만이 아니었다 — **카카오 SDK 가 컨테이너에 직접 붙이는 축척·로고(좌하단 135×19)** 도 마커 위 레이어라 그 위 배지·토글이 전부 축척 DOM 에 먹혔다. 그래서 `chromeRects` 가 **컨트롤 + 컨테이너 직속 자식 중 전체 크기가 아닌 것**을 함께 잡는다(클래스·구조에 기대지 않아 SDK 갱신에 강하다). **검증**: `npm run lint`·`npm run build` 통과 + node **14케이스** 통과(`anchorKeepOut` 4 · 금지 구역 10) + 라이브에서 재현된 형상(343×389 · 컨트롤 (299,269)-(331,377))을 그대로 옮겨 **금지 구역 1218자리 전수 스윕 → 전부 해소** + 앱 줌 흐름 시뮬레이션(경주형 × 7시드 × 모바일·데스크톱, 컨트롤을 최상위 불투명 사각형으로 둠) **82상태에서 컨트롤 가려짐 14 → 0**, **그 외 겹침 108 → 108** — 즉 **㉡ 을 악화시키지 않는다**(금지 구역이 자리를 줄이므로 이 확인이 필수였다). ✅ **라이브 실측**(실 백엔드 경주 · 코스 #77·#78·#79·#84 · 390px iframe): 수정 전 축척에 가려졌던 **`경주 재매정`** 이 수정 후 **43px 비켜나** 금지 구역 밖으로 나갔고(투영을 역산해 원래 자리와 실제 자리를 비교) 그 상태의 14개 항목 전부 도달 가능. 펼치기 흐름 전 단계도 가려짐 0. ⏳ **미해결로 남긴 것**: ① **㉡ 미착수** ② 합성 마우스 드래그로 만든 상태에서 컨트롤 겹침 1건(`경주 인왕동 사지`)이 관찰됐다 — 순수 함수 전수 스윕과 실제 조작(펼치기·줌 버튼) 상태에서는 재현되지 않아 **합성 드래그가 카카오의 정식 `idle` 을 못 띄워 금지 구역이 갱신되지 않은 것으로 의심**되나 확정하지 못했다. → 다음에 **실제 손 제스처(또는 카카오 SDK 의 pan API)로 재확인** 필요. → 순서 §그룹F F8
